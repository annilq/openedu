# 娃娃日常学习应用 · 产品方案（V0.1 草稿）

> 通过 `/grill-with-docs` 访谈对齐产出。本文档为决策基线，后续落地以本方案 + ADR + 术语表为准。

## 1. 背景与目标

- **用户**：两个娃娃（一个二年级、一个四年级）；产品规划覆盖**小学 → 初中**全学段。
- **痛点**：日常学习缺乏系统性的练习编排与进度跟踪；家长想当"班主任"但缺工具；低龄娃娃学习动力难维持。
- **目标**：用 AI 为娃娃生成对齐教材的日常练习，家长布置任务、查看进度、管控内容与时长，逐步培养娃娃的学习习惯与自驱力。
- **一期北极星指标**：两个娃娃每周各自完成 ≥5 次 AI 生成练习，家长每周可看到一份薄弱点报告。

## 2. 用户与角色

| 角色 | 说明 | 核心权限 |
|------|------|----------|
| 家长（管理员） | 应用的"班主任" | 布置每日任务、查看所有娃娃进度与报告、设定内容范围与时长上限、查看 AI 交互日志 |
| 娃娃（二年级） | 独立账号 | 接收任务、做题打卡、查看自己的积分/徽章；受限内容与时长 |
| 娃娃（四年级） | 独立账号 | 同上，进度与二年级完全隔离 |

> 默认「共用一台平板，娃娃切换账号登录」；各有设备的场景架构不变，仅登录方式调整。

## 3. 核心能力分期（避免范围蔓延）

| 阶段 | 能力 | 说明 |
|------|------|------|
| **一期 MVP** | 刷题练习(A) + 每日任务打卡(C) | AI 生成客观题，即时批改与解析，家长布置每日任务 |
| **二期** | 错题本 + 遗忘曲线复习(D) | 自动归集错题，按间隔重复提醒复习 |
| **三期** | AI 伴学答疑(B) | 娃娃自由提问，AI 讲解；需启用内容安全双层防护 |

## 4. 学科与内容范围（一期）

**原则**：对齐教材；前期 AI 联网检索知识点，后期建教材知识库（RAG）；**客观题优先、AI 易批改**。

| 学科 | 一期包含 | 暂不做 |
|------|----------|--------|
| 数学 | 计算/口算（加减乘除、竖式）、简单应用题（文字题） | 几何证明、复杂方程 |
| 语文 | 字词（看拼音写汉字、组词、近反义词、成语填空）、短篇阅读理解（100字内，选择/填空） | 作文、长阅读主观题 |
| 英语 | 单词（拼写、中英互译、读音匹配）、简单语法（单选/填空） | 口语对话、写作 |

**内容来源路线**：
- 前期：agent 通过联网检索抓取对应年级/教材知识点 → 出题。
- 后期：建教材知识库（向量库），AI 意图识别 → 匹配知识点 → 出题/讲解（对应 LangChain RAG 能力）。
- 领域接口预留 `KnowledgeRetriever`、`IntentRouter`。

## 5. 技术架构

```
┌─────────────────────────────────────────────┐
│  Flutter 原生 App（平板优先，iOS/Android）      │
│  角色切换 / 做题 / 打卡 / streak·徽章 / 家长端   │
└───────────────────┬─────────────────────────┘
                     │  HTTPS
┌───────────────────▼─────────────────────────┐
│  Python 后端（Docker 容器化，本地起步·预留云）     │
│  ├─ 领域服务接口（自封，业务只依赖此层）           │
│  │    QuestionGenerator / Grader /             │
│  │    KnowledgeRetriever / IntentRouter         │
│  ├─ LangChain 编排层（agent/chains/tools）       │
│  ├─ Provider 抽象（适配多厂商 LLM，模型先不定）     │
│  ├─ 内容安全层（双层防护 + 拦截转家长）            │
│  └─ API / 鉴权 / 多账号                          │
└───────────────────┬─────────────────────────┘
                     │
            ┌────────▼────────┐
            │  PostgreSQL      │
            │ （本地起步）      │
            └─────────────────┘
```

- **前端**：Flutter 原生 App，平板优先。
- **后端**：Python + LangChain；自封「领域服务接口 + 框架 adapter」，LangChain 自带 provider 抽象（不重复造轮子）。
- **数据库**：PostgreSQL（本地 Docker 起步，架构预留云部署）。
- **AI**：模型暂不定；通过 LangChain provider 抽象适配多厂商，未来插拔替换。
- **Pi / Eve**：不作为运行时引擎（与 Python 后端割裂）；Pi 可作开发期编码助手。

## 6. 非功能约束

- **内容安全（硬门槛）**：①系统层 prompt 锁死"仅适合 X 岁、纯学习相关"；②输出后校验（敏感词/越狱检测，异常拦截转家长）；③家长可查所有 AI 交互日志。一期风险低，但框架先搭，三期直接套用。
- **家长管控**：家长可设定每日使用时长上限与内容范围。
- **激励（轻量）**：每日打卡 streak、学科积分、知识徽章、家长语音/文字表扬。纯前端状态，不依赖 AI，一期可做。
- **合规（标红）**：教材（人教版等）有版权。**开发/自用阶段可用；凡做成对外分发产品，必须先取得教材版权授权或改用公版/自编内容。** 上线前硬门槛。

## 7. 分期路线图（里程碑建议）

**一期（MVP，目标：两周内可用骨架 + 出题闭环）**
1. 后端：Python + FastAPI + LangChain 骨架；QuestionGenerator / Grader 领域接口；接一个默认 provider（先 mock 或免费模型）。
2. 数据：PostgreSQL 表结构（用户、娃娃、任务、题目、作答记录）。
3. 前端：Flutter 登录/角色切换、做题页、打卡页、家长任务布置页。
4. 内容：数学计算 + 语文字词 + 英语单词，AI 生成 + 即时批改。

**二期**
- 错题本、遗忘曲线复习调度、薄弱点报告（家长端）。

**三期**
- AI 伴学答疑（启用内容安全双层防护）、教材知识库 RAG、意图识别路由。

## 8. 待确认与风险

| 项 | 状态 | 备注 |
|----|------|------|
| 大模型选型与成本 | 待定 | 一期可用免费/低成本模型验证，后期按质量与成本定 |
| 教材版权 | 上线前必须解决 | 已在合规约束标红 |
| 后端部署运维 | 本地 Docker 起步 | 架构按云设计，未来可搬轻量云 |
| 离线能力 | 未定 | 一期需联网出题；已生成题目可缓存离线做（后续考虑） |
