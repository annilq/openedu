# Impeccable Design Guidelines — 娃娃学习 (kids_learn)

本项目为「娃娃日常学习 App」，Flutter + Riverpod，**平板优先**。

> **ADR-0014 确认（修订 ADR-0003）**：采用**双模式**——家长工作台（Parent Mode，密排专业）与娃娃学习台（Child Mode，更大字号+学科色+适度趣味）。两者共用 surface/spacing/radius 令牌，仅在字号阶梯、语气与学科色权重上分化。暖绿主题已删除，保留中性+靛蓝基底 + 受控学科色。

## Design Context

### Users
- **家长（Parent Mode，工作台）**：创建/管理娃娃账号、生成任务、查看进度（正确率、连续打卡）、管控 AI 额度。需要 Linear/Notion 级的专业工作台观感——密排信息、精确排版、键盘快捷、微交互反馈。沿用 15sp 密排基线。
- **孩子（Child Mode，学习台）**：小学低年级（1-3 年级）儿童，回家后完成家长布置的每日任务。低龄、需动力——独立字号阶梯（~17sp 基线）、更圆角、学科色凸显、暖容器、打卡/徽章有轻动效。与 Parent Mode 共享令牌，仅密度与语气不同。

### Brand Personality
- **关键词：精致、专业、克制**（refined / professional / restrained）。
- 情感目标：家长感到「这是一个认真的工具」而非「玩具」；界面靠工艺精度而非花哨色彩建立信任。

### Aesthetic Direction
- **Linear/Notion 风格中性主题**：微暖白 surface（`#FBFAFA` 亮 / `#0F0F0E` 暗）+ 纯白卡片（`#FFFFFF`）+ 1px 极细描边分层（`#ECECEA`），**无阴影**。单一靛蓝强调色（`#5E6AD2` 亮 / `#7B82EA` 暗）用于 selection/focus/progress/link。CTA 按钮近黑底白字。
- **亮暗双主题**：暗色暖中性深炭（非纯黑），保留靛蓝强调色提亮。
- **密排统一字号**：正文 15sp 基线，双端共用。Inter（西文/数字，变量字体打包于 `assets/fonts/Inter.ttf`）+ HarmonyOS Sans SC（CJK 回退）。每个字号有对应 tracking，行高按用途区分。
- **表面五级层次**：surface（内容区）/ surfaceRaised（卡片）/ surfaceHover（hover 态）/ surfaceSunken（侧栏/凹槽）/ surfaceActive（选中药丸）。禁止手动 withValues(alpha:) 透明度变体。
- **语义色降饱和**：positive（极淡绿 `#EFF5EC`）/ warning（极淡琥珀 `#FAF3E8`）/ error（极淡红 `#FCE8E6`）/ info（极淡靛蓝 `#EEF0FC`）。用于 badge/chip/icon 容器底色，不用于大面积背景。
- **学科色（Subject Accents，ADR-0014 新增）**：在中性+靛蓝基底上叠加受控多色——数学=蓝 `#4C7DE0`、语文=玫瑰 `#D85A6E`、英语=翠绿 `#3FA67E`（均中饱和，预留扩展槽）。每个学科配 `container`+`fg` 降饱和对，仅用于学科标识/进度条/图标容器，不用于大面积背景；Parent Mode 数据可视化中低调使用，Child Mode 凸显。
- **交互 Tier 1+2**：全局 hover 态、press 反馈、转场分级（120ms 交互 / 200ms 状态 / 300ms 页面）、loading→loaded crossfade、tooltip（延迟 500ms）、Cmd+K 命令面板、侧栏首字母快捷键。**无半透明 focus ring**——focus 用 bg 变化表达。
- **反参考（anti-reference）**：
  - 不用暖绿主题（已删除）
  - 不用大字护眼基调（≥20sp 已废弃）
  - 不用重阴影分层（Linear 用描边不用阴影）
  - 不用高饱和荧光色
  - 不用半透明 focus ring
  - 不用俗套 AI 紫蓝渐变

### Design Principles
1. **双模式、共享令牌**：Parent Mode 密排专业、Child Mode 大字趣味，靠令牌分化而非两套独立体系；一致性仍来自令牌。
2. **精致但克制**：靠 tracking/字重/描边/hover 的精确校准建立品质感，而非色彩堆砌。一色（靛蓝）+ 四档降饱和语义色，到此为止。
3. **描边分层，无阴影**：所有层次靠 1px 极细描边 + surface 色阶表达，不用 BoxShadow。
4. **亮暗双主题，中性一致**：亮色微暖白、暗色暖中性深炭，均走令牌，避免纯黑。
5. **一致性来自令牌而非临时值**：所有间距/圆角/字号走 `AppSpacing / AppRadius / AppText`，禁止 `Colors.*` 硬编码。
6. **转场分级**：120ms 交互态 / 200ms 状态切换 / 300ms 页面进入。loading→loaded 用 crossfade。不用统一 200ms 一刀切。
7. **反馈即信任**：hover（surface 变色）、press（bg 深一档）、tooltip（500ms 延迟）、骨架屏加载、空态引导、温柔错误+重试——每个状态都有完整的、符合专业语气的反馈。

## Typography Scale

| 令牌 | 字号 | 字重 | Tracking | 行高 | 用途 |
|---|---|---|---|---|---|
| displayLarge | 22sp | w700 | -0.6 | 1.2 | 页面大标题 |
| displayMedium | 20sp | w700 | -0.4 | 1.2 | 模块标题 |
| headlineLarge | 18sp | w600 | -0.3 | 1.25 | 卡片标题 |
| headlineMedium | 17sp | w600 | -0.2 | 1.3 | 区块标题 |
| titleMedium | 16sp | w600 | -0.1 | 1.35 | 列表项标题 |
| titleSmall | 15sp | w600 | 0 | 1.35 | 小标题 |
| bodyLarge | 15sp | w400 | 0 | 1.5 | 正文 |
| bodyMedium | 14sp | w400 | 0 | 1.45 | 次正文 |
| bodySmall | 13sp | w400 | 0.2 | 1.45 | 辅助文字 |
| labelLarge | 14sp | w600 | 0.2 | 1.35 | 按钮文字 |
| labelMedium | 13sp | w600 | 0.3 | 1.35 | 标签/Chip |
| labelSmall | 12sp | w500 | 0.3 | 1.35 | 极小辅助/tooltip |

- 数字一律 `fontFeatures: [FontFeature.tabularFigures()]`
- 西文/数字走 Inter，CJK 自动回退 HarmonyOS Sans SC
- `fontFamilyFallback: ['HarmonyOS Sans SC', 'Noto Sans SC']`

## Color Tokens

### 亮色

| 令牌 | 值 | 用途 |
|---|---|---|
| surface | `#FBFAFA` | 内容区背景 |
| surfaceRaised | `#FFFFFF` | 卡片/浮起容器 |
| surfaceHover | `#F4F4F2` | hover 态 |
| surfaceSunken | `#F4F4F2` | 侧栏/凹槽 |
| surfaceActive | `#EDEDF0` | 选中态药丸 |
| onSurface | `#1D1B17` | 主文字（暖中性近黑，禁纯黑） |
| onSurfaceVariant | `#6B7280` | 次文字 |
| outline | `#ECECEA` | 卡片/分隔线 1px 描边 |
| outlineHover | `#D1D1CE` | hover 描边 |
| accent | `#5E6AD2` | selection/focus/progress/link |
| onAccent | `#FFFFFF` | accent 上的文字 |
| cta | `#1D1B17` | CTA 按钮底色 |
| onCta | `#FFFFFF` | CTA 按钮文字 |
| semanticPositive | `#EFF5EC` | 正向 badge 底 |
| semanticPositiveFg | `#3B7A2D` | 正向 badge 文字 |
| semanticWarning | `#FAF3E8` | 警告 badge 底 |
| semanticWarningFg | `#8A6D1F` | 警告 badge 文字 |
| semanticError | `#FCE8E6` | 错误 badge 底 |
| semanticErrorFg | `#B91C1C` | 错误 badge 文字 |
| semanticInfo | `#EEF0FC` | 信息 badge 底 |
| semanticInfoFg | `#4338CA` | 信息 badge 文字 |

### 暗色

| 令牌 | 值 | 用途 |
|---|---|---|
| surface | `#0F0F0E` | 内容区背景 |
| surfaceRaised | `#161615` | 卡片/浮起容器 |
| surfaceHover | `#242423` | hover 态 |
| surfaceSunken | `#1A1A19` | 侧栏/凹槽 |
| surfaceActive | `#2A2A28` | 选中态药丸 |
| onSurface | `#F5F5F4` | 主文字 |
| onSurfaceVariant | `#9CA3AF` | 次文字 |
| outline | `#2A2A28` | 描边 |
| outlineHover | `#3D3D3A` | hover 描边 |
| accent | `#7B82EA` | selection/focus/progress/link |
| onAccent | `#0F0F0E` | accent 上的文字 |
| cta | `#F5F5F4` | CTA 按钮底色（暗色反色） |
| onCta | `#0F0F0E` | CTA 按钮文字 |
| semanticPositive | `#1A2E1A` | 正向 badge 底 |
| semanticPositiveFg | `#86C060` | 正向 badge 文字 |
| semanticWarning | `#2E2618` | 警告 badge 底 |
| semanticWarningFg | `#D4A82E` | 警告 badge 文字 |
| semanticError | `#2E1A1A` | 错误 badge 底 |
| semanticErrorFg | `#F08888` | 错误 badge 文字 |
| semanticInfo | `#1A1A2E` | 信息 badge 底 |
| semanticInfoFg | `#9BA0E8` | 信息 badge 文字 |

---

## Dual-Mode Type Scale（双模式字号阶梯，ADR-0014）

Parent Mode 沿用上表（15sp 基线）。Child Mode 在 Parent 基础上整体放大一档、放宽行高，提升低龄可读性：

| 令牌 | Parent | Child | 用途 |
|---|---|---|---|
| displayLarge | 22 | 26 | 页面大标题 |
| displayMedium | 20 | 24 | 模块标题 |
| headlineLarge | 18 | 21 | 卡片标题 |
| headlineMedium | 17 | 19 | 区块标题 |
| titleMedium | 16 | 18 | 列表项标题 |
| bodyLarge | 15 | 17 | 正文（Child 基线） |
| bodyMedium | 14 | 16 | 次正文 |
| bodySmall | 13 | 14 | 辅助文字 |
| labelLarge | 14 | 15 | 按钮文字 |

- Child Mode 数字同样 `tabularFigures()`；tracking 与 Parent 一致。
- 切换由独立于 `AppThemeMode` 的 `AppUserMode`（parent/child）控制，主题 build 时按 mode 选字号集合。

## Subject Accent Tokens（学科色令牌，ADR-0014）

在中性+靛蓝基底上叠加，中饱和、仅小面积使用。

### 亮色

| 令牌 | 值 | container | fg | 学科 |
|---|---|---|---|---|
| subjectMath | `#4C7DE0` | `#E9F0FC` | `#2B5BB0` | 数学 |
| subjectChinese | `#D85A6E` | `#FCEBEE` | `#B23A4E` | 语文 |
| subjectEnglish | `#3FA67E` | `#E7F5EE` | `#2C7C5C` | 英语 |
| subjectReserved* | 预留 | 预留 | 预留 | 未来学科（科学/历史…） |

### 暗色（提亮）

| 令牌 | 值 | container | fg |
|---|---|---|---|
| subjectMath | `#7FA8F0` | `#1B2740` | `#A9C4F5` |
| subjectChinese | `#E78FA0` | `#3A1F26` | `#F2B6C2` |
| subjectEnglish | `#6FC4A2` | `#163026` | `#A7E0C6` |

- **约束**：学科色仅用于学科标识 / 进度条 / 图标容器 / 小面积 chip；禁止作大面积卡片背景或全屏底色。`Parent Mode` 仅在 mastery/进度可视化中低调使用，`Child Mode` 可凸显。

## Responsive Breakpoints（响应式断点，ADR-0014）

| 档位 | 宽度 | 导航 | 备注 |
|---|---|---|---|
| compact | < 700pt | 底部导航（Child）/ 抽屉（Parent） | 手机、平板竖屏窄 |
| medium | 700–1023pt | 可收起侧栏（64/240） | iPad mini / Air 竖屏 |
| expanded | ≥ 1024pt | 展开侧栏 240 + 内容 | iPad Pro / 桌面 |

- 内容区 maxWidth：compact 满宽留 16 边距；medium/expanded 居中 `maxWidth: 1080`（沿用 `ParentOverviewView` 现有约束）。
- 导航壳由 `DesktopShell` 升级为 `AdaptiveShell`：按 `MediaQuery` 宽度选底部导航 / 抽屉 / 侧栏。

## Motion · Celebrate（适度趣味动效，ADR-0014）

保留 interaction 120 / state 200 / page 300。新增：

- `celebrate` **450ms easeOutBack**：徽章解锁、连击 +1、打卡成功反馈。**仅 Child Mode 启用**；Parent Mode 不触发。
- 微奖励：correct 题 `scale-pop` 120ms；streak flame 轻微 pulse。克制，不游戏化过度。

## 规范一致性（落地前必须）

- 禁止 `withValues(alpha:)` 透明度变体（已有 13+ 处违反，需审计清除，改用令牌的 `container`/`surfaceSunken` 等实体色）。
- 所有卡片走 `AppCard`（含 1px 描边）；散落 `Container` 直接着色视为违反。
- 出现硬编码 `Colors.*` / 魔法十六进制即视为违反；一律走令牌。
