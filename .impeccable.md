# .impeccable.md

设计上下文单一事实源。`app_theme.dart` / `app_motion.dart` 等处的「见 .impeccable.md」注释指向本文件。
落地决策见 `docs/adr/0044-neo-brutalist-visual-language.md`、`docs/adr/0045-adaptive-layout-breakpoints-and-keyboard-access.md`；领域术语见 `CONTEXT.md`。

> 注：本文件此前**长期缺失**，但代码注释已引用它作为设计依据（`app_theme.dart:117`、`app_motion.dart:5`），
> 导致设计约定散落在注释里、无人维护、风格漂移。2026-09-15 首次建立。

## Design Context

### Users（用户与场景）

两个角色共用一套视觉语言（ADR-0044），物理条件不同但强度统一：

- **家长（Parent）** — 家长工作台。平板上做三件事：看掌握度看板、管错题列表、出任务表单。
  特征是**数据密集**，一屏要扫读十几个知识点行。情绪目标：一眼看清、有掌控感、不焦虑。
- **儿童账户（Child Account）** — 娃娃学习台。一屏只有 3–4 个大按钮 + 一张错题卡，纯触控。
  字号整体放大一档（ADR-0014 `_childScale`），低龄可读性是硬需求。情绪目标：想点、点了有反馈、答错不丢脸。

两端**只差字号与控件尺寸**，色饱和度、色块面积、圆角、动效强度完全一致。不设 parent/child 双色板。

### Brand Personality（品牌人格）

**直率 · 有力 · 明朗**

不卖萌、不端着、不用柔和阴影讨好。撞色和硬描边带来的「劲儿」要能被 8 岁孩子和 40 岁家长同时接受：
孩子看到的是鲜明的色块和会弹的按钮，家长看到的是清楚到不用想的边界。

### Aesthetic Direction（美学方向）

**新粗野（Neo-Brutalism）**：高饱和原色撞色 + 2px 墨黑描边 + 无模糊硬阴影 + 弹性动效。

- **参考**：Gumroad 早期界面、Figma 社区新粗野作品、Duolingo 的按压下沉手感、Linear 的**反面**。
- **反面参考（明确不要）**：① 本仓库 2026-09 前的 Linear 克制风（中性灰白 + 1px 描边 + 无阴影 + 靛蓝小面积点缀）；
  ② 渐变紫蓝的「AI 生成 SaaS 风」；③ 柔和圆角 + 模糊阴影的 Material 2；④ 零圆角的冷硬几何扁平。
- **主题**：亮色为主（纸底 `#FDFBF7`）。暗色模式保留但**不优先**——儿童端在暗底上撞色会失控。

### Layout（自适应与断点）

布局决策**只**基于「可用宽度」（`LayoutBuilder` 的 `constraints.maxWidth`）。**不按设备形态分支**——
Flutter 应用跑在可缩放窗口、多窗口与画中画里，设备形态不等于可用空间；且 master-detail 的详情栏比窗口窄得多，
按 `MediaQuery.size.width`（屏宽）算出来的约束必然溢出。

三档断点与内容宽度都是令牌（`AppLayout`，ADR-0045），**禁止写裸数字**：

| 断点 | 值 | 布局 |
|---|---|---|
| `compactMax` | 700 | 紧凑：娃娃端底栏 / 家长端汉堡 + 左抽屉 |
| `largeMin` | 1200 | 大屏：侧栏 + 内容；有详情时 master-detail 双栏 |

| 内容宽度 | 值 | 用途 |
|---|---|---|
| `contentWide` | 1080 | 家长端工作区（列表 / 仪表盘 / 表单）——壳统一兜底 |
| `contentReading` | 820 | 答题与阅读区 |
| `contentCard` | 520 | 居中结果卡 |
| `contentNarrow` | 480 | 登录、表单对话框 |
| `contentEmpty` | 440 | 空态 / 提示卡 |
| `contentFloat` | 380 | 浮层（悬浮助手） |

宽度上限由 `AdaptiveShell` 统一兜底（`Align(topCenter)` + `ConstrainedBox`）——**不是 `Center`**，`Center` 竖向
也会居中，会把内容不足一屏的页面浮到屏幕中央，与本仓「内容贴顶自然布局」口径冲突。页面需要更窄时自带更强的
`ConstrainedBox`，**内层更紧者生效**。`Navigator.push` 的整页不在兜底范围内，必须自查。

结构尺寸同样收口 `AppLayout`：

| 令牌 | 值 | 用途 |
|---|---|---|
| `tapTarget` | 44 | 方形触控目标（侧栏头部按钮 / 紧凑顶栏菜单键 / 行内图标操作） |
| `sidebarHeaderPadding` | LTRB 8/12/8/4 | 侧栏头部四向内边距——左缘必须等于导航项左缘 |
| `sidebarMenuWidth` | 224 | 侧栏菜单浮层的**外框**宽（= `sidebarExpanded - 2 × sm`）；内容侧须再减 `popoverChrome` |
| `popoverChrome` | 20 | 浮层外框相对内容多出的开销（主题内边距 2×8 + 2px 描边 2×2） |
| `menuMaxHeight` | 400 | 下拉浮层最大高度（超出后浮层内部滚动） |

**浮层锚点**：一律用显式 `ShadAnchor`，**不用** shadcn 默认的 `ShadAnchorAuto`——它的默认是
`bottomCenter ↔ bottomCenter`（相对触发卡**水平居中**）且底层还会再居中一次，触发卡比浮层窄时两侧必溢出、
左溢部分还会被屏幕左缘钳住。两档取值：**开在下方左缘齐平**（`ShadAnchor(offset: Offset(0, xs))`，
浮层与触发卡共用同一条左边缘）；**轨态向右侧飞出**（`overlayAlignment: Alignment.topRight`，
不压住轨道的导航图标）。注意命名方向与直觉相反：`childAlignment` 作用在**浮层**上、
`overlayAlignment` 作用在**触发卡**上。

**钉高的容器里，内容不会自动垂直居中**——`ShadCard` 内部固定是 `Row(crossAxisAlignment: start)` →
`Column(mainAxisSize: min)`：内容只按自身高度收缩并**贴上沿**。内容自撑高度时（绝大多数卡片）容器高 = 内容高，
看不出问题；一旦外面用 `SizedBox(height:)` 把卡片钉高（侧栏头部触发器就钉到 `tapTarget`），差值全落到下沿——
实测卡片 44 高、内容 28 高，上边距 1、下边距 15，内容中心比容器中心高 7px。钉高的调用点必须自己包一层
`Center`；**不要**去 `AppCard` 内部居中，那会动到全站每张卡片的布局。守卫
`test/sidebar_header_layout_test.dart`。

### Interaction（选中 / 悬停 / 焦点）

**同一屏里「选中」只能有一种视觉语言**——无描边药丸 + `surfaceActive` 填充 + accent 图标（`AppSidebarItem`、
浮层选项、抽屉项共用）。**不要用带描边的卡片表示选中**：那会在已带描边的浮层里再套一层带边的盒子（盒中盒），
把「选中」和「容器边界」两件事混成同一种视觉。

三态分层，互不覆盖：

| 态 | 画法 | 令牌 |
|---|---|---|
| 悬停 | 底层药丸底色 | `AppFocusableAction(hoverHighlight: true)` → `surfaceHover`（比选中浅一档） |
| 选中 | 子项自带底色（盖住悬停） | `surfaceActive` |
| 焦点 | `foregroundDecoration` 描边（覆盖绘制、**不参与布局**） | `accent` × `AppElevation.borderWidth` |

- 悬停走 `AppFocusableAction.hoverHighlight`，**不要**各组件自建 `StatefulWidget + MouseRegion` 手搓一遍。
- 焦点环必须用 `foregroundDecoration`：用 `decoration` 会让描边参与布局，聚焦时元素尺寸跳动。
- 焦点环取 `accent` 而非墨黑——墨黑描边全站都是，用墨黑画环会被读成「边框变粗了」。
- 任何可点区域都必须进焦点树（`AppFocusableAction`），**裸 `GestureDetector` 是 bug**：桌面端 Tab 跳不过去。

### Design Principles（设计原则）

1. **边界是功能，不是装饰。** 相邻高饱和色块对比度中位数仅 1.67（最低 1.00），没有墨黑描边它们在视觉上根本分不开。
   任何取消描边的「简化」都是破坏可读性。
2. **色块是强调件，不是背景纸。** 彩色填充面积 ≤ 卡片 40%，单屏色相 ≤ 3，列表行禁止整行填充。
   撞色的作用是让人一眼找到重点，不是把屏幕铺满。
3. **一个色只有一种合规文字，AA 是硬门槛。** 亮块配墨黑字、深块配白字，实测全部 ≥ 4.5:1。
   不允许为了「好看」换文字色——所有高饱和色配白字最高只有 4.78。
4. **学科永不只靠颜色。** 色相 + 明度差 + 几何标记三重编码（数学 ■ / 语文 ● / 英语 ▲）。红绿色盲是真实用户。
5. **动效要有质量差。** 用真弹簧（`SpringDescription` / `springster`）而非 `Curves.easeOutBack`——
   后者让所有元素共用一条曲线，多元素同时动时会「齐步走」，显得廉价。大卡片重、chip 轻。
6. **抽象最少化。** 不引入双色板、双圆角、双动效曲线。唯一保留的双份是 Child Mode 字号阶梯，因为它是硬需求。
7. **reduce-motion 是默认要处理的，不是加分项。** 隐式动画不会自动尊重系统设置，必须显式把 duration 归零。
8. **键盘可达性是设计的一部分，不是无障碍补丁。** 裸 `GestureDetector` 不进焦点树——Tab 跳不过去、Enter 点不动，
   而 `flutter analyze` 照不出来（不是类型错误）。任何可点区域一律走 `AppFocusableAction`；焦点环用
   `foregroundDecoration` 画（不参与布局、不跳尺寸），颜色取 `accent` 而非墨黑（墨黑到处是描边，
   会被读成「边框变粗了」而不是「焦点在这里」）。

### Accessibility（无障碍底线）

- 文本对比度 ≥ 4.5:1（WCAG AA），非文本图形元素 ≥ 3:1。
- 学科色在红绿色盲下可辨（靠明度差 + 形状兜底，不靠色相）。
- `MediaQuery.disableAnimations` 为 true 时所有动画退化为静态或零时长。
- 触控目标：家长端 ≥ 32px，儿童端 ≥ 40px（沿用 `AppControl` 现有推导）。
- 键盘：所有可点区域须能被 Tab 到、被 Enter/Space 激活，且焦点可见（对比度 ≥ 3:1）。
