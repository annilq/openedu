# 2026-09-21

## 分析：家长端「个人信息页被任务详情遮挡」（只诊断未改码）

- 根因：`home_screen.dart` 的覆盖层清理不对称。`_parentTap()`（侧栏）显式清 `_reviewingTask`/`_editingChild`，
  `_onProfileTap()`（底部「我的」/ `AdaptiveUserBlock`）只置 `_showProfile = true`，**不清覆盖层**。
- 链路：任务卡 → `_navigateToReview` → `_reviewingTask ≠ null` → `_buildParentDetail()` 返回审核页 →
  `AdaptiveShell._singleColumn = widget.detail ?? widget.body`，中屏/紧凑档 detail **整幅顶替 body**，
  `_buildParentPage()` 里的 `ProfileScreen` 压根没进树（不是被盖，是没渲染）。大屏 ≥1200 是 5:8 双栏，表现不同。
- 同病三处（都绕过 `_parentTap` 就没清层）：`_onProfileTap`(117) / `_onNavigateToAddChild`(124，清了 editing 没清 reviewing) /
  `_onNavigateToEditChild`(146，`_buildParentDetail` 优先级 review > edit → 审核中编辑娃娃资料被吞)。
- 附带：进个人信息时 `_parentNavIndex` 未复位 → 侧栏「任务」与「我的」同时高亮。
- 修法方向：把「清覆盖层」收口成一个 `_clearOverlays()`（或让所有导航入口统一走 `_parentTap`），
  profile 视作一个伪索引而不是与 `_parentNavIndex` 并列的布尔。

## 修复：复习页「返回」→ 白屏 + 热重启报 `_history.isNotEmpty`（3 commit）

- **根因**：`ReviewScreen` 在娃娃端是 `IndexedStack` 页签、**不是 push 路由**，页内两处
  `Navigator.of(context).pop()`（空态「返回」/ 完成卡「返回首页」）弹的是**根导航栈最后一条路由（整个 App）**。
  弹空后界面全白但**不立即报错**（没人再触发 Navigator 重建），按 R 热重启触发 reassembly 才炸
  `NavigatorState.build: assert(_history.isNotEmpty)`（navigator.dart:5950）。
- **判据**：`NavigatorState.build` 断言 `_history.isNotEmpty` + «MediaQuery 是 error-causing widget»
  （`main/app.dart:155` 包在 `CupertinoApp.builder` 里）＝ 根 Navigator 被弹空，**别去查 MediaQuery**。
- **修法**：出口回调注入（`ReviewScreen.onExit` / `ReviewEmptyView.onBack`），home 翻成 `_switchChildTab(0)`；
  未注入时退 `Navigator.maybePop`（根栈只剩一条路由时 `canPop=false` → no-op，与 `AppTopBar`/`AppPushedPage` 同口径）。
- **同批两处遗留**：① `_done` 永久为真（页签常驻不重建 → 复习完一次后新到期题再也进不来），
  判据用 `DueReviewLoading`（`answer()` 直接给 `DueReviewLoaded`，不经 Loading，故不会误清完成卡）；
  ② 顶栏 trailing 塞「打印图标 + 题号 chip」溢出 40px 槽位 32px（右缘外被裁）→ 计数并入标题「复习 1/3」。
- **守卫**：新增 `test/tab_screen_no_root_pop_test.dart`（静态扫壳内页签文件禁裸 `pop`）+
  `test/review_tab_exit_test.dart`（把页面放进「只挂一条路由」的 MaterialApp，点返回后断言占位内容还在——
  只断言按钮存在测不出「pop 弹错栈」）。
- ⚠️ **通用纪律**：任何「非 push 路由」的页面（壳页签 / 覆盖层）都**不得**直接 `Navigator.pop`，
  只能 `maybePop` 或走注入回调；`flutter analyze` 完全照不出来。

## 追问：「把受影响功能都做成页面」能否解决？（结论：能根治，但要动 ADR-0045）

- **关键不在「是不是页面」，在「是不是同一个状态」**。现在家长端有三个**并列**导航状态：
  `_parentNavIndex`（int）+ `_reviewingTask`/`_editingChild`（detail 载荷）+ `_showProfile`（bool）。
  三者并存 ⇒ 必然存在渲染优先级裁决，而裁决散落在各个回调里（`_parentTap` 记得清、`_onProfileTap` 忘了清）⇒ 迟早再漏。
  页面化（审核/编辑并入索引）让它们在同一维度上互斥，遮挡无从发生；profile 也必须并进去，否则仍是并列布尔。
- **个人信息本来就是页面**（`_buildParentPage` 里 `_showProfile` 直接返回 `ProfileScreen`）——对它「做成页面」无效，
  它的问题是被更高优先级的东西顶替。所以真正要页面化的是**审核与编辑**。
- **两种「页面」别混**：① 页内页面（并入 `_parentNavIndex`，侧栏仍可点）——推荐；
  ② `Navigator.push` 路由页——`HomeScreen` 直接挂在 `CupertinoApp` 根栈下（`main/app.dart:121`），
  push 会**连侧栏一起盖住**，用户根本点不到「我的」，问题以另一种方式消失；且要自带 `AppPushedPage`/宽度约束与返回。
- **代价**：① 放弃 ADR-0045 的大屏 master-detail（5:8 双栏），需修订该 ADR；
  ② 丢掉大屏「左侧换一条任务、右侧审核页跟着换」的能力（`ParentTaskReviewScreen.didUpdateWidget` 就是为它写的）；
  ③ 审核页宽度 8/13×1080≈665 → 整页 1080，题目卡片/操作条需视觉复核（页面自带 `AppContentFrame`）。
  iPad Pro 横屏 1366 ≥ largeMin 1200 → 双栏在主力设备上**确实生效**，不是纸面能力。
- **已确认事实**：`ParentTaskReviewScreen` 自带「返回首页」按钮(:191) + `AppContentFrame`(:206/235) → 整页化无需补顶栏/宽度；
  `ChildFormScreen` 自带 `AppTopBar(showBack:true)`(:152)，且创建态**已经是** index 5 的页内页面 → 统一成页面是一致性净收益。

## 分析：Flutter 大文件拆分（只诊断未改码）→ `docs/refactor/2026-09-21-flutter-ui-decomposition.md`

- 数据：lib 138 文件 / 25620 行；**16 个 ≥400 行文件 = 11.6% 文件数占 48.6% 行数**。
  ≥900 行三个：`app_theme.dart` 2744 / `dev/theme_preview.dart` 1189 / `models.dart` 902。
- **关键结论：别按行数统一处理，先分成因**。A 令牌文件塞 widget（app_theme）→ 拆；
  B 纯数据聚合（models）→ 按聚合拆 barrel；C 页面骨架/列表脚手架重复（8–10 处）→ 抽公共组件（收益最大）；
  D 页面本身是组合根/表单（home_screen / task_form / task_review / assistant_cards）→ **只拆内部私有子组件**；
  E `dev/theme_preview.dart` **不动**。
- 已确认重复（带行号）：`_statusLabel` ×2（bank:395 / assistant_cards:789，注释已自认「同口径」）、
  `_qtypeLabel` ×2（bank:403 / question_card:493）、手搓 `_EmptyHint` ×2（bank:816 / review:552，绕过 ADR-0051）、
  筛选 chip 四份手写（bank:554-593 / tasks:495-527 / form:658 / wrong:283-349）、
  分页列表+footer 四处（wrong:123 / tasks:215 / wrong_questions_screen:121 / bank:635）、
  导出确认+跳转两套（bank:111 / tasks:117）、页骨架 8–10 处（`AppContentFrame` 有 10 处调用点）。
- ⚠️ **纠正既有结论**：「`AppFocusableAction` 移到 shared/widgets 会成循环」**只对一半**。
  实测 `AppTheme`(328-1054) 里那些 widget **只在注释中出现，代码零引用**；真正需要它的是
  `AppCard`(:1435) / `AppIconAction`(:1829) / `AppTextAction`(:1886)——**它们自己也是 widget**。
  所以「单独搬 → 成环」成立，但**12 个 widget 一起搬 → 不成环**（搬后 app_theme 不再引用任何 widget，widget 侧单向 import 取 `colorsOf`）。
- 批次：P0 labels+空态 / P1 models barrel 拆（调用点零改）/ P2 抽 5 个 shared 组件 / P3 app_theme 12 widget 整体搬 / P4 大页面内拆私有件。
  P2 与 P3 顺序需二选一（先 P3 收敛令牌层，或 P2 后返工 import）。

## 落地：ADR-0058 前端文件规模与组件编写规范 + 规模守卫（拆分本身延后）

- **ADR 取号 0058**（0055 仍被 2026-09-20「资料库+RAG」轮预留，0056/0057 已用）。
- 新文件：`docs/adr/0058-frontend-file-size-and-component-authoring.md`、
  `frontend/test/file_size_guard_test.dart`；`AGENTS.md` 开发规范段新增一条 ADR-0058 摘要。
- 规范要点（阈值）：文件 **≤400 行** / `build` ≤60 / 私有方法 ≤40 / 单文件私有 widget ≤3；
  **一个文件只暴露一个公开物**；Page → Section → Widget 三层；
  抽 `shared/` 三条门槛（≥2 真实调用点 + 不 import features + 只抽形状不抽取值）；
  四态走 AppLoading/AppError/AppEmptyState；枚举→文案只在 `labels.dart`；**Rule of Two**。
- **守卫是「棘轮」不是「限时整改」**：未登记文件 ≤400 直接拦（主要执行力）；
  已登记 15 个超限文件基线**只许下调**（长回去就失败）；文件降到 ≤400 后必须删登记（第 3 条断言催删）。
  `lib/dev/**` 与 `*.g.dart`/`*.freezed.dart` 豁免。3 条断言实测全绿。
- ⚠️ **基线是在另一会话正在改 `home_screen`/`app_theme`/`adaptive_shell` 的时刻采集的**
  （home_screen 610→656、app_theme 2744→2740）。若那个任务还要让它们变长，
  需同步调高基线并在 commit 正文写理由——或等任务落地后重新采集一次基线。
- 拆分批次（P0–P4）**未执行**，等进行中任务结束后再动。

## 重构：AGENTS.md 改为纯渐进披露入口（细节下放 docs/agents/）

- **体积**：14328 字符 / 88 行 → **6409 字符 / 101 行**（−55%）；**最长行 1203 → 214 字符**。
  原来的「细节」是 8 条 300–1200 字符的超长 bullet（一整段规则正文塞在一行里）。
- **新结构**：`按任务找文档` 路由表（改什么 → 先读哪份）+ `硬约束速览（一句话版）`
  （每条一行结论 + 括号指向正文）+ 命令速查 / skills / 关键事实源。
- **细节下放到两份新文档**：`docs/agents/frontend.md`（分层 / 布局键盘 / 导航单一状态 ADR-0059 /
  文件规模 ADR-0058 / 助手页面形态 ADR-0047-0048 / 前端守卫一览）、
  `docs/agents/ai.md`（工具 schema ADR-0040 / 引擎失败归因 ADR-0038 / 卡片协议 ADR-0042-0054 /
  推理分流 ADR-0043 / AI 守卫一览）。
- **设计语言段整体删除**，不复述——已确认 4.78 对比度、40% 填充、描边三档 `borderWidthSm`/
  `borderWidthHairline`、`sidebarMenuWidth`/`menuMaxHeight` 等**都已存在于 `.impeccable.md`
  与 ADR-0044/0046**，无内容丢失。
- ⚠️ **ADR 编号差点撞车**：本会话用 0058（文件规模），另一会话同时把「导航单一状态」也写了 0058
  （AGENTS.md 里留了两条 0058 引用）→ 对方已改 0059。教训：取号后**立刻建文件**，别只在 AGENTS.md 里写号。
  已在 AGENTS.md 关键事实源加一条「取号前看目录最大号 + 查 `.workbuddy/memory/` 预留」。

## 落地：ADR-0059 家长端导航收敛为单一页面状态 + 移除 master-detail

- ⚠️ **ADR 号撞车**：本轮先用 0058，写完才发现同日另一会话已把 0058 给了「前端文件规模规范」
  （见上一条）。按「先预留者优先」**已把导航这篇改成 0059**，并同步改了 ADR-0045 / AGENTS.md /
  4 个前端文件里的引用。**取号前务必先读当天日志的「ADR 取号」行，不只是看目录空位。**
- 改法：`home_screen` 三个并列状态（`_parentNavIndex` + `_reviewingTask`/`_editingChild` + `_showProfile`）
  → 一个 `sealed _ParentPage _parentPage`（11 个分支），所有入口只调 `_go(page)`；
  `AdaptiveShell` 删 `detail` 入参与双栏分支；`AppLayout` 删 `masterFlex`/`detailFlex`。
- **为什么 `sealed` 是真修**：`_buildParentPage()` 的 `switch` 由编译器保证穷尽 → 新页面漏登记
  **编译失败**，而不是运行时「点了没反应」；清层纪律从「每个回调记得调」变成「没有第二个状态可清」。
- 验收：`flutter analyze` 零 issue，`flutter test` **260 全绿**（含新增 4 例）。
- ⚠️ **两个连带坑**（都是「改了 A 忘了 B」）：① `test/stretch_row_guard_test.dart` 的棘轮断言
  `removed.isEmpty` → master-detail 那个裸 `Row(stretch)` 站点消失后**必须同步删登记**，否则红；
  ② `list_density_test.dart` 注释里「大屏 detail 打开时主栏约 446」的前提没了，改成「侧栏展开后剩余 446」。
- 代价（已接受并写进 ADR）：大屏无双栏；丢「左侧换任务、右侧审核页跟着换」（`didUpdateWidget` 曾专为它写）；
  审核页宽 ≈665 → 1080，**题目卡与操作条未做视觉复核**（留作后续）。`largeMin 1200` 现无代码消费者。

## 回归并修复：娃娃端点「我的」打不开个人信息（ADR-0059 自身引入）

- **根因**：`_onProfileTap()` 是**两个角色共用**的回调（家长端侧栏底部 / 娃娃端侧栏底部与紧凑底栏
  都走 `_profileDestination`），改造后我只写了家长端那一份 `_go(const _Profile())`，
  娃娃端读的是 `_showProfile`，**没人再置 true** → 点了没反应。改前它是 `setState(_showProfile = true)`，两端共用。
- **修法**：按 `widget.user.isParent` 分派（家长端 `_go(_Profile())`，娃娃端 `setState(_showProfile = true)`）。
- ⚠️ **纪律**：`_profileDestination` 这类**跨角色共用**的回调，改的时候必须两个角色各走一遍；
  `flutter analyze` 照不出来（类型都对，只是娃娃端那份状态没被写）。
- **守卫**：`test/parent_nav_single_source_test.dart` 新增娃娃端用例（点「我的」→ 渲染出「退出登录」+ 页签零高亮），
  共 5 例。`flutter test` **261 全绿**、analyze 零 issue。
- ⚠️ **顺带踩到另一会话的规模守卫**：`home_screen` 因加注释涨到 667 > 基线 656
  （ADR-0058 `file_size_guard_test` 的棘轮只许下调）→ 压注释回到 **655**。
  **改共享大文件前先看 `file_size_guard_test.dart` 的 `_baseline`，别只看能不能编译。**

## 业务拓展盘问（grill-with-docs）：从「拓展业务」收敛到「闭合学习闭环」

- **用户拍板的定位**：①**自家工具**为主（不是商业、不是机构）+ ②**轻量开源**（别人可自部署），
  但 **README 只放产品相关，技术内容全部迁 `CONTRIBUTING.md`**（新文件）。
  ⚠️ **明确放弃「跨家庭内容分享」**——故 ADR-0019 版权红线对本方向归零。
  ⚠️ 纠正一个易混点：**「开源给多人用」≠「内容跨家庭流通」**。前者是 `parent_id` 隔离天然支持的多租户，
  后者才撞合规。别再拿合规去吓阻「多人使用」。
- **闭环六段实测审计（子代理核查，带行号）**：**只有 1 段真通**。
  ✅ 答错即建错题（`tasks/service.py:1236`）。
  ❌ 出题不消费错题/掌握度（`question/pipeline.py:45-111` 入参只有 subject/grade/knowledge_point/
  qtype/difficulty/interests，`Mastery`+`WrongQuestion` 在出题侧**零引用**）；
  ❌ 掌握度无出题出口（`mastery_board.dart` 纯展示无回调，`parent_task_form_view.dart:71` 知识点硬编码
  `'两位数加减法'`）；❌ 纸质批改无回流（`export/schemas.py:14-29` 无答案字段，`scan|ocr|grading` 零命中）；
  ⚠️ 到期触达半通（只在 `initState` 拉一次 + 手动下拉）；❌ 毕业无复检（`graduate()` 只打时间戳）。
  **结论：闭环不是转得慢，是根本没闭——产出端有数据，消费端没入口。**
- **决策（ADR-0060）**：接反馈边。D1 家长**显式**入口（不自动注入 prompt——否则"第三单元测试卷"
  混入薄弱点，出出来的两头不靠）；D2 粒度是**同类题**（按错题仿写，不是只按知识点——否则孩子
  "异分母通分"栽三回、系统出"同分母加减"，全对，闭环白转）；D3 **仿写靠 prompt 塞样例，不做向量检索**；
  D4 代表错题由服务端挑、随掌握度返回（上限 3）。（B/D 断点本轮不做，已写进 Considered Options。）
- ⚠️ **Q2 用户选了 B（同类题）而非我推荐的 A**——成本更高，风险是"像不像"无判据，已在 ADR 记明。
- ⚠️ **ADR 取号又撞了一次**：我先写 0058，才发现同日另一会话已占 0058（前端文件规模）与 0059
  （导航单一状态）。按「先预留者优先」改为 **0060**。教训复现（第三次）：**取号前必须读当天日志的
  「ADR 取号」行**，只看 `docs/adr/` 目录会漏掉**尚未提交的新文件**——`git status` 可见
  `?? 0058-frontend-...`。
- ⚠️ **又踩 BSD grep 的 `\|` 陷阱**：`grep -n "0058\|0060"` **静默返回空**，差点以为替换成功。
  一律 `grep -E`。这是本项目第三次踩到。
- **新术语（已入 `CONTEXT.md`）**：薄弱知识点 / 同类题 / 代表错题。
  **打印导出定性为「辅助输出、不参与闭环」**（批改结果不回流是设计非缺陷），已改「打印导出」+「学生卷」两条。
- **顺手修的失效文档**：README 路线图写「AI 伴学答疑（+每日额度）」，但 `tutor/router.py:7`
  明写配额已移除；`README.md:203` 也有「按配额判定」。改写后已在 `CONTRIBUTING.md` 注明
  「每日配额管控已移除，不存在 `/tutor/quota`」。

## 拆分报告失效复核（22:55，ADR-0059 合入后）
- 复核对象 `docs/refactor/2026-09-21-flutter-ui-decomposition.md`。逐条实测结论：
  **结构结论全部仍成立，仅「数值 / 行号引用」失效 7 处**，已就地更正并加 🔄 标记 + 顶部复核记录。
  lib 总量 25,620 → 25,608（−12），**不影响 P0–P4 批次划分**。
- 失效清单：`home_screen` 610→**655**（导航收敛 `d695eed`）· 分布表数字 · `profile_screen`
  **已删除**（折进 `home_screen` 的 `_Profile`）· `AppContentFrame` 调用点 10→**16**（12 文件）·
  `app_theme` 三个 widget 行号 1435/1829/1886 → **1327/1802/1851** · 测试 41/7034 → **47/7340** ·
  守卫基线 `home_screen` 656→**655**（棘轮收紧，3/3 仍绿）。
- ✅ 实测确认**未失效**：`_statusLabel` 双份（bank:395 / cards:789，注释仍写「同口径」）·
  `_qtypeLabel` 双份（bank:403 / card:493）· 两处手搓 `_EmptyHint`（bank:816 / review:552）·
  `app_theme` 内 12 个 widget 仍全在（:147/:1327/:1478/:1618/:1690/:1802/:1851/:1904/:2031/:2142/:2207/:2246）。
- 🔴 **复核新发现 3 项（补为 §3.7）**：① `home_screen.build()` **67 行**，超 ADR-0058「build ≤60」
  ——新规范立好后第一个违反者，且是本次导航收敛自己造成的，已列 P4 优先；② `_ParentPage` sealed
  + 11 子类 + `_buildParentPage()` 44 行 switch（:178/:583-651）可整块迁 `parent_pages.dart`；
  ③ `app_theme.dart:2684` 注释仍在传播「单个搬 widget 也成环」的**半截错误结论**，P3 时一并改。
- ⚠️ **ADR-0058 撞号已解除**：另一会话把他们的掌握度 ADR 改成 **0060**（现 0058=文件规模 /
  0059=导航单一状态 / 0060=掌握度反馈边）。他们日志里「0058 被占」的教训行已不适用，我在
  MEMORY.md 里改回中性表述。

## 助手路由漏词排查（23:10，「我错了哪些题」没查错题本）
- **现象**：家长问「我错了哪些题」→ 答「看不到你的练习或错题，请把题目发过来」；问「我的错题本」→
  正常返回 3 道错题。**同一意图两种说法，一个取数一个不取数。**
- **实测**（`/tmp/probe_route.py`，用 `discover_subagent_manifests` + `agent_core.router.classify`，
  parent visible = guide/query/question/tutor）：`我错了哪些题→tutor`、`我错过的题→tutor`、
  `哪些题我做错了→tutor`、`我有哪些错题→query`、`我的错题本→query`。
- **归因**：路由是**字面子串包含**匹配（`router.py` 规则 → hints → priority 最低兜底）。
  「我错了哪些题」不含 query 任何完整 trigger（「错题本」「我的错题」「有哪些题」都需连续匹配，
  「错了哪」把「错题」断开），question 的「题目」也不匹配 → 落到 priority −10 的 **tutor**；
  tutor 只有 `tutor_explain`、**没有任何取数工具**，所以只能请用户发题目。**不是路由代码错，
  是词表没覆盖这种说法**（意图识别问题）。
- 🔴 佐证：`query/tools/list_wrong_questions.py` 的 DESCRIPTION 自己写着「用于『最近错了哪些题』」
  ——工具作者预期这个说法，但 manifest 词表没收，两侧不一致。
- **未修**（待用户定）：候选最小修法是给 query triggers 加带「哪些」的短语（「错了哪些」「做错了哪些」
  「哪些题错了」），**不要加裸「错/做错」**——会把「这道题我哪里做错了」这类伴学问句从 tutor 抢到 query。
  守卫 `tests/ai/test_query_subagent.py`（tutor 意图 unaffected / child 出题仍 tutor）。
- 结构性备选：`classify(..., llm_classify=)` 端口已预留（`runtime.py:87` 传 `deps.llm_classify`），
  但 `app/features/assistant/router.py` **未接线** → 当前纯规则、确定性优先。接线会多一次模型调用。
- **MEMORY.md 超限治理**：14,490 → **11,480 字符**（超注入上限会截断）。做法：探针/Ollama 三条
  压成要点、学习闭环改为指向 `2026-09-21.md` 的指针、守卫测试名统一收口到
  `docs/agents/frontend.md` §6。⚠️ 期间被并发会话改过一次 → 回读后才 Write（又一次命中既有纪律）。

## P0–P4 拆分批次执行完毕（refactor 报告 §8）
- 8 个 commit：`fd17f02`(P0) `44f6277`(P1) `f996ab9`(P2) `9d7f765`(P3) `50821a0`(P4-a home_screen)
  `4fcb3ad`(P4-b assistant_cards) `fe17bcb`(P4-c task_form) `18108b8`(P4-d task_review)
  + `7423d14`(docs)。**守卫 `_baseline` 14 → 11 条**（models.dart / assistant_cards /
  task_form / task_review 拆到 400 以下，登记即删）。
- ⚠️ **私有标识不跨 library**：`_ParentPage` + 11 个页面子类要搬出 `home_screen` 就必须改公开
  （`ParentPage` / `OverviewPage` …）。ADR-0059 的判据是「只有一个 `_parentPage` 字段」，与类名
  是否公开无关 → 改名不破坏约束，只需同步改 `parent_nav_single_source_test` 的断言。
- ⚠️ **`assistant_cards` 拆开后 payload 读法（`_s`/`_i`/`_rowOf`/`_statsOf`/`_questionLines`）无处可去**：
  渲染与「复制纯文本」两处共用 → 落 `features/assistant/domain/card_payload.dart`（公开
  `cardStr/cardInt/...`），这是唯一不复制的去处。拆 widget 前先看有没有这种跨调用点的私有函数。
- ⚠️ **第三份确认弹窗**：`parent_task_review_screen` 的 `_showConfirmDialog` 是 `AppDialog.confirm`
  的第三份抄写且只剩一个调用点 → 删。Rule of Two 在这里的动作是「删」不是「抽」。
- 📌 **ADR-0058 §8 的基线表已与代码不一致**（表 14 项 / 实际 11 项），已加硬提醒：事实源是
  `test/file_size_guard_test.dart` 的 `_baseline`。照表改会写错。
- 仍在基线但**不属于**本报告范围：`parent_question_bank_view` 838 / `parent_tasks_view` 649 /
  `parent_question_card` 511 / `assistant_chat_page` 557 / `parent_wrong_questions_view` 442 /
  `child_home` 428 / `parent_child_selector` 404 / `parent_overview_view` 401。下一轮要拆需另起分析。
