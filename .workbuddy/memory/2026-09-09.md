# 2026-09-09

## ADR-0024 文档同步（genkit 历史描述）
- 用户要求把 ADR-0024 里「genkit 原生 action 端点」等历史描述同步到 2026-09-08 清理后的真实状态。
- 改动 `docs/decisions/娃娃学习App_ADR.md`：
  - ADR-0024 备选「复用 genkit flow 做助手」追加现状注（已落定不采用，Genkit flow 与原生 action 端点已从 flows.py/main.py 移除）。
  - ADR-0024 实现备注新增「Genkit 现状」：genkit 已从编排/传输层完全退役，`genkit_fastapi` 移除，不再暴露任何原生 action 端点；仅作底层 LLM 引擎经 `app/ai/engine.py` 间接调用；业务只依赖 `LLMProvider`。
  - 修正废除端点说明：ai router 现仅留家长回放端点，tutor router 不再挂 AI 生成路由。
  - 顺带修正 ADR-0027 决策 3 的「`app/ai`（Genkit flow）」为「Genkit 仅作底层 LLM 引擎」。

## 备注：文档范围外仍有 genkit 旧描述
- ADR-003（框架隔离）、ADR-0015 等仍把 genkit-fastapi / Genkit 编排层作为底座描述，未改（本次仅限 ADR-0024 要求）。如需全局一致可后续统一修订。

## ADR 文档 genkit 描述全局统一（2026-09-09 续）
- 用户要求把 ADR-003 / ADR-0015 等仍沿用 genkit-fastapi 编排层描述的段落统一，并按现有实现同步、分析是否移除未使用内容。
- 改动 `docs/decisions/娃娃学习App_ADR.md`：
  - ADR-003 决策/历史注：澄清 Genkit 仅作内部 LLM 引擎（`app/ai/engine.py`），`genkit-fastapi` 与原生 action 端点已移除，框架隔离原则保留。
  - ADR-0015：顶部新增「状态（2026-09-08）」横幅，明确决策 2（端到端 Genkit 协议）/ 决策 3（Genkit 编排流式 flow）已被 ADR-0024/0025 取代；背景、`SSE 事件信封（已退役）`、备选、后果中关于 `genkit-fastapi`/`serve_flow`/`package:genkit/client.dart` 单栈的描述均加「现状：已取代/不再使用」标注。
  - ADR-0017：顶部加「状态」横幅（REASONING/STEP/CARD 信封被 ADR-0025 AG-UI 信封广义化）；修正死代码引用——`genkit_ai_client.dart`→`assistant_api_client.dart`，`main.py monkeypatch`→`AgentRuntime` 统一错误封送，`tasks_generate` flow→`generate_questions_stream`/`generate_question` 经 subagents/question 编排。
  - 原则：ADR 决策原文保留（标记 superseded），仅把已删除文件/端点引用改为当前事实；未整段删除历史决策。
- 待办（超出 ADR 文档范围，未动）：`wayfinder/migration-08b-genkit-fullstack.md` 描述「统一 Genkit 协议」步骤，现 Genkit 协议本身已被 AG-UI 取代，建议该迁移文档加「已废弃」横幅或归档。

## ADR 文档精简（删除已废弃内容，613→446 行）
- 用户明确要求「把已废弃的都删除，adr 文档太长了」——从「标记 superseded」改为「整段删除废弃内容」。
- 用 Python 脚本对 `docs/decisions/娃娃学习App_ADR.md` 做块级正则替换（比逐行 Edit 更可靠，尤其代码块）：
  - ADR-0011（Pi/Eve 预研占位，从未采用）→ 整段删除。
  - ADR-0015 → 收敛为「状态横幅 + 仅存意图（指向 ADR-0024/0025/004/008）+ 4 条决策要点 + 备选/后果」，原 Genkit 单栈协议细节全部作废。
  - ADR-0017 → 收敛为「状态横幅 + 背景 + 6 条核心 UX 意图 + 备选/后果」，原 REASONING/STEP/CARD 信封的「数据结构 / AG-UI 映射表 / 发射时序」代码段全部删除（协议现由 ADR-0025 AG-UI 信封承担）。
  - ADR-0022 → 收为 2 行 tombstone（已被 ADR-0026 的 AssistantSession/AssistantEvent 取代）。
  - 顺手修正 ADR-0016/0020/0023/0013 中残留的 genkit 旧引用（端点名、`走 Genkit`、`Python 锁定 3.14` 的 Genkit 注、ADR-0015 协议引用等）。
- 校验：`serve_flow`/`package:genkit`/`tasks_generate`/`genkit_ai_client`/`SSE 事件信封`/`AG-UI 事件映射`/`发射时序` 均归零；仅 ADR-003 仍含 2 处 `genkit-fastapi`，但语义是「已移除」（当前事实，保留）。ADR-0011 已无残留引用。
- 经验：ADR 标题作者原本对 011/012/013 用 3 位、0014+ 用 4 位编号；删除 0011 占位后保留 012/013 原样，不作全局重编号。

## AI 出题/答疑生成代码收敛（删死代码）
- 分析结论：AI 功能端点层早已只有 SSE（`POST /api/v1/assistant/chat`），旧的 JSON 端点（`/ai/tasks/generate`、`/tasks/batch-generate`、`/ai/tutor/ask`）在 ADR-0023/0024 已删；前端 `home_notifier` 只走 SSE。不存在"JSON 类型"可删。
- 领域层 `app/ai/flows.py` 有两套生成函数，但 stream 那支是死代码（零调用）：删除了 `generate_questions_stream`、`tutor_stream`、`_build_stream_prompt` 及 `TaskGen*Chunk` 信封类、`build_retriever` 未用导入；同步更新 `app/ai/__init__.py` 惰性导出与 `__all__`。
- 保留一次性 `generate_question` / `_tutor_generate`（真正驱动 SSE 的 DATA/ASSISTANT_MESSAGE 事件），两者共用 `_assemble_question`/`_mock_question`，无逻辑漂移。
- 验证：`uv run ruff check app/ai` 通过；`tests/ai`+`tests/domain` 77 passed/2 skipped。`tests/api/routes` 本环境因沙箱 broker 权限弹窗无法跑（环境限制，非代码问题）；`test_generate_stream_smoke.py` 仍引用已删的 `/ai/tasks/generate` 端点，属历史遗留，本次未动。
