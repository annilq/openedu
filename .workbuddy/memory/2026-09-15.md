# 2026-09-15

## 密钥止血 + 模型来源单点化（ADR-0038 收尾 / ADR-0039）

承接昨日 401 事故（密文当 API Key 外发）的**用户侧收尾动作**，并按其要求做两处收敛。

### 1. 密钥止血（当日完成并验证）

- `backend/.env` 显式写入随机 `MODEL_APIKEY_SECRET`（48 字节 urlsafe）。此前该值为空 → Fernet 密钥由 `SECRET_KEY` 派生 → `SECRET_KEY` 一漂，全部密文失效。
- **重加密 DB**：`ModelConfig` 唯一那行先用旧密钥 `dev-secret-change-me` 解出明文，与 `backend/.env` 的 `DEEPSEEK_API_KEY` 交叉校验一致（len=35, tail `f474`），再用新密钥重新加密写回。新密文尾号 `YnZfH__J`；旧默认密钥 `changeme` 已解不开新密文。
- 端到端复验（新进程读 `backend/.env`）：`decrypt` 出明文、`resolve_engine` 传出 `api_key` len=35 tail=f474、密文未外泄、`openai_compat/deepseek-v4-flash` 解析成功。
- **`env_file` 改为 CWD 无关**（`app/core/config.py`）：原 `("../.env", ".env")` 是相对路径，在仓库根起服读不到 `backend/.env` → 密钥静默回落默认值，与事故同一失效模式。现按 `__file__` 解析绝对路径，实测在仓库根 / `backend/` / `/tmp` 三种 CWD 下均正确加载。

### 2. 移除内置模型目录（ADR-0039）

用户要求「所有 model 必须手动输入，减少冗余」。全链路删除 `BUILTIN_MODELS`：

- 后端：`config.py` 删字段；`ai/engine.py` 删 `json` / `_builtin_models` / `list_builtin_models` 与优先级第 2 条；`ai/__init__.py` 去导出；`schemas.py` 删 `BuiltinModelInfo`、`ModelListResp` 只留 `custom`；`router.py#list_models` 不再合并两份来源。
- 前端：`ModelInfo.isBuiltin` / `ModelListResp.builtin` 删；管理页「内置模型」分区与 `_builtinCard` 删（改单一「我的模型」）；`model_selector.dart` 与 `parent_task_form_view.dart` 只遍历 `custom`。
- 顺带清掉三处文案腐化：`ai/model_catalog.py`「内置模型服务商目录」→「服务商预设目录」（这是服务商目录，不是模型清单，命名撞车正是冗余来源）、`ai_plumbing.py` 与 `test_batch_generate_model.py` 里的「内置模型」措辞。

### 3. 新增模型强制 API Key

- 后端 `ModelConfigCreate.api_key: str = Field(min_length=1)` + `field_validator` 去空白后非空（空/纯空白 → 422）；`ModelConfigUpdate` 保留 `None` = 不修改，但传空白串也拒（不允许用 `" "` 抹掉已有密钥）。
- 前端 `ModelCreateReq.apiKey` 由 `String?` 改为必填 `String`（类型层面堵住），表单提交前拦截并提示。
- **连带修掉一个 422→500 缺陷**：`RequestValidationError` 处理器把 pydantic 原始 `errors()` 交给 `jsonable_encoder`，自定义校验器抛 `ValueError` 时 pydantic 把异常对象塞进 `ctx["error"]` → 序列化失败 → 本该 422 变成 500。新增 `app/core/errors.py#_safe_validation_errors`（只留 loc/msg/type）。不修的话第 3 条上线即 500。

### 4. 测试与文档

- `tests/ai/test_model_resolution.py`：删 2 例内置解析，新增 2 例（无 session 不给解析 / 越权不解析且不回落），4 → 5。
- `tests/api/routes/test_models.py`：7 → 13，新增必填（缺/null/空/纯空白四态）、去空白、编辑不传 key 保留原值、传空白被拒、换 key。
- `tests/domain/test_llm_smoke.py`：不再依赖 `BUILTIN_MODELS`，改为自建一行 `ModelConfig` + `SMOKE_MODEL_NAME/BASE_URL/API_KEY`，顺带把「配置解析 → 引擎 → 出题契约」纳入覆盖。
- 文档：新增 `docs/adr/0039-single-source-of-models.md`；ADR-0015 加「部分被取代」标注（保留核心决策、划掉内置目录）；ADR-0038 追加「补充」记录 env_file 与 .env.example 两处同族入口；AGENTS.md / README.md / `docs/agents/development.md` / `docker-compose.yml` 同步；两份 `.env.example` 重写（`backend/` 那份还在宣传早已删除的 `LLM_PROVIDER=mock` / `langchain`；根那份声称留空密钥是「进程内随机、重启失效」，实际是回落 `SECRET_KEY`——正是事故成因）。

### 5. 验证判据

- `ruff check .` → All checks passed。
- `pytest -q`（单次干净全量）→ **306 passed, 2 skipped, 0 failed**（2 skip 为需真实模型服务的 smoke）。
- `flutter analyze` → No issues found。
- 配置加载：三种 CWD 下 `MODEL_APIKEY_SECRET` 均加载成功。
- 密钥链路：新密文可解、旧密钥不可解、`resolve_engine` 传出明文密钥、密文未外泄。

### 6. 未做 / 遗留

- **未 commit**（本轮所有改动仍在工作树）。
- 改动 `SECRET_KEY` 会失效所有登录态，故仍是默认 `changeme`（JWT 签名用）；`docker-compose.yml` 的默认是另一个值 `change-me-in-prod`。生产必须显式配。
- **启动期密钥健康检查仍未做**：现在还是要等到提问失败才知道密钥解不开（服务端只有 warning）。
- `DATABASE_URL=sqlite:///./app.db` 仍是相对路径 → 换 CWD 会指向另一个库文件（与已修的 env_file 同族问题，未动）。
- 编辑模型时若 provider+baseUrl 匹配不到服务商预设，下拉会显示第一个预设却不出模型名建议（显示误导，数据不受影响）。
- prettier/真机验证门禁仍归用户。

---

## 附：查询助手「原始工具调用协议泄露」修复（当日追加，未 commit）

用户报「AI 学习助手查询任务时得到乱码消息」——剪切板实为**模型原始输出**：内部思考链 + 未解析的 `<invoke name="list_parent_tasks">` 伪协议，被当成答案回流给了用户。

### 定位过程（真接口实测定案）

- 通读链路：`assistant/service.py`（`rt.run()` → SSE 帧）→ `agent_core/subagent.py#run_with_tools` → `agent_core/adapters/genkit.py`（`return_tool_requests=True`）。
- 前端排除：`assistant_message_list.dart` 把 THINKING 渲成「思考中…」转圈，`ai_text_fold.dart:49` 对 THINKING **直接忽略**，`stream_reasoning_panel.dart` 只用于出题路径 → 用户看到的原文**必然是 `ASSISTANT_MESSAGE`（最终答案）**，不是思考面板。
- genkit 侧排除「工具没下发」：`genkit_openai/models/model.py:263-264` 确实写 `openai_config['tools']`，`:350-366` 确实解析原生 `tool_calls`；`get_default_openai_model_info` 对未知 openai_compat 模型返回 `Supports(multiturn=True)`（`tools` 未设，非 False），且 `genkit/_ai/_generate.py:1143-1144` 的 TODO 表明**尚未实现按 `supports.tools` 拦截** → tools 是下发的。
- **真接口实测（决定性）**：用 `backend/.venv` 直连 `deepseek-v4-flash` 带工具打一次，返回 **`TOOL_REQUEST name=list_parent_tasks input={'status':'assigned'}`** → 默认模型原生 FC 正常，**不会**触发该路径。
- 结论：泄露来自**另一类把工具调用写成文本/XML 的模型**（本地 Ollama 或中转/网关模型）。对它 `_extract_tool_calls` 取不到 `ToolRequest` → 无 `ToolCall` 事件 → `acc` 被当答案。

### 修复（`backend/agent_core/subagent.py`，ADR-0033 补充已记录）

- 新增 `_TOOL_CALL_PROTOCOL_RE`（`<invoke name=` / `"name": "` / `function_call` / `"function": {`）+ `_text_looks_like_tool_call(text, tool_names)`：**含协议标记 且 点名已注册工具** 才算命中（`:127-142`）。
- 工具型 subagent 某轮无原生 `ToolCall` 但命中 → `ERROR(code="TOOL_UNSUPPORTED")` 硬失败，不再把原始协议当答案（ADR-0033「硬失败不降级」的正确兜底）。
- **思考也缓冲**：`turn_thinking` 累积本轮 `TextDelta`，**轮次结束再决定冲刷**（`:193,219,267,286`）——命中泄露则整段丢弃。旧实现边收边 yield thinking，协议仍会混在 THINKING 帧里流出（前端虽不显示，SSE 流不干净）。
- 反向不变量：无协议标记的普通文本收尾照常作答案，不误伤。

### 验证判据

- `ruff check .` → All checks passed。
- `pytest -q` 全量 → **308 passed, 2 skipped**（较 ADR-0039 时点 +2，即本轮两个防护用例）。
- 新增用例：`test_text_tool_call_protocol_is_not_leaked_as_answer`（断言无 ASSISTANT_MESSAGE、无 THINKING 泄露、码为 TOOL_UNSUPPORTED）、`test_plain_text_answer_without_protocol_marker_passes_through`。

### 遗留

- 该防护是**文本启发式**：若模型把调用写得毫无协议标记（纯自然语言「我去查一下」），仍会当普通回答流出——只是不含内部协议，泄露危害已消除；要根治须靠模型侧支持原生 FC。
- Ollama 强制填 api_key 占位符仍是上一节的取舍。

---

## 附二：任务查询「status 必填」真实根因 = genkit 的 OpenAI strict 改写（ADR-0040）

用户报「查任务时模型反复重试 status 并最终把调用写成文本」，并判断「大概是没传任务状态，需把 status 改成非必填，不传就查全部」。

### 结论：用户的直觉方向对，但归因错了

**`status` 源码里本来就是可选**（`list_parent_tasks.py`：`"required": []`，描述写「不传＝全部」；handler `if status is not None and status not in TASK_STATUSES`）。改它**不产生任何 wire 变化**——真正决定必填的是 genkit 发包前的加工。

### 根因链（逐环有证据）

1. `genkit_openai/models/model.py#_get_tools_definition`（第 88-120 行）对**每个**工具无条件执行
   `_ensure_strict_json_schema(parameters, ...)` 并打 `'strict': True`。
2. `openai/lib/_pydantic.py#_ensure_strict_json_schema` 第 55-57 行：
   `json_schema["required"] = [prop for prop in properties.keys()]` —— **全部 property 变必填**，
   另加 `additionalProperties: False`。我们写的 `"required": []` 在 wire 上不存在。
3. **真接口实测**（用 `.venv` 直连 `deepseek-v4-flash`，原样下发 7 个工具 + `query_sop.md`）：
   - 非 strict schema → 模型**不传 status**（`list_parent_tasks args={}`）；
   - 复刻 strict 后的 schema → 模型被逼填 `{"child_id": "", "child_name": "", "status": ""}`。
4. `status=""` 既非 `None` 也不在 `["draft","assigned","done"]` 内 → handler 回灌
   `status 只能是 [...] 之一，收到：''` —— **正是模型引用的那句报错**（它说「status null failed /
   schema says required」不是臆断，是照 wire schema 说的）。
5. 模型因此反复试探（省略→仍必填→空串→还错→null→还错）→ 撞 SOP「不重复调用同一个工具凑结果」
   → **空转并把调用叙述进文本**（上一节已修的泄露路径）。

**同类陷阱**：`list_wrong_questions.limit` / `list_due_reviews.limit`（`integer`，strict 下必填）——
模型只能自己编个数字 → **静默截断结果**（200 道错题只说 20 道），或填 `0` → 撞「limit 必须大于 0」。

排除项：genkit 不校验入参（`_core/_action.py:499-500`，input_schema 为 dict 时 `_input_type=None`）；
工具 schema 确实下发了（`model.py:263-264`）。

### 修复（ADR-0040）

- `_shared.py` 新增 `UNSET_TOKENS` / `NO_FILTER="all"` / `optional_str()` / `optional_int()`；
  **归一收口在 `resolve_children`**（全部定位工具的公用入口）→ 7 个工具一次性受益，不逐工具打补丁。
- `list_parent_tasks.status` 的 `enum` 改为 `["all","draft","assigned","done"]`，描述「不过滤时传 all 或空字符串」。
- `limit` 描述改「0 或空＝不限」；`optional_int` 把 `0`/空 → `None`，负数报错。
- 定位参数描述改为「不指定时传空字符串」，模型不必猜。
- 契约测试 +4（`test_query_tools_contract.py`）：证据锚点（strict 确实把 required 改写为全量）、
  枚举必含 `NO_FILTER`、**行为级**「缺席编码填满 ≡ 不传」、非法值仍报错。
- 文档：新增 `docs/adr/0040-tool-schema-strict-mode-absent-encodings.md`；AGENTS.md 加「工具 schema 硬规则」并更新 ADR 索引至 0040。

### 验证判据

- `ruff check .` → All checks passed；全量 `pytest -q` → **312 passed, 2 skipped**（+4）。
- **守卫自证有效**：临时回退 handler 的归一行后跑契约测试 → 1 failed（`status 只能是 [...] 之一，收到：'all'`），恢复后全绿。
- **真接口复验**：改后 schema 下模型填 `status: "all"`（合法枚举 + 显式不过滤），handler 归一到 None → 不再报错。
- 编译期不变量：`_shared.py` 顶部注释 + ADR 记录了「新工具忘加缺席编码会被测试拦下」。

### 遗留

- 本约定依赖 `openai.lib._pydantic` 这个**私有 API** 的 strict 行为；升级 genkit / openai 时证据锚点测试会先失败。
- `strict: True` 下模型仍无法传未知字段；`tool_choice='none'`（出现 tool 消息后强制文本收尾）未动。
- `"all"` 成为保留词（被归一为缺席），不能当真实学科名/状态值用。

---

## 附三：草稿任务查询「child_id 不是合法的 uuid：'null'」（ADR-0040 第二例）

用户报「查询草稿任务时提示查询失败：child_id 不是合法的 uuid：'null'」，并提议两个方案：
① 把客户端当前切换的 `child_id` 传过去；② 娃娃登录时直接用登录的 `child_id`。

### 根因：与 status 同一机制，只是缺席值被**文本化**了

模型对「没有目标娃娃」的表达是**字符串** `"null"`（不是 JSON `null`——那样报的是 `'None'`）：

```json
{"child_id": "null", "child_name": "", "status": "draft"}
```

旧 handler `str(child_id).strip()` 对 `"null"` 为真 → 送去 `_as_uuid` → 抛
`child_id 不是合法的 uuid：'null'` → 模型再试一轮。**strict 强制填值**是同一个前提（附二）。

### 两条建议的结论

- **② 已是现状，无需改动**：`extra["child_id"]` 由端点从 JWT 写入
  （`assistant/service.py:85-90`，child → `caller.user.id`，parent → `None`），**客户端传不进来**；
  `resolve_children` 在孩子分支直接 `return [own]`、**忽略入参**。`AssistantChatReq` 里也没有
  `child_id` 字段（MEMORY.md「业务字段不要前端预填」）。
- **① 不做**：故障不是缺上下文，是 strict 强制填值；且多一个客户端可控定位字段=新攻击面。
  若真要「家长在看哪个娃就默认查哪个娃」，正确形态是 `AssistantChatReq.focusChildId`
  → 服务端校验归属后写 `extra["focus_child_id"]` → 工具层仅在家長未显式指定时采用。
  已写进 ADR-0040「明确不做」段，将来要做请单独立 ADR。

### 修复

- `UNSET_TOKENS` 扩到各语言口径的文本化缺席：
  `{"", "all", "any", "*", "none", "null", "nil", "undefined", "unset", "n/a", "na"}`。
- `_as_uuid` 增加 `hint` 参数；`child_id` 的报错追加「按昵称定位请改用 child_name；不指定目标请留空」
  —— **回灌给模型的错误必须带可执行出路**，否则它只能换个猜法重试。
- 契约测试 +18（两条 parametrize，各 9 个缺席值）：`child_id` / `child_name` 的文本化缺席
  必须等价于「不指定」，且草稿仍进 `unassigned_items`；并在
  `test_absent_normalization_does_not_weaken_real_validation` 里补 `child_id="abc"` 仍报错。

### 验证判据

- `ruff check .` → All checks passed；全量 `pytest -q` → **330 passed, 2 skipped**（+18）。
- **守卫自证**：临时把 `"null"` 从 `UNSET_TOKENS` 抽掉 → 测试复现原话
  `child_id 不是合法的 uuid：'null'（按昵称定位请改用 child_name；不指定目标请留空）`；恢复后全绿。
- **真接口复验**（strict wire schema）：问「有哪些草稿任务？」→
  模型传 `{"child_id": "", "child_name": "", "status": "draft"}` —— `""` 与 `draft` 都合法，
  草稿路径通。
- 注意：归一有**两处**（handler 里为算 `explicit_target`、`resolve_children` 里为 7 个工具统一收口），
  幂等、无害；只测其中一处不会暴露问题，验证时要动 `UNSET_TOKENS` 这类真机制。

### 遗留

- 同 ADR-0040：依赖 `openai.lib._pydantic` 私有 API 的 strict 行为，升级时证据锚点测试先失败。

## 顺手修复三个遗留项（config / 密钥，已 push 330d806）

- **SECRET_KEY 默认 changeme**：`backend/app/core/config.py` 新增 `resolve_effective_secret_key`（:23），未显式配置时生成随机密钥并落盘 `backend/.secret_key`（gitignore），`_resolve_secret_key` 校验器（:95）注入；跨重启稳定、消除不安全默认。
- **DATABASE_URL 相对路径漂移**：默认值改 `backend/` 下绝对路径（:87），`_normalize_db_url`（:46）把相对 SQLite 路径按 `backend/` 解析 —— 与 env_file 修复同族。
- **启动期密钥健康检查（缺）**：新增 `backend/app/core/secrets.py:check_runtime_secrets_health`（:21），`app/main.py` lifespan 在 init_db 后调用；生产缺配 SECRET_KEY/MODEL_APIKEY_SECRET 直接阻断启动，对已存 ModelConfig 解密冒烟（解不开=密钥轮换→启动期 WARN 点名模型），避免提问时才 401。
- 测试 `backend/tests/core/test_secrets.py`（10 断言）；注意沙箱 broker 禁止重建 `tmp_path` 临时目录（EEXIST），两个 SECRET_KEY 测试改用内存桩 `_MemFile`/`_ReadOnlyFile`，不依赖真实 fs。
- ADR-0041 记录上述决策。commit `6b2d93a`(fix) + `330d806`(docs)，已 push。

> 更正（同日）：上述「未提交文件」**不是** xin 的 WIP，是同一会话里「助手卡片协议 v2」那批改动，
> 当时按主题拆开提交，遗留到下一步处理。见下方记录。

## 助手卡片协议 v2：DATA 帧类型化卡片（ADR-0042）

**起因**：AI 用工具查到数据后，消息里应出**卡片**（任务卡 / 题目卡 / 学情卡），而不是把数据拼成一句话。
诊断结论：`DATA` 通道本身没问题（`data.result` 一直是任意 JSON、种类走 `extra.type`，`agent_core` 不感知卡片语义），
问题在契约只有一种形状 `{type: 标签, subject, stem}`，且**判别键在三处被丢掉**：

1. `result.type` 是标签（「错题」），信封 `data.type` 是种类（`query`）—— 同名异义；
2. 前端 `AiTextFold._withCard` 只取 `data['result']`，`data.type` 直接扔；
3. 落库 `cards.append(ev.data.get("result", ev.data))` —— payload 不自描述。

实测后果：出题卡载荷是 `asdict(GeneratedQuestion)+reasoning`、**没有 `type` 字段** → 前端只渲染「数学 + 题干」，
题型/难度/选项/答案/解析/出题思路全部不可见（同一张卡在家长出题表单 `_PreviewCard` 里是全的）。
查询卡则把明细用 `；` 压成一行。

**改动**：

- 后端 `query/render.py` 全量重写：`Card(kind, payload)` 数据类 + `_KIND` 映射表
  （`list_children→child_list`、`list_parent_tasks`/`list_today_tasks→task_list`、
  `list_wrong_questions→wrong_question_list`、`list_due_reviews→due_review_list`、
  `get_progress→progress`、`get_mastery→mastery_list`），明细走 item shaper 出结构化 dict，
  `_MAX_ITEMS=5` 截断 + `total` 报真实命中数，`get_progress` 出 `stats` 而非 `items`，空/错走 `notice`。
- `query/agent.py#render_tool_result`：把 kind 写进信封 `extra={"type": card.kind}`。
- `features/assistant/service.py:245`：改成 `cards.append(ev.data)` **整帧落库**（含判别键），原来是 drops type。
- 前端新增 `domain/assistant_card.dart`（`AssistantCard.fromData` 容错解析 + `AssistantCardKind` 常量）、
  `presentation/widgets/assistant_cards.dart`（`AssistantCardTile` 按 kind 分派：题目卡 / 列表卡 / 指标卡 / 降级卡），
  `shared/utils/question_labels.dart`（从 `parent_task_form_view.dart:1016` 抽出 `qtypeLabel`/`difficultyLabel` 供跨 feature 复用）。
- `assistant_message_list.dart#_Bubble` 重构：文本泡 + **卡片落在气泡外侧**（卡片自带 surfaceRaised 底 + 1px outline，
  塞进气泡是双层容器），复制判据改为「正文或卡片非空」，复制内容 = 正文 + 卡片纯文本（与视觉共用同一份行投影）。

**明确不做：通用 GenUI（服务端下发 UI schema）** —— 消费者只有一个（单 Flutter 客户端 + 单一设计系统），
一旦允许服务端描述布局，设计系统一致性与可达性（双模式字号映射）就从客户端契约变成服务端契约，
而服务端既不知道当前是家长端还是娃娃端，也不该知道。ADR-0042 记录三条理由。

**ADR 编号插曲**：本 ADR 初稿写 `0041`，与同日已推送的 `0041-startup-secret-health-and-cwd-independent-config.md`
撞号 —— **同一批工作里两个 0041**。已改名为 `0042-assistant-typed-card-protocol.md` 并同步全部交叉引用
（`AGENTS.md:31`/`:63`、`docs/agents/architecture.md:108`、ADR-0025/0033、后端 agent.py/render.py/service.py + 两处测试、
前端 assistant_card/ai_text_fold/assistant_message_list/assistant_cards + 两处测试）。
**教训：起 ADR 前先 `ls docs/adr/` 看最大编号，尤其在自己刚推送过 ADR 的同一天。**

**验证**：后端 `pytest tests/ai tests/api/routes` → 271 passed；`ruff check` 改动文件 All checks passed；
前端 `flutter analyze` → No issues found；feature 边界 R1/R2/R3 扫描为空。
`flutter test` 沙箱内跑不起来（flutter_tester WebSocket），纯逻辑测试先用等价 `dart run` 脚本验过（23 断言）。
已提交并 push。

## 附四：「错题本」查询泄露复现 —— 根因是「推理被当成答案」（待修）

用户报查询错题本时输出模型英文独白 + `<invoke name="list_wrong_questions">` 伪协议 + 卡片。
用真实账号（家长 `annilq` → 娃娃 `lsc` / `a13be10f…f3d0`）跑**生产等价链路**复现
（`resolve_engine` → `build_provider` → `QuerySubAgent` → `run_with_tools`，临时脚本已删）：

- 模型（`deepseek-v4-flash`）**没有产出原生 ToolCall**；`_extract_tool_calls(resp)` 为空；
- 但 `acc` 是**模型的内部独白**（英文 `The user wants the wrong-answer notebook for lsc…`，
  1255 字符）→ 作为 `ASSISTANT_MESSAGE` 原样下发 → 前端渲染成 AI 正文（与卡片同一条气泡）；
- `<invoke …>` 协议本身落在 **THINKING 帧**（前端忽略），**现有防护没命中**：
  `_text_looks_like_tool_call` 只看 `acc` 里有没有协议标记，这次独白里没有 → 漏网。

**结构性根因**：`agent_core/adapters/genkit.py:524-528` 在 tools 路径把**所有**流片段
（含 `SegmentKind.REASONING`）一律 `yield TextDelta`，`run_with_tools` 又把所有 `TextDelta`
累进 `acc` → **推理与正文在协议层就不分**；没有原生工具调用时，「独白」就成了「答案」。
附一的文本防护只覆盖「答案里带 XML 协议」这一种形态，覆盖不到「答案是独白」。

**正确修法（未做，待用户定）**：`TextDelta` 加 `kind`（text / reasoning），adapter 按
`SegmentKind` 标注；`run_with_tools` 里 reasoning 只进 `turn_thinking`、不进 `acc`；
工具型 subagent 若「无原生 ToolCall 且正文为空」→ `ERROR(TOOL_UNSUPPORTED)` 硬失败，
而不是把独白当答案。

## 附五：AI 消息 Markdown 渲染方案分析（未落地）

用户问「AI 回复大部分是 markdown，需要支持渲染，成熟且简单的方案有哪些」。

现状：前端无任何 markdown 依赖（`pubspec.yaml` 只有 riverpod/dio/shadcn_ui…），
AI 气泡是纯 `Text`（`assistant_message_list.dart:172-178`），`**`、列表、表格原样显示。

**推荐 `gpt_markdown` 1.2.1**（2026-08-23，BSD-3，认证发布者 useval.io）：
- 依赖只有 `flutter_math_fork`（纯 Dart、无原生插件，不需平台配置）；
- 为 AI 输出而写：1.2.0 起「已定稿前缀缓存、只重建尾部」→ 直接适配现有逐帧回填的流式；
- Markdown + 行内/块级 LaTeX + 表格 + 代码块 + 选择，样式走 `GptMarkdownStyleSheet` + builders；
- 自带解析器（非 `markdown` 包 AST），对畸形 markdown（未闭合 `**`/```）容错 —— 流式场景关键；
- 要求 Dart ≥3.7 / Flutter ≥3.32（本机 3.47.2 满足）。

备选 `flutter_markdown_plus` 1.0.12（2026-07-10，原官方包续作，底层 `markdown` 包 AST，
GFM 表格/任务列表/脚注齐；依赖 markdown/meta/path）。缺点：样式绑 Material 主题，
而本项目根是 **`CupertinoApp`**、子树无 Material 祖先（`assistant_message_list.dart:220-221`），
须改用 `MarkdownRaw`；公式要另装 `flutter_markdown_plus_latex`。

不推荐 `markdown_widget` 2.3.2+8：**2025-04 起 16 个月无发布**，7 个依赖
（url_launcher / flutter_highlight / scroll_to_index / visibility_detector…），
TOC 与代码高亮是强项但聊天气泡用不上，LaTeX 只能从 example 抄。

**接入设计（三步，尚未实施）**：
1. `pubspec.yaml` 加 `gpt_markdown: ^1.2.1`（可把 env sdk 下界抬到 `^3.7`）。
2. 新增 `shared/widgets/app_markdown.dart`（`App*` 前缀、不订阅 feature provider → 符合 ADR-0037
   的通用设计系统组件定位）：把 `AppTheme.colorsOf/textOf` 映成一套 style sheet，
   Parent/Child 双模式 + 暗色只在这一处对齐。
3. 接入点只有一处：`assistant_message_list.dart#_BubbleBody` 的 AI 分支
   （用户气泡保持纯文本，`thinking` 占位分支不动）。

**风险/待真机验证**：CupertinoApp 根（须确认渲染栈不要求 Material 祖先；内置代码块复制按钮
建议关掉、沿用已有 `_CopyButton`）；两端默认都不透传内联 HTML（安全，保留）；
长答案逐帧重解析的性能（gpt_markdown 1.2 有前缀缓存，flutter_markdown_plus 是全量重解析）。

---

## 待办核查：A/C/D/E/F 判定（此前「待办问题」分析多数已过期）

用户要求核验某份「项目待办分析」中 A/C/D/E/F 是否真实存在。逐条对着真实代码复核，**结论：绝大多数已修复，只有 E 真实存在**。

- A-1 儿童账户页 Category 标题白字不可见 → **已修**（`interest_picker.dart:161-165` 改 `app.onSurface`，注释直指该 bug）。
- A-2 生成任务流式闪现 → **代码层面已消解**：后端 THINKING 每 16 字符冲刷（`translate.py:34`），前端逐帧 setState 到 `liveReasoning`（`home_notifier.dart:96-110` → `parent_task_form_view.dart:480-507`）。无真机验证记录。
- C-1 compaction → **已修**（`assistant/repository.py:44-135` + `service.py` 自动摘要；commit 50064e8/be420df）。评审只要求 P1，实际 P1+P2 都做了。**无 ADR**。
- C-2 扩展钩子 → **已修且已立 ADR-0035**（`ports.py:148 Hooks`；commit 5164845）。
- D-1 guard.py / D-2 tasks write-path 下沉 / D-3 SSE reducer fold / D-5 asyncio bridge → **全部已完成**。
- D-4+6 `Resource<T>` 迁移 → **部分完成**（`resource.dart` 已建；Models/Review/Bank/Children 仍自建四态）。
- E 编辑模型下拉误导 → **真实存在，未修**（`model_form_dialog.dart:199`）。
- F SECRET_KEY 默认 changeme → **已修并立 ADR-0041**（`config.py:23-43,95-98`；commit 6b2d93a）。
- 附带：日志「附四」标「待修」的助手泄露，HEAD `77fd33f` 已修（`TextDelta.kind` 推理/正文分流；`subagent.py:220`）——但代码把该决策标成「ADR-0041」，与 `docs/adr/0041`（密钥健康检查）**撞号**，真实正文缺失。

**滞后文档（需更新）**：`docs/agent-core-architecture-review.md` §7 表、`AGENTS.md`「关键事实源与已知风险」段、以及本仓 `MEMORY.md`（已订正）。已 push 至 `77fd33f`，工作树干净。

## 2026-09-15（续）三项收口：修 E + 建 ADR-0043 + 文档订正

按「直接动手 1/2/3」执行并验证：

1. **修 E（唯一真缺陷）**：`shared/widgets/app_inputs.dart#AppPickerField` 的 `value` 由 `T` 放开为 `T?`、新增 `placeholder` 参数（默认「请选择」）；`model_form_dialog.dart` 服务商下拉由 `value: _presetKey ?? widget.presets.first.key` 改为 `value: _presetKey` + `placeholder: '自定义（未匹配预设）'`。未匹配预设不再硬显示第一个（DeepSeek）。9 个调用点传非空值，兼容。
2. **建 ADR-0043**：`docs/adr/0043-reasoning-text-channel-split.md`（推理/正文分流）。把 5 个文件的误引用 `ADR-0041`→`ADR-0043`：`agent_core/{ports,adapters/genkit,subagent}.py` + `tests/ai/{test_tool_loop_bounds,test_genkit_adapter_tools}.py`；`config.py`/`secrets.py`/`main.py` 的 0041（密钥健康检查）保持不动。
3. **文档订正**：`AGENTS.md` 加 ADR-0043 硬规则条目、风险段标注 P0/P1/P2 关闭、ADR 索引改 0001–0043；`docs/agent-core-architecture-review.md` 顶部加「2026-09-15 复核」状态横幅、§6 补前端逐帧 setState 结论、§7 表加「状态」列标注 P0/P1/P2 已关闭、「待定」项收敛为仅余真机验证。

**验证**：后端 `ruff check` All checks passed；`pytest tests/ai/{test_tool_loop_bounds,test_genkit_adapter_tools,test_layering_invariants}.py` → 59 passed；前端 `flutter analyze` → No issues found。**未闭合判据**：A-2（SSE 逐帧）与 E 的下拉显示需真机确认。

## 2026-09-15（续二）修复 B-3 / B-4

用户指令「修复 B-3，B-4」。B-3/B-4 来自此前「项目待办分析」清单。

### B-3：助手回答推理/正文分流 —— 实测已修，无需改码

- **读真代码而非信记忆**：`agent_core/adapters/genkit.py:_stream_with_tools` 已按 `SegmentKind` 标注 `TextDelta(delta=seg.text, kind=_TEXT_KIND_OF.get(seg.kind, TextKind.TEXT))`，`_TEXT_KIND_OF` 映射 `REASONING→REASONING`；docstring 引用 ADR-0041（实为 ADR-0043，撞号见上）。
- `subagent.py:248-254` reasoning 只进 `turn_thinking`、不进 `acc`；`subagent.py:311-320` 工具型 subagent「无原生 FC 且正文空」→ `ERROR(code="TOOL_UNSUPPORTED")` 硬失败，独白不再被当答案。
- 守卫测试 `tests/ai/test_tool_loop_bounds.py` 已含 `test_reasoning_only_turn_with_tools_hard_fails_instead_of_leaking_monologue` 等；`pytest tests/ai/test_tool_loop_bounds.py -q` → **24 passed**。
- **结论**：MEMORY.md 标「待修」是过期记忆。不重复实现，仅复验为绿。

### B-4：AI 气泡 Markdown 渲染 —— 已落地

- `frontend/pubspec.yaml`：env sdk 下界 `>=3.5.0` → `>=3.7.0`（gpt_markdown 要求 ≥3.7，运行时 3.47.2 满足）；加 `gpt_markdown: ^1.2.1`。`flutter pub get` 耗时 12m4s 成功（触发 `flutter_math_fork/nested/provider/tuple` 拉取）。
- 新增 `frontend/lib/shared/widgets/app_markdown.dart`（`App*` 前缀、零 `features/` import → ADR-0037 合规）：包 `GptMarkdown`，把 `AppColors`/`AppText` 令牌映到 `GptMarkdownStyleSheet`（heading/inlineCode/codeBlock/blockQuote/link/list/table）。
  - `CodeBlockStyle(showCopyButton: false)` —— 关掉包内置复制按钮，复用已有的整条消息 `_CopyButton`，避免 CupertinoApp 根引入 Material 渲染栈。
  - 仅 AI 消息走 Markdown，用户气泡仍是纯 `Text`。
- `assistant_message_list.dart`：加 import；`_BubbleBody` 改三分支 —— thinking 转圈占位不变、user 保持 `Text(onPrimary)`、AI 分支 `return AppMarkdown(message.text)`（底 surfaceContainerLow / 字 onSurface）。
- **验证**：`flutter analyze lib/features/assistant/presentation/widgets/assistant_message_list.dart lib/shared/widgets/app_markdown.dart` → No issues found (4.7s)；全量 `flutter analyze` → No issues found (5.9s)。

### 当前工作树（未提交）

`git status --short` 关键项：
- 已 commit 的 B-3 收口：`backend/agent_core/{adapters/genkit,ports,subagent}.py`、两处测试、`docs/adr/0043-reasoning-text-channel-split.md`（对应 HEAD `77fd33f`）。
- 本会话 B-4 改动：`frontend/lib/shared/widgets/app_markdown.dart`（新）、`assistant_message_list.dart`、`pubspec.yaml`、`pubspec.lock`。
- 顺带被之前的「修 E」改动一并带入：`model_form_dialog.dart`、`shared/widgets/app_inputs.dart`、记忆文件。
- **均未 commit**。真机验证门禁（markdown 渲染效果 / 复制按钮 / 双模式对齐）仍归 xin。

### 收口（14:46 复核）

- **真机验证通过**：E（编辑自定义 provider 的模型 → 下拉显示「自定义（未匹配预设）」，不再冒充 DeepSeek）与 A-2（出题 THINKING 打字机式逐帧，非 5 秒后闪现）均确认无问题 → **前述三条未闭合判据全部关闭**。
- **全部已提交并推送**：`349e4ec`（HEAD == origin/main，领先 0）。该提交含 B-3 收口（ADR-0043 + genkit/ports/subagent + 两测试）+ 文档订正（AGENTS.md / 架构评审）+ 修 E（app_inputs.dart / model_form_dialog.dart）+ B-4 Markdown 渲染（`app_markdown.dart` 新增、`assistant_message_list.dart`、`pubspec`）。
- `MEMORY.md` 三处过期项已订正：编号悬空 → ADR-0043 已落；Markdown 渲染 → 已落地；修 E → 已完成。
- 遗留：`app_markdown.dart` docstring 只引 ADR-0036（单入口），未为「气泡 Markdown 渲染」单独立 ADR——如需治理闭环可补 0044。

## 2026-09-15（续三）错题本查询泄露原始 XML 工具调用 —— 已修

**用户报障**：问「查询我的错题本」，AI 输出竟是 `<invoke name="get_mistakes">…</invoke>` 原文 + `【娃娃 · lsc（4年级）】可查询的娃娃` 卡片。

### 落库铁证（`backend/app.db`，会话 `ff07d664fefa4f1f97e4c1d1ac3118f5`）

| turn | role/step | 内容 |
|---|---|---|
| 5 | user/input | 「我的错题本有哪些」 |
| 2 | tool/tool_call | `list_children`，args `{}` —— **原生 FC 成功** |
| 3 | tool/tool_result | `list_children` → `a13be10f-…`（lsc / 4年级） |
| 4 | assistant/output | `content` = `<tool_calls>\n<invoke name="get_mistakes">\n<parameter name="child_id" string="true">a13be10f…</parameter>\n</invoke>\n</tool_calls>`；`payload.cards` = `child_list`（即用户看到的卡片） |

默认模型（`modelconfig` 表）：`deepseek-v4-flash` / `provider=openai_compat` / `base_url=https://api.deepseek.com` / `is_default=1`。

### 两个根因

1. **闸门判据绑了工具名（`agent_core/subagent.py:141` `_text_looks_like_tool_call`）**：原为「协议标记命中 **且** 点名已注册工具」。模型把 `list_wrong_questions` **幻觉**成 `get_mistakes`，`any(name in text for name in tool_names)` 必然落空 → 放行 → 原始协议经 `assistant_message(acc)` 落库并下发。既有回归测试 `test_tool_loop_bounds.py:375` 用的是**已注册**的 `list_x`，恰好绕过此洞。
2. **SOP 在逼模型走最易退化的路径**：`query_sop.md` / `_SYSTEM` 原写「先调 `list_children` 拿 `child_id`，再带 id 查明细」——每次跨娃娃查询都强制多一轮「复述一串 uuid」，而这正是 `deepseek-v4-flash` 最容易退化成文本调用的动作（`tools/_shared.py:45-65` 已记录同源形态）。

### 修复

- `agent_core/subagent.py`：协议判据拆**强 / 弱**两档。强标记（`<invoke name=` / `</invoke>` / `<parameter name=` / `<function_calls>` / `antml:`）**命中即判泄露，不绑工具名**；弱标记（`"name": "` / `function_call` / `"function": {`）保留「点名已注册工具」收紧（防误伤讲解 JSON 的正文）。`_flushable_thinking` 复用同一函数，故 THINKING 通道同步收紧。
- 新增 `_PARTIAL_TOOL_CALL_HINT`：`native_fc_seen=True` 时（数据卡已下发）报「本轮未完成，上方数据已给出」，而非「查询无法执行」——后者与用户所见矛盾。
- `query_sop.md` + `query/agent.py#_SYSTEM`：改为**优先用 `child_name` 一跳直达**，仅昵称歧义时才取 `child_id`。`list_children.py` DESCRIPTION 同步去掉「不确定时先调本工具」的引导。
- 新增 4 条测试（`test_tool_loop_bounds.py`）：编造工具名的协议不得外泄、已成功调用后报 partial、思考里的编造名草稿丢弃、弱标记无工具名时不误伤。

### 验证

`ruff check .` → All checks passed；`pytest tests/ -q` → **351 passed, 2 skipped**（`test_tool_loop_bounds.py` 单文件 24 → 28）。
**未闭合判据**：真机复测「我的错题本有哪些」——期望不再出现 XML，且要么一张 `wrong_question_list` 卡，要么明确的错误提示。



