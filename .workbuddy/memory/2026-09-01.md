# 2026-09-01

## 分析：Flutter 客户端是否使用了 Genkit（票据 08 架构复盘）
- 用户判断「Genkit 是全栈方案，前后端都要支持效果才好」→ 核查当前状态。
- 结论：**前端没有用 Genkit，且当前架构下也无法直接用 Genkit Dart 客户端**。
  - 前端 `frontend/pubspec.yaml` 无 `genkit` 依赖；消费后端走自研 `SseClient`（`lib/shared/data/remote/sse_client.dart`），解析项目自定义 SSE 信封 `token/question/safety_refusal/done/error`。
  - 后端确实用了 Genkit（`app/ai/engine.py`/`flows.py`、`app/api/routes/stream.py`，依赖 `genkit`/`genkit-fastapi`/`genkit-openai`/`genkit-ollama`）。但 `stream.py:9` 明确「**不采用 serve_flow 原生格式，避免前端改解析**」——后端刻意把 Genkit 翻译为自定义 SSE 信封，且 `main.py` 只挂载 `api_router`，**未挂载 genkit-fastapi 的 `serve_flow` 原生 action 端点**。
- 官方 Genkit Flutter 客户端事实（来自 `genkit.dev/docs/app-frameworks/flutter/`）：
  - 包名 `genkit`，import `package:genkit/client.dart`；`flutter pub add genkit`。
  - 用法 `defineRemoteAction(url: flowUrl, fromResponse:, fromStreamChunk:)` → `.stream(input)` 返回部分对象的异步流；**它调的是后端 flow 的原生 action HTTP 端点**（如 `http://host:port/flowName`），吃 Genkit 的 typed structured streaming 协议，不是我们的自定义 SSE 信封。
  - 要求 **Dart SDK ≥ 3.10.0**；本机 Flutter 3.47.1 / **Dart 3.13.1** → 门槛满足，可装。
  - 需 CORS（web/调试）+ 自带的鉴权头注入（我们端点要 `Bearer`，genkit 客户端是否支持需验证）。
- 架构张力：用户的「全栈」直觉正确（GenUI/DevTools/结构化流式/统一多模型只有端到端才兑现）；但当前 ADR-0015 + ADR-003 是反方向决策——后端代理、前端与模型解耦、可无 key 跑 MockProvider 闭环。
- 若走全栈 Genkit（需用户拍板，勿擅自改）：① 后端挂载 `serve_flow` 把 flow 以原生 action 暴露（安全层包进 flow 或 wrapper action，守住 ADR-008）；② 前端 `flutter pub add genkit`，用 `defineRemoteAction` 替 `SseClient`，类型模型 TutorReply/QuestionPreview 配 `fromStreamChunk`；③ 代价=前端耦合 Genkit 线协议（部分反转 ADR-0015 的解耦收益）。建议分阶段：先并行挂原生端点（解锁 DevTools + GenUI 能力）同时保留生产自定义 SSE UI，再在小屏试点 `defineRemoteAction`。
- 待用户决策：是否从「解耦自定义 SSE」切到「全栈 Genkit Dart 客户端」。本会话仅分析，未改动代码。

## 决策：统一 Genkit 全栈（用户拍板，2026-09-01）
- 用户决定：**前后端统一技术栈与协议，Flutter 端也用 Genkit**；MockProvider 改为 flow 内「一次性模拟数据源」分支；要求最佳实战、避免后期多栈维护；并要明确的迁移文档 + 迁移后更新技术架构文档。
- 已产出（Phase 0，未改代码）：
  1. 迁移文档 `wayfinder/migration-08b-genkit-fullstack.md`：目标架构、4 条关键设计决策（flow=安全单元 / 薄路由鉴权+配额+输入安全并注入 user 上下文 / mock 入 flow 分支 / 前端 defineRemoteAction + JWT 注入待核实）、Phase 1-4 分阶段计划、风险回滚、验收清单。
  2. 修订 `娃娃学习App_ADR.md` 的 ADR-0015：决策 2 由「轻量 SSE 渲染」改为「端到端 Genkit 协议（前后端统一单栈）」；备选「Genkit 已采纳为统一 AI 栈」推翻原 LangChainProvider 折中；后果同步（MockProvider 退役改 flow 分支、LangChainProvider 退役、前端依赖 genkit client）；SSE 信封段标注退役中。
- 关键技术事实（写文档时确认）：当前 Genkit 仅当 LLM SDK 用（`engine.genkit.generate/stream`），**无 `@ai.flow`、未挂 `serve_flow`**；`stream.py` 自定义 SSE 信封、前端 `sse_client.dart` 均将被 `defineRemoteAction` + 原生 flow 端点取代。本机 Dart 3.13.1 ≥ genkit dart 客户端要求的 3.10.0（门槛满足）。
- 待 Phase 1 spike 核实：`defineRemoteAction` 如何注入 `Authorization: Bearer`（决定薄路由鉴权方案 A 或 B）。
- 下一步：等用户确认后执行 Phase 1（后端 flow 化 + serve_flow 暴露 + mock 分支）~ Phase 4（更新技术架构_后端.md / 技术架构_Flutter.md / 术语表 / ticket 08）。

## 修复：前端 build 失败（Flutter macOS debug）
- 现象：`flutter run` 编译 `lib/main.dart` 报 3 错：① `home_screen.dart` import `../widgets/parent_dashboard.dart` 文件不存在；② `ParentDashboard` 未定义；③ `child_home.dart` 用 `task.subject`/`task.grade`/`task.knowledgePoint`（TaskModel 已无这些字段）。
- 根因：Cupertino 迁移中 `home_screen.dart` 被简化为直接组合 `ParentDashboard`，但 `parent_dashboard.dart` 从未创建（家长视图拆到了 `widgets/parent/` 子目录的模块化视图）；`child_home.dart` 是 v2 redesign 版本，仍按旧的「学科在 Task 上」写法，而 `TaskModel` 已按 ADR-0004「学科下沉到题」只存 `title/status/questions`（subject/grade/knowledgePoint 现属 `QuestionModel`）。
- 改动：
  1. 新建 `frontend/lib/features/home/presentation/widgets/parent_dashboard.dart`：`ParentDashboard(user, onNavigateToPractice)`（StatelessWidget，SingleChildScrollView 组合 `ParentChildSelector`/`ParentOverviewView`/`ParentTaskFormView`/`ParentQuestionBankView`/`ParentWrongQuestionsView`/`ParentTutorQuotaView`/`ParentTutorLogsView`；出题/题库的 `onNavigateToReview` 暂以 `static _noReview` no-op 占位，完整审阅流由 `parent_task_review_screen` 承载）。
  2. `child_home.dart`：任务卡片标签改从 `task.questions.first` 取 subject/grade/knowledgePoint（空题卡仅显示题数）。
- 验证：本环境无 Flutter SDK，未运行；仅静态修复。请本地 `flutter analyze` + `flutter run` 复核。
- 提示：ParentDashboard 内 `ShadPopover`（parent_child_selector）依赖 `ShadTheme` 祖先；若首页根不是 ShadApp/CupertinoApp 混用 shadcn，需在父级补 `ShadTheme`（生产入口 `lib/main/app.dart` 若已含则无碍）。

## 修复（续）：ParentDashboard.onNavigateToPractice 类型不匹配
- 现象：`home_screen.dart:105` 传 `_navigateToPractice`（`void Function(TaskModel)`）给 `ParentDashboard.onNavigateToPractice`，但上一轮 `ParentDashboard` 误声明为 `VoidCallback`，编译报「`void Function(TaskModel)` 不能赋给 `void Function()`」。
- 修复：`parent_dashboard.dart` 的 `onNavigateToPractice` 参数类型改为 `void Function(TaskModel task)`（与 `ChildHome` 及 `_navigateToPractice` 实参签名一致）。该回调在家长仪表盘内暂作保留（与 ChildHome 保持 API 对称），模块化家长视图不消费它。

## 优化：恢复 HomeScreen 默认左右分栏布局（AdaptiveShell）
- 问题：上轮 Cupertino 迁移把 `home_screen.dart` 从 `AdaptiveShell`（侧栏 240 + 内容，ADR-0014 响应式左右分栏）改成顶部 `CupertinoNavigationBar` + 单列 `ParentDashboard` 扁平页，导致平板/桌面端左右分栏与三档断点（expanded 侧栏 / medium 收起轨 / compact 底栏或抽屉）全部失效，家长 8 个视图被压进一个超长滚动页。用户反馈「默认左右布局被改了」，确认要完全恢复分栏壳。
- 改动：
  1. 重写 `lib/features/home/presentation/screens/home_screen.dart`：恢复 `AdaptiveShell` 包裹，家长端侧栏目的地（概览/布置任务/错题本/AI答疑记录/AI管控/题库/模型管理）+ 娃娃端（首页/复习/错题本/AI伴学/掌握度），侧栏导航在壳内切换各视图（家长 `switch`、娃娃 `IndexedStack`）；保留侧栏顶部 `ParentChildSelector` 与底部 `AdaptiveUserBlock`（我的）；保留草稿审核覆盖层（`ParentTaskReviewScreen`）与编辑娃娃覆盖层（`ChildFormScreen`）。
  2. 删除 `lib/features/home/presentation/widgets/parent_dashboard.dart`（扁平化产物，恢复分栏后成为死代码）。
  3. 娃娃端 `ChildHome` 的复习/错题/伴学 CTA 改回「切换侧栏 tab（setState _childNavIndex）」而非整页 push；`PracticeScreen` 仍整页 push。
- 验证：`flutter analyze lib/features/home/presentation/screens/home_screen.dart` → No issues found；全项目 analyze 仅剩 `test/genkit_client_test.dart` 的 4 个 error（源于并行流化重构删除 `sse_client.dart`，与本次布局修复无关）。

## 修复：点击「生成任务」超时（改为流式生成 + 落库）
- 现象：真实模型下点「生成任务」超时。根因：`TaskGenNotifier.generate()` 调同步接口 `POST /tasks/batch-generate`（一次性生成并写库后才返回 `TaskResp`），多题串行生成超过客户端超时。流式端点 `POST /ai/tasks/generate`（Genkit flow `tasks_generate` 逐题 `ctx.send_chunk`）早已存在，但只预览不落库。
- 改动（两步法，消除超时且保留「生成即落库草稿」语义）：
  1. 后端 `models.py` 新增 `TaskFromGenerated{title,child_id,specs,focus_interest,model,questions}`；`app/api/routes/tasks.py` 新增 `POST /tasks/from-generated`：校验 child 归属 + 非空，把已生成的 `questions`（QuestionPreview.toJson，snake_case）构造成 `TaskQuestion` 复用 `batch_generate_task` 落库为 draft，返回 `TaskResp`。不重新调出题引擎（避免二次生成/超时）。
  2. 前端 `home_notifier.dart`：`generate()` 改为先 `streamTasks(body)` 逐题渲染题卡（`TaskGenPreview(streaming:true)`），流结束后再 `POST /tasks/from-generated` 落库 → `TaskGenSuccess`（跳转草稿审核）。新增 `savePreview(...)`：预览「保存为任务」直接落库已流式题卡（不再二次生成）。`models.dart` 给 `QuestionPreview` 补 `toJson()`。
  3. `parent_task_form_view.dart`：抽 `_currentSpecs(SelectedChild)`/`_currentFocus()`；`_buildActionArea` 在流式/落库期间隐藏按钮、首卡到达前显示加载、之后仅逐张显示题卡（无重复 spinner）；预览「保存为任务」→ `_savePreview`。
- 验证：后端 `uv run ruff` 通过；`tests/api/routes` 60 passed/1 skipped（新增 `test_from_generated_route.py`：落库题量与内容、`422` 空题、`403` 越权）；前端 `flutter analyze` 在改动 3 文件「No issues found」。`test/genkit_client_test.dart` 的 4 个 error 为预存（genkit 0.15.1 `ActionStream` 构造签名变化，与本次无关）。

## 修复二：生成任务 422 + 预览出题返回 mock
- 现象：(1) 点「生成任务」→ `POST /tasks/from-generated` 返回 422；(2) 点「预览出题」返回 mock 数据（而非真实模型生成）。
- 根因1（422）：前端 `home_notifier.dart` 的 `generate()` 调 `_persist(questions, body)` 时，`body` 漏注入 `questions`（`TaskFromGenerated.questions` 必填 `list[dict]`）；`savePreview` 才显式加了。修复：在 `_persist` 内统一 `body['questions'] = questions.map((q)=>q.toJson()).toList()`，并删除 `savePreview` 的冗余注入。
- 根因2（mock）：流式 flow `tasks_generate`（flows.py）调 `resolve_engine(input.model, parent_id=parent_id)` **没传 session**，且 `parent_id` 来自 `ctx.context` 是**字符串**（UUID 对象的 `str()`）；`engine.py` 第 1 步 `mc.parent_id == parent_id` 是 `UUID==str` **恒为 False**。两因叠加 → `ModelConfig` 自定义模型永不命中，回退全局 `LLM_PROVIDER=mock`（默认）。而同步 `batch-generate` 传的是 UUID 对象 + session，故曾行为不一致（也解释了之前"真实模型超时"只发生在同步路径）。
- 修复：(a) `engine.py` 比较改为 `str(mc.parent_id) == str(parent_id)`（兼容 UUID/str）；(b) `flows.py` 的 `tasks_generate` 与 `tutor_ask` 均在 `with Session(db_engine) as s:` 内调 `resolve_engine(..., session=s)`，使流式路径的 ModelConfig 解析与同步路径一致。
- 验证：后端 `ruff` 通过；新增 `tests/ai/test_engine_resolve.py`（字符串 parent_id 解析 ModelConfig、越权返回 None、缺 session 跳过）3 passed；全量 `pytest` 130 passed/3 skipped。前端 `flutter analyze` home_notifier 无警告。
- 说明：若用户未在下拉选模型且全局 `LLM_PROVIDER=mock`（无 key），预览仍走 mock——这是零 key 闭环设计；要真实生成需在 UI 选真实模型或配置全局 provider+key。

## 修复三：「生成任务」「预览出题」报 stream finished without a final result chunk
- 现象：两按钮都报该错，后端 `POST /ai/tasks/generate` 返回 200。根因两层：
  1. 主 bug：`generate_questions_stream` 真实引擎路径取 `result = await sr.response` 后直接 `result.stem`/`yield result`，但 Genkit 流式 `.response` 是 `ModelResponse`，结构化输出在 `.output`（而非响应对象自身）→ `AttributeError` 抛异常。异常被 `genkit_fastapi.event_stream` 捕获并发 `data: {"error": ...}` 错误帧，但 Dart 客户端（genkit 0.15.1 client.dart:97）**只认 `error: ` 前缀帧**，`data:` 错误帧既无 `result` 也无 `message` 键被静默丢弃 → `responseCompleter` 永不完成 → 报 "stream finished without a final result chunk"。
  2. 错误被掩盖：服务端错误帧前缀与 Dart 客户端不一致。
- 修复：
  - `flows.py`：`generate_questions_stream` 改为 `parsed = result.output` 再 `yield parsed`（QuestionSchema）；单题失败 `except` 回退 `_mock_question` 保证流不中断、末帧仍正常。
  - `main.py`：monkeypatch `genkit_fastapi.handler.format_stream_error`，把错误帧前缀从 `data: {"error": ...}` 改为 Dart 客户端可解析的 `error: {"error": {...}}`（含 message）。
  - `tests/utils/sse.py`：`parse_genkit_sse` 同步识别 `error: ` 前缀错误帧并抛 AssertionError（与真实线格式对齐）。
- 验证：无效 spec 实测返回 `error: {"error":{"message":"3 validation errors for TaskGenInput..."}}`，Dart 端将正确抛真实文案；后端全量 `pytest` **130 passed/3 skipped**（`test_generate_stream.py` 2 passed、`test_engine_resolve.py` 3 passed 均含）；`ruff` 通过。前端未改动（server-side 修复）。

## 设计讨论：合并「生成任务」+「预览出题」为统一「出题」（grill-with-docs）
- 用户报「生成任务」题都生成出来后 422（日志 `POST /tasks/from-generated -> 422`）。经穷举复现：端点仅在 `questions` 缺失或 `child_id` 非法 UUID 时 422；前端 `child_id` 恒为合法 UUID（`SelectedChild.id` String），且 `_persist` 已注入 `questions`（修复三的 home_notifier.dart:98）。真实流式题卡原样 POST 只拿 403（越权，非 422）。结论：用户 422 极可能是后端未热加载上轮的 `_persist` 修复 → 让其重启 `uv run fastapi dev` 再验；若仍 422 需贴 Network 面板 `detail` 原文。
- `InsecureKeyLengthWarning`（HMAC key 20B<32B）为独立安全问题，SECRET_KEY 应 ≥32 字节，先记下。
- 架构确认：两端点均为客户端主动调用（home_notifier.generate→_genkit.streamTasks→/ai/tasks/generate；流结束→_persist→POST /tasks/from-generated），服务端不自驱。
- 经 `/grill-with-docs` 访谈锁定合并方案（ADR-0016）：单一「出题」按钮 → 流式预览 → 家长手动「存为任务(draft)」/「加入题库(逐题多选)」；题任务关系=关联引用（question_id 指向题库 Question）；复用 from-generated，新增 POST /questions/bank/bulk。已写 ADR-0016 到 `娃娃学习App_ADR.md`、补术语表「出题与题库同步」段。
