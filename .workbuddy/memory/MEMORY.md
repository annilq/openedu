# MEMORY.md — 长期记忆（已收敛）

## 启动约定
- 每会话第一步 Read 根 `AGENTS.md`（`/Users/yunqi/Documents/develop/openedu/AGENTS.md`），含包管理器（后端 uv / 前端 flutter）、每任务命令、硬约束、细分规范链接、macOS 联调网络权限。

## 项目位置
- 娃娃学习App：`/Users/yunqi/Documents/develop/openedu`（原 WorkBuddy 目录已弃）。

## 后端（FastAPI + SQLModel + uv）
- 验证：`cd backend && uv sync` / `uv run pytest`。无 Alembic；迁移手写于 `app/core/db.py:run_migrations`（兼容 sqlite TEXT / postgres JSON，幂等）。
- **Feature-First 架构（ADR-0027，2026-09-08 落地）**：每能力一个 `app/features/<name>/`（router+schemas+repository+service）；ORM 表集中 `app/db/models/`；共享内核 `app/domain/`（provider/safety/retriever/quota 等 AI 基础设施）+ `app/core/`（config/security/errors/db/deps），不拆；`app/ai`（Genkit flow）+ `app/ai/subagents`（ADR-0021/0024 编排）留共享内核。~**已删** `app/crud.py`/`app/api/routes/`；~`app/models.py` **保留为 re-export shim**（仅 `from app.db.models import ...` 兼容 13+ 处旧 `from app.models import X`，ORM 实体以 `app/db/models/` 为唯一注册源）；`app/api/deps.py` **保留**（`CallerDep`/各角色依赖）。依赖统一走 `app.core.deps`（CurrentUser/CurrentParent/CurrentChild/CallerDep/SessionDep）。`TutorLog` 落库只由 `app/features/assistant/router.py` 负责（带真实 grade），SubAgent/flow 不重复落。
- **AI 助手统一入口（ADR-0024/0025/0026，2026-09-08 落地）**：所有 AI 功能（出题/伴学答疑/查任务/治理观测除外）统一走 `POST /api/v1/assistant/chat`（SSE，AG-UI 事件信封）。旧 `/ai/tutor/ask`、`/ai/tasks/generate`、`/tutor/ask` 已删除；`/tutor/{logs,quota,usage}` 与 `/ai/debug/conversations*` 属治理/观测，保留。运行时内核 `agent_core/`（protocol/ports/router/registry/runtime/subagent/tools/errors + adapters/）+ `app/ai/subagents/<business>/` 文件夹化 subagent（question/tutor/tasks，各含 manifest.py+agent.py+tools/+skills/）；意图路由 = manifest `triggers` 规则优先 + 启发式兜底。前端 `lib/features/assistant/`（SSE client + notifier + 悬浮按钮 + 对话弹窗）统一消费；首页出题（home_notifier）与伴学（tutor_notifier）均已改接 `/assistant/chat`。**genkit 已彻底退役**：前端 `genkit_ai_client.dart` 与 `pubspec.yaml` 的 `genkit` 依赖均于 2026-09-08 删除（`flutter pub get` 已清其传递依赖）；后端 `app/ai/flows.py` 的孤儿 genkit flow（tutor_ask/tasks_generate）已删，`genkit` 仅作底层 LLM 引擎经 `engine.genkit` 被 `app/domain` 调用，不经 `genkit_fastapi` 暴露原生 action。
- LLM：`resolve_engine` 解析优先级 = 家长 `ModelConfig` 表 → `BUILTIN_MODELS`(env) → 全局 `LLM_PROVIDER` → None；业务只依赖 `LLMProvider` 抽象。`genkit` 仅 import 于 **`agent_core/adapters/genkit.py`**（工厂 `build_genkit_engine` 内延迟导入，全后端唯一落点）。Genkit 不做编排层。
- **agent_core 三层结构（ADR-0031/0032，2026-09-10 落地）**：内核 `agent_core/`（协议+端口 `ports.py`+编排，**零第三方/零 `app.*`**）→ 适配器 `agent_core/adapters/genkit.py`（`build_genkit_engine` 构造 Genkit + `GenkitLLMProvider` + `decode_stream`/`Segment`）→ 集成 `app/`。依赖单向 `app → adapters → kernel`。**命名对齐六边形（ADR-0032）**：`seams.py`→**`ports.py`**（与 `adapters/` 成对）、弃用 `contrib` 提法改 `adapters/`；**genkit 不进内核**（会污染基座、强制外部方案吃 genkit）。`app/ai` 顶层**只剩** `engine.py`（纯配置解析→委托适配器）`model_catalog.py`（服务商产品预设）+ `subagents/`；**出题业务全在 `app/ai/subagents/question/`**：`pipeline.py`（prompt 组装 + `step_label` + `stream_question` + provider 版 `generate_question`）、`parsers.py`（`QuestionSchema`/`SchemaQuestionParser`）。共享件在 `app/domain/`：`prompts.py`（`EDU_SYSTEM_PROMPT`）、`structured.py`（`schema_field`/`coerce_dict`）、`grader.py`（`Grader` + `GradeSchema`）。出题统一走 `LLMProvider`（落库重生成 = `build_provider(engine=…)` → `pipeline.generate_question(provider, …)`）。**已删** `app/ai/generation.py`、`app/ai/parsers/`、`app/ai/segment.py`（→适配器）、`app/ai/debug_log.py`（零调用方死代码）、engine 版 `generate_question_stream`。**subagents 仍留 `app/ai/subagents/`**（本应用专属，ADR-0032 决定**不提**为 `backend/subagents/`）。**打包：`agent_core` 随 app wheel 一起打包**（`backend/pyproject.toml` 的 `packages=["app","agent_core"]`；已删 `agent_core/pyproject.toml`，**不单独发布**——取代 ADR-0031 计划第 7 步「发内部 PyPI」）。已清 `app/features/ai/repository.py` 写入侧死代码（`create_conversation`/`add_message`/`finish_conversation`），只留只读 `list_conversations`/`get_conversation_messages`（`/ai/debug/conversations` 用）。**分层不变量已机制化（2026-09-10）**：`backend/tests/ai/test_layering_invariants.py` 用 AST 断言守护 ①内核零 `app.*` ②内核零第三方 ③`import genkit` 全后端仅 `adapters/genkit.py` 且必须函数体内 ④已删 `app.ai.{generation,parsers,segment,debug_log}` 零残留；扫描**必须走 AST**（`grader.py` docstring 提到旧模块名，grep 会误报）。`agent_core` 不在 `uv.lock`、未装进 `.venv`（靠 backend/ 为 rootdir 路径导入），但 wheel 内含全部 11 文件（`uv build --wheel` + 隔离 `pip install --no-deps` 实测可用、不拉 genkit）。
- **测试一律不得依赖真实模型（2026-09-10 定规，CI 红线后立）**：`tests/conftest.py` 在 import app 前置 `os.environ["LLM_PROVIDER"]="mock"`，并 autouse 打桩 `app.ai.runtime.runtime.build_provider` 为 `tests/utils/fake_provider.FakeLLMProvider`（确定性文本/题卡）。根因：本地 `backend/.env` 有 deepseek key → 本地绿、CI 无 key → 出题/答疑断言红（本次 2 failed）。**新增任何走 LLM 的测试都不许接真模型**（真模型连通性只由 `tests/domain/test_llm_smoke.py -m smoke` 负责）。打桩点是**端点模块自身**的工厂 `app.features.assistant.router.build_provider`（该模块 import 期已把名字绑进自身命名空间；改 `app.domain.build_provider` 无效）。注意 `app.features.tasks.router` 的落库出题走**真实** `app.domain.build_provider`（不被打桩）——故 tasks 相关测试须自行 monkeypatch `app.ai.subagents.question.pipeline.generate_question` 或 `resolve_engine`。副作用：全套件 22s → 6.5s。
- `uv` **不在默认 PATH**：用 `/opt/homebrew/bin/uv`（或 `~/.local/bin/uv`）。
- `TutorService.aexplain` 是唯一实现，`explain` 只是 `asyncio.run(aexplain(...))` 的同步桥（曾因复制粘贴双份导致无引擎时异步侧漏判 `raw is None` → 下发 `answer=None/blocked=False`）。

- **ADR-0033 业务查询 Tools（2026-09-10 立；六阶段全部完成，256 passed/2 skipped）**：`agent_core` 的 `run_with_tools` 原是死代码（三个生产 subagent 全 `tools=[]`）。根因三处已查证：①适配器传 dict 给 `generate_stream(tools=)`（形参是 `Sequence[str|Tool]`）②适配器 `stream(tools=)` 从不 yield `ToolCall` 且 `except: pass` 静默降级 ③**`history` 形参被适配器完全忽略** → 工具回灌丢失 + `while True` 无轮次上限 = 死循环（`FakeLLMProvider` 靠 `if history` 掩盖）。方案：L2 原生 function calling，`return_tool_requests=True` + `genkit.tool(...)` 构造**占位 Tool**（不注册、函数体永不执行），执行权留内核；`max_turns=3`（`BaseSubAgent` 类属性）；硬失败 `TOOL_UNSUPPORTED`（删静默降级）。工具 handler 复用 request session **同步直调**（SQLModel Session 非线程安全，**禁** `to_thread` 复用）。**测试替身（阶段 6）**：`FakeLLMProvider` 支持 `tool_script` / `script(...)` 多跳脚本，跳数**只数 history 里 `role=="tool"` 条目**（`_completed_hops`）——旧 `if history` 一刀切会被 `/assistant/chat` 客户端自带 history 吞掉首轮工具调用；`requests`/`calls` 供次数与跳序断言；不传脚本＝默认单跳（既有用例零改动）。端点端到端用例在 `tests/api/routes/test_assistant.py`（复用 `tests.ai.test_query_tools_contract._setup` 数据装置 + `tests.ai.test_fake_provider_tool_script.py` 守替身自身契约）。
  - **已落地**：`agent_core`（`max_turns`/`render_tool_result`/`TOOL_TURN_LIMIT`/`TOOL_UNSUPPORTED`）→ `adapters/genkit.py` 工具路径（`return_tool_requests=True` + `_build_tools`/`_build_messages`/`_extract_tool_calls`）→ 4 份 `features/<x>/service.py`（children/tasks/mastery/review，只含读路径）→ `app/ai/subagents/query/tools/`（7 个只读工具 + `_shared.py` 的 `resolve_children`/`project_for_role`/`envelope` + `registry.QUERY_TOOLS`）→ `app/ai/subagents/query/`（manifest+agent+render+skills）。
  - **`query` = 唯一生产 tool loop subagent**：`business="query"`/`name="学情查询"`/`roles=[parent,child]`/`priority=12`（接住原 tasks 的 10，压住 question 的泛词「题目」）/`skills=["query_sop"]`；`__init__` 里 `self.tools = list(QUERY_TOOLS)`（非空即触发 runtime 的 `run_with_tools`）；`run` 只作「绕过 runtime 直调」兜底（委托同一套 `run_with_tools`，不复制编排）。**`tasks` 已删**（`conversation.kind` 由 tasks→query，历史不迁移）；**`app/ai/tools/` 已整包删除**（`list_tasks` 逻辑并入 query 工具、`search_knowledge` 零调用方），已加入 `test_layering_invariants.DEAD_MODULES`。
  - **卡片契约（决策 11）**：`TOOL_RESULT` 存原始载荷，`query/render.py::render_cards` 折成前端 `_CardTile` 形状 `{type, subject, stem}`（`stem` 空则不渲染），由 `QuerySubAgent.render_tool_result` 逐条发 `DATA` 帧（`data.type=="query"`）→ **前端零改动**。卡片只做展示摘要：不二次裁剪角色（裁剪只在 `project_for_role`）、不吐原始 JSON、超 5 条截断。`DATA` 帧必在 `TOOL_RESULT` **之后**。
  - **测试装置坑**：`tests/ai/` 原缺 `__init__.py`（`tests/api`、`tests/domain` 都有）→ 已补，否则 prepend 模式下 `tests/ai/test_x.py` 被收成顶层模块名 `test_x`，跨文件 `from tests.ai.test_x import _setup` 会加载出两份模块实例。查询用例复用 `tests/ai/test_query_tools_contract.py::_setup` 数据装置（1 家长+2 娃娃+任务+错题+未派发草稿），不写第二份。
  - **阶段 1（内核）完成 2026-09-10**：`errors.py` 加 `ToolUnsupportedError`；`BaseSubAgent` 加 `max_turns=3` 与 `render_tool_result(name,result)->list` hook；`run_with_tools` 重写为有界 `for` 循环（`TOOL_TURN_LIMIT` / `TOOL_UNSUPPORTED` 硬失败并 return / 同轮多工具全执行 / `default=str` 兜 UUID）。测试 `tests/ai/test_tool_loop_bounds.py`；全量 **187 passed / 2 skipped**。
  - **阶段 2（genkit 适配器）完成 2026-09-10**：`agent_core/adapters/genkit.py` 新增工具路径（`_stream_with_tools` + `_build_tools`/`_build_messages`/`_extract_tool_calls`/`_response_text` + `_placeholder_tool`）。**占位 Tool 用 `genkit.tool(fn,name=,description=,input_schema=)`（临时不注册；`register_tools` 调用期挂子 registry）——不是 `define_tool`（需 Registry）**。`generate_stream(messages=[...], prompt=..., tools=[Tool...], return_tool_requests=True)`；genkit 组装顺序 = **system → messages → prompt**。`ToolResponse.output` 传 **JSON 文本字符串**（openai 插件做 `str(output)`，传 dict 会变单引号 repr）。**history 契约破坏性变更：回灌须成对**——先 `{"role":"assistant","content":str,"tool_calls":[{name,args,ref}]}` 再 `{"role":"tool","name","ref","content"}`；`ref` 由 `run_with_tools` 的 `ref_seq` mint，**请求/结果必须同值**（= provider `tool_call_id`，错配 OpenAI 400）。新增 `tests/ai/test_genkit_adapter_tools.py`（25 例，**不 import genkit**，鸭子类型替身，否则破 layering invariant 3a）；全量 **213 passed / 2 skipped**。
  - **genkit 0.10.0 事实（已实证，勿再猜）**：顶层导出 `Message/Part/Role(system|user|model|tool)/TextPart/ReasoningPart/ToolRequest/ToolRequestPart/ToolResponse/ToolResponsePart/tool`；`define_tool` 只在 `genkit._ai._tools`（需 Registry）。`ToolRequest{ref,name,input,partial}`、`ToolResponse{ref,name,output,content}`。openai wire：assistant → `{role:'assistant',tool_calls:[{id:ref,type:'function',function:{name,arguments:json.dumps(input)}}]}`；tool → `{role:'tool',tool_call_id:ref,content:str(output)}`。`_ai/_generate.py`：`if turn_options.return_tool_requests or len(tool_requests)==0: return`（故 return_tool_requests 时 genkit 不执行工具）。**读 genkit 源码时 bash `grep` 对 `_core/_typing.py` 返回空**，须用 `python3 - <<'EOF'` 正则提取。
  - **坑：全量 pytest 40–90s，前台 120s 超时会被 SIGTERM 掐成 exit 137 且无输出 → 必须 `run_in_background`**。
  - **阶段 3（业务层下沉 service）完成 2026-09-10 —— 纯重构**：新建 `features/{children,tasks,mastery,review}/service.py`（**只含读路径**，写路径不动）。`children/service.py::require_owned_child(session,parent,child_id,code=,message=)` 收口原先散在 tasks×5+mastery×1 的同形归属判断（code/message 可覆写 → 响应体逐字不变）；`list_children_of` 供工具取孩子列表（**禁**跨 feature 直连 auth 仓储）。`tasks/service.py`：`question_to_resp/task_to_resp/wrong_question_to_resp` + `list_parent_tasks/list_today_tasks/list_wrong_questions/child_progress` + 家长复合用例 `list_owned_child_wrong_questions/owned_child_progress`。`mastery/service.py`：`build_mastery` + `get_mastery_for_user`（双角色）。`review/service.py`：`list_due_reviews`。`tasks/repository.py` 加 `list_tasks_by_parent`。router 用模块别名限定调用（`tasks_service.X`）；tasks −134 / mastery −65 / review −26 行，**路由/状态码/响应模型/错误码/文案全不变**。新增 AST 不变量 ⑤service 不得 import router ⑥service 不得 import `fastapi`（已实测能捕获违规）。全量 **215 passed / 2 skipped**。
  - **阶段 4（工具层）完成 2026-09-10**：`app/ai/subagents/query/tools/` = `_shared.py` + 7 工具模块（各导出 `SPEC`）+ `registry.py`（`QUERY_TOOLS`）。**`tools/__init__.py` 刻意不 import registry**（否则包级循环）。工具：list_children / list_parent_tasks / list_today_tasks / list_wrong_questions / list_due_reviews / get_progress / get_mastery。`_shared` 提供 `ANSWER_FIELDS={answer,explanation}`、`LOCATOR_PROPS`、`ToolArgumentError(ValueError)`、`resolve_parent`、`resolve_children`（**娃娃恒为自己并忽略入参**；家长都不传=名下全部；指定但不在名下→抛错不静默）、`project_for_role`（**递归**）、`dump`（`model_dump(mode="json")`，**必须**，否则 history 存进 Python repr）、`child_block/envelope`。**统一出参信封** `{children:[{id,name,grade,items,meta}],unassigned_items,total_children,total_items}`（进度/掌握度等单条聚合也塞进 `items` 单元素列表 → 全工具同构）。**三个非显然决定**：①`list_parent_tasks` 在娃娃端**等价 list_today_tasks**（娃娃的 `ctx.extra["parent_id"]` 是其家长，走家长分支会返回兄弟姐妹全量任务=越权）；②未派发草稿只在**未指定目标娃娃**时进 `unassigned_items`，精确指定即剔除；③`require_owned_child` 抛的 `AppErrorException` 在 `_shared` 内**归一为 ToolArgumentError**（工具层不需要 HTTP 信封语义）。契约测试 `tests/ai/test_query_tools_contract.py`（16 例：工具形状/娃娃端无答案+**家长视角对照组**/投影递归/越权/信封同构/**全部工具只读**（6 表行数不变）/聚合抽查）。AST 不变量扩至 **8 条**（新增 ⑦`query/tools` 不得 import router/repository ⑧不得 import fastapi）。全量 **233 passed / 2 skipped**。
  - **变异验证纪律（踩过坑）**：断言守卫有效性时必须确认测试**真的执行了**——`pytest -k "child_clean"` 匹配不到 `test_all_tools_return_clean_payload_for_child`，跑出 "16 deselected" 空集，差点误判「守卫有效」。**看 passed/failed 数，不看 deselected**。本轮两步变异均实测有效（去掉投影 → 精确报泄漏字段；越界模块 → 不变量 7/8 双双报路径）。
  - **测试 helper 坑**：`tests/utils/user.py::register_parent` 返回 **200 不是 201**，且响应体只有 `access_token`（无 `user` 字段）；家长 id 须 `session.get(User, child_id).parent_id` 反查。
  - **坑：并行跑两个 pytest 会让 `conftest` 的 `test_app.db` unlink 被沙箱 safe-delete broker 拒（`PermissionError: broker denied delete`）→ 串行跑。**

## 前端（Flutter + Riverpod + shadcn_ui + cupertino_ui）
- 验证：`cd frontend && flutter pub get` / `flutter analyze`（零警告）/ `flutter test`。
- **Flutter SDK 不在默认 PATH**：用绝对路径 `/Users/yunqi/Documents/flutter/bin/flutter`。`flutter test` 必须剥代理：`env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy /Users/yunqi/Documents/flutter/bin/flutter test`（否则 flutter_tester WebSocket 被本机代理拦截，全部测试加载失败，与代码无关）。
- **shadcn_ui 硬约束（入口是 `ShadApp`=WidgetsApp，整树无 Material 祖先）**：
  - 禁止 Material Chip 家族（`ChoiceChip`等）→ 用 `ShadButton`/`ShadButton.outline` 或 `shared/widgets/app_chip.dart` 的 `AppChip`。`Switch/Checkbox/Radio/Slider` 同理改 shadcn 件（`ShadSwitch` 等）。
  - `AppCard(onTap:)` 用 `GestureDetector` 包裹，禁止 `ShadButton.ghost(width:double.infinity)`（无限宽撑爆卡片内容）。
  - **shadcn `ShadInput` 文字垂直居中根因+修法（2026-09-08，已实测验证）**：shadcn 的 `EditableText` **不暴露 `textAlignVertical`**（全 0.56.3 源码无此参数），默认 `top` → 文字落在编辑盒**顶端**；盒子居中 ≠ 文字居中（compact 偏上 ~2.8px、child 模式偏上 ~8.7px）。`editableTextSize: Size(maxW, controlH-4)` 只钉盒高、**字形仍顶对齐**，不能解决。正确修法：单行输入加 `strutStyle: AppControl.inputStrut(context, style)`（`forceStrutHeight:true` + `height=(controlH-4)/fontSize` 把行高强制撑满编辑盒，Flutter 半行距使字形上下均分→居中）。多行输入（本就该顶对齐）/ 带大竖向 padding 的输入框（如聊天栏，盒子高、文字盒短而自然居中）**勿设** strut。裸 `ShadInput` 也走同款 `inputStrut`。全局约束 `minHeight(controlH)`（AppTextField 另用 `tightFor`），tight 32 盒子含边框+shadcn 内部预留使可编辑区 = controlH-4。
  - 含 `ShadButton` 的 widget 测试需 `ShadTheme` 祖先（`ShadApp.custom(appBuilder: (c)=>MaterialApp(home:X))`）。
  - 描边仅 `outline` 一档；主题映射单向走 `AppTheme.shadThemeData()`；品牌主色统一靛蓝（`AppColors.primary`/`onPrimary`）。
  - `LucideIcons` 来自 `shadcn_ui` 再导出，用到须 import `shadcn_ui`。
- 做题「当场订正」不推进遗忘曲线（2026-09-04）：订正复用 `/tasks/{id}/answer` 就地判分，答对不调 `mark_review_result`。
