# 2026-08-30

## 设计重定向（ADR-0014）延伸：掌握度可视化按学科色着色
- 改 `frontend/lib/features/home/presentation/widgets/parent/parent_overview_view.dart`：
  - `_MasteryBar` 进度条 `color` 由等级色改为 `SubjectAccent.forContext(fromName(item.subject), ctx).accent`；行首加 10×10 学科色点。
  - 删除原 `_MasteryLevel` 枚举 + `_mappingOf`，新增顶层 `_levelBadge`（已掌握/较扎实=successChip、巩固中=infoChip、薄弱/待加强=warningChip），保留等级警示。
  - `_buildMastery` 顶部加学科色图例 `Wrap(AppTags.subject(...))`。
- `flutter analyze` No issues found（修一处顶层函数误用 static 的 lint）。
- 刻意未做：娃娃端掌握度视图（无看板，需新增目的地+屏，超出着色范围）；学科色已在娃端任务卡/做题页标签消费。
- 产物：`design/mastery-subject-color-landing-2026-08-30.md`。ADR-0014 学科色闭环至此完成（家长端看板 + 娃端标签均已消费）。

## 娃端「我的学科掌握度」屏（ADR-0014 收尾）
- 抽共享 `features/home/presentation/widgets/mastery_board.dart`：`MasteryBoard`(ConsumerWidget, `isChild` 参数切第一人称文案) + `_MasteryBar` + `_levelBadge`，家长/娃共用。
- `parent_overview_view.dart`：`_buildMastery` 退化为 `=> const MasteryBoard()`；删除迁走的 `_levelBadge`/`_MasteryBar` 死代码；移除不再用的 `models.dart` import。
- 新 `features/home/presentation/screens/child_mastery_screen.dart`：`ConsumerStatefulWidget`，`initState` 用 `widget.user.id` 触发 `masteryNotifierProvider.load(...)`；渲染 `SectionTitle('我的学科掌握度')` + `MasteryBoard(isChild:true)`。
- `home_screen.dart`：`_childDestinations` 加第5项「掌握度」(`LucideIcons.target`, index4)；`_buildChildView` IndexedStack index4 加 `ChildMasteryScreen(user: widget.user)`；import 新增。娃端导航：首页/复习/错题本/AI伴学/掌握度 + 我的。
- `flutter analyze` No issues found（修三处：娃屏缺 flutter 基础 import、`ConsumerState.build` 误用双参、parent 调用点残留 `ref` 实参）。
- 产物：`design/child-mastery-landing-2026-08-30.md`。**ADR-0014 全部落地闭环**（①审计 ②双模式 ③响应式 + 学科色两端消费）。
