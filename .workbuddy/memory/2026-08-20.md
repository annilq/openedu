# 2026-08-20

## 娃娃日常学习应用 · 需求访谈对齐（/grill-with-docs）
通过 relentless 访谈为用户的"娃娃学习App"完成决策对齐，产出三份文档（位于工作根目录）：
- `娃娃学习App_产品方案.md` —— 背景/角色/分期/学科范围/架构/路线图
- `娃娃学习App_ADR.md` —— 12 条架构决策记录
- `娃娃学习App_术语表.md` —— 统一词汇

### 关键决策
- 用户：二年级 + 四年级娃娃，规划小学→初中
- 前端：Flutter 原生 App（平板优先）
- 后端：Python + LangChain（自封领域接口 + LangChain provider 抽象，不重复造轮子）
- 数据：PostgreSQL（本地 Docker 起步，架构按云设计）
- 账号：家长管理员 + 娃娃独立账号
- 内容：对齐教材；前期 AI 联网检索知识点，后期知识库 RAG；一期客观题优先、AI 即时批改
- 分期：一期刷题+打卡 / 二期错题本+遗忘复习 / 三期 AI 答疑
- 安全：双层防护 + 家长可见日志 + 每日时长上限
- 合规：教材版权上线前必须解决（标红）
- Pi/Eve 不作为运行时（Pi 作开发助手）；模型暂不定，provider 可插拔
- 用户选择先出方案文档（走A），后续可叫我落地最小原型（B）

## 娃娃学习App · 最小原型落地（B）与本地验证
- 进入 B：搭出可跑后端 + Flutter 骨架，本地（mock 模式、无需模型 key）验证完整闭环成功。
- 后端：FastAPI + SQLAlchemy(async) + JWT；domain 层 QuestionGenerator/Grader + LLMProvider 抽象（MockProvider 兜底 / LangChainProvider 真实调用）；路由 auth/children/tasks。
- 前端：Flutter login + home（家长端生成任务/看娃娃列表，娃娃端做题/打卡）。
- 验证闭环全绿：注册家长→加娃娃→生成任务(2题)→今日任务→答题(对错批改)→打卡→进度(正确率0.5/连续打卡1天)，后端无报错。
- 踩坑修复：①passlib 与新版 bcrypt 冲突，改用 bcrypt 直连；②SQLAlchemy async 需 greenlet（已加 requirements）；③progress 误用 func.count(X).where，应为 select(func.count(X)).where。
- 交付：backend/ + frontend/ 代码骨架 + README.md + 三文档，共 31 个文件。

## 流程纠正：补齐"技术架构选型 + 详细功能文档"
- 用户反馈：B 阶段直接写代码，跳过了「后端技术架构选型」「Flutter 技术架构选型」「详细功能文档」，不符合正常开发流程。
- 已补三份文档（工作根目录）：
  - `技术架构_后端.md` —— 分层/目录/领域层/ER/API契约表/安全/部署/二三期扩展点；基于已落地代码写实。
  - `技术架构_Flutter.md` —— 采用用户指定的 `flutter-clean-architecture-riverpod`（Clean Arch + Riverpod）基线，落地分层/目录/状态管理/网络/路由/平板适配/扩展点。
  - `功能规格_PRD_全三期.md` —— 一/二/三期均详细：EARS 需求、功能清单、流程、交互、边界、权限、埋点、验收标准；版权标红为硬门槛。
- 用户明确选择：Flutter 用 Clean Architecture + Riverpod（参照指定 GitHub repo）；PRD 写全三期；架构预留二三期扩展点。
- 教训：后续任何功能开发，应先有架构/功能文档并经用户确认，再写代码（正常流程：需求→PRD→技术架构→评审→实现）。

## /to-spec 生成合并 spec
- 用户执行 `/to-spec`，将整段对话综合为面向 agent 实现的 spec（Problem/Solution/User Stories/Implementation/Testing/Out of Scope/Further Notes）。
- 落盘：`spec_娃娃学习App.md`。测试 seam 定为：主=HTTP API 契约(TestClient 端到端)，次=领域层单测(Grader/Provider 契约)。
- 发布受阻：项目未连接任何 issue tracker（TAPD/CNB/GitHub 均 disconnected），且无项目事项创建工具，无法打 `ready-for-agent` 标签发布；spec 内置该标签记录，待用户决定发布去向。

## /to-tickets 拆分 tracer-bullet tickets
- 用户执行 `/to-tickets`，将 spec 拆为 12 个垂直切片 ticket，落盘为本地 `.scratch/wawa-learning/issues/01-12-*.md`（无 tracker 可发布，按 to-tickets 本地文件约定）。
- 切分：T01 测试基座(无依赖) → T02 Flutter MVP Clean Arch(T01) → T03 LLM 真接通(T01) → T04 错题归集(T02) → T05 遗忘曲线(T04) → T06 掌握度看板(T04) → T07 Flutter 错题本(T05,T06) → T08 IntentRouter+Tutor(T03) → T09 安全层+日志(T08) → T10 AI 时长管控(T08) → T11 知识库RAG(T08) → T12 Flutter AI伴学(T08,09,10,11)。
- 每 ticket 含 What to build / Blocked by / Status: ready-for-agent / Acceptance criteria；均按依赖顺序编号。
- 用户确认：粒度合适、依赖正确、落盘为本地文件。

## 后端架构改用 fastapi/full-stack-fastapi-template 二次开发
- 用户要求后端基于官方全栈模板做二次开发。
- 关键决策（用户确认）：ORM 切 SQLModel（同步）、现在就脚手架移植、Python 锁 3.14。
- 完成：克隆模板 → 裁剪（去 React/邮件/Traefik/Alembic/Items/Superuser）→ 用模板约定重写全部后端代码（core/config+db+security、api/deps+main+routes、crud、models SQLModel+UUID 主键、domain 领域层完整移植）→ 本地 Python 3.14 venv 验证 pytest 2 passed（闭环+错误路径全绿）。
- 认证方案从「bcrypt 直连」升级为模板的 `pwdlib[argon2,bcrypt]` + `pyjwt`（正好规避 passlib 冲突坑）。
- 已更新文档：ADR-013（新增）、技术架构_后端.md（全面改写为 SQLModel+模板分层）、spec_娃娃学习App.md（Implementation/Testing/Further Notes 改为模板基线）、ticket T01（标注已完成的基线状态+补充项）。
- 后端最终文件：app/(main+models+crud+core/+api/+domain/) + tests/(conftest+utils+test_loop) + pyproject.toml + Dockerfile + docker-compose.yml + .env(.example)。

## T02: Flutter MVP Clean Architecture + Riverpod 落地
- 用户要求完成 T02（Flutter MVP 全量）。
- 完全重写 frontend/：从 http 包骨架升级为 Clean Architecture 三层（data/domain/presentation）+ Riverpod StateNotifierProvider + Dio 网络层（Token 拦截器 + 错误统一为 HttpException）+ sealed class 状态机（Idle/Loading/Success/Error）+ shared_preferences 本地存储。
- 32 个 Dart 文件，覆盖 6 个 feature：authentication（登录注册）、home（家长端/娃娃端角色分流）、practice（做题闭环：出题→作答→即时批改→解析→下一题→完成打卡）、children（娃娃账号管理）、profile（个人设置/退出）。
- 平板适配：护眼暖色主题（≥20sp 正文、大圆角、大点击区）、横屏友好。
- 后端增量：新增 `GET /api/v1/auth/me` 端点（前端登录后获取用户 role/grade），含 pytest 测试（3 passed）。
- 设计决策：无代码生成（用 sealed class 代替 freezed、Navigator 代替 auto_route），避免 build_runner 依赖；Token 获取流程：register/login → saveToken → 调 /auth/me → 获取用户信息 → saveUserJson。
- 防作弊：娃娃端 `/today` 恒不返回 answer，家长端 `generate` 返回 answer。
- 待补：各 notifier 的 mocktail 单测。

## T03: 可插拔 LLM 真接通 smoke
- 用户要求推进 T03（LLM provider 真接通验证）。
- 改动：pyproject 新增 `langchain-openai`（OpenAI 兼容端点覆盖国产模型）；`build_provider` 对未知 LLM_PROVIDER 值告警并回退 mock。
- 新增测试 3 个文件（tests/domain/）：`test_provider_factory.py`（mock 默认/langchain 显式/未知值回退/缺配置报错）、`test_langchain_provider.py`（_build_model 从 settings 接线 base_url/model、generate_question 契约字段齐全、_parse_json 容错）、`test_llm_smoke.py`（真实模型 smoke，`-m smoke` marker，需 LLM_PROVIDER=langchain + key + RUN_LLM_SMOKE=1，默认 skip）。
- 结果：15 passed + 1 skipped（smoke），ruff 全过。顺手修复既有 lint（import 排序/未用 import/未用变量）。
- 踩坑：mock patch 必须在 with 作用域内调用被测方法（_run_with 之前提前 return 导致 patch 已退出）；async provider 方法需 asyncio.run 包装。
- 已验证「换 base_url/model 即换厂商」：test_build_model_uses_settings_config 断言 ChatOpenAI 构造参数全部来自 settings。

## 项目整体移动至 /Users/yunqi/Documents/develop/openedu
- 用户要求把项目从 WorkBuddy 工作区移到 `/Users/yunqi/Documents/develop/openedu`（原目录已存在且为空）。
- 执行：移动全部文件（含隐藏）→ 校验完整（118M）→ 删除空源目录。
- 踩坑：Bash 工具持久 cwd 指向已删目录导致所有命令被拒（"Working directory does not exist"），用子代理以 Write 建占位文件 + rm 重建空目录后恢复。
- 修复：`backend/.venv` shebang 失效 → `rm -rf .venv && uv sync` 重建，pytest 15 passed + 1 skipped 全绿；Flutter 无构建缓存无需处理。
- 新项目根：`/Users/yunqi/Documents/develop/openedu`。旧 WorkBuddy 目录仅保留空占位目录。

## DeepSeek 快捷预设接入并真实验证
- 用户 `.env`/`.env.example` 添加 `DEEPSEEK_API_KEY`/`DEEPSEEK_BASE_URL`，要求读取验证。
- 实现：config.py 新增 `DEEPSEEK_API_KEY/DEEPSEEK_BASE_URL/DEEPSEEK_MODEL(默认 deepseek-chat)` 字段；LangChainProvider 支持双配置源（`LLM_PROVIDER=langchain` 读 LLM_*，`=deepseek` 读 DEEPSEEK_*），`_config_key()` 统一校验；build_provider 接受 deepseek。
- 测试：工厂加 deepseek 显式/缺 key 报错；契约测试加 deepseek 接线；smoke skipif 支持 deepseek。修正 provider fixture 未设 LLM_PROVIDER 导致 RuntimeError 的隐患。
- 验证：`LLM_PROVIDER=deepseek RUN_LLM_SMOKE=1` 真实调用 DeepSeek 成功（1 passed，3s 内出题），settings 读取正常。18 passed + 1 skipped，ruff 全过。
- 提醒：`.env.example` 含真实 key，有泄露风险，用户后续 git 化前应替换为占位符。

## T04: 错题自动归集
- 用户 `/implement T04`。需求：答错自动归集错题集（故事 13）、家长/娃娃可查、单测覆盖三行为。
- 实现：
  - `models.py` 新增 `WrongQuestion` 表（`UniqueConstraint(child_id, question_id)`，first_wrong_at + wrong_count）+ `WrongQuestionResp` 响应模型。
  - `crud.py` 新增 `upsert_wrong_question`（已存在则 wrong_count+1，不建多条）与 `list_wrong_questions`（join Question 按时间倒序）。
  - `tasks.py`：`answer_question` 答错时自动归集；新增 `GET /tasks/wrong-questions`（娃娃自查）、`GET /tasks/children/{id}/wrong-questions`（家长查，校验亲子关系）。错题本含答案/解析供复习（正常做题仍防作弊）。
  - `conftest.py` 清理列表加 WrongQuestion。
- 测试：`tests/api/routes/test_wrong_questions.py` 4 用例（答对不归集、答错归集字段完整、重复错 wrong_count 递增、权限 403）。全量 22 passed + 1 skipped，ruff 全过。
- 踩坑：共享 SQLite + session 级 db fixture → 测试间固定用户名重名冲突，_setup 改为每测试唯一用户名。
- 文档：技术架构_后端.md API 契约表补两接口、修正 `/tasks/generate`→`/tasks`、配置表补 DEEPSEEK_*。
- 后续：T05 遗忘曲线复习依赖本 ticket 的 WrongQuestion。

