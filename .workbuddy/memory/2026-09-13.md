# 2026-09-13

## 出题失败提示「本次未能生成题目，请调整科目或年级后重试」根因与修复

- 现象：前端点「生成任务」报该文案。根因 = 后端 `.env` 的 `LLM_PROVIDER=mock`（默认），
  `resolve_engine()` 返回 None → `GenkitProvider.generate_question_stream` 静默空迭代 → 0 题卡 →
  SubAgent 兜底文案。该文案误导：mock 模式下科目/年级根本没被用。
- 修复（三处，未破坏 `tests/ai/test_question_subagent_stream.py` 5 例，已 `uv run pytest` 通过）：
  1. `backend/app/domain/genkit_provider.py`：引擎为 None 时 `yield QuestionFailed(reason="未配置 AI 出题引擎…")` 而非空迭代。
  2. `backend/app/ai/subagents/question/agent.py`：`run()` 捕获 STEP status=error 的 label 作 `fail_reason`；收尾优先用 `fail_reason`，否则用诚实文案（不再笼统归咎科目/年级）。
  3. `frontend/.../home_notifier.dart`：0 题防御性兜底文案改为中性的「检查 AI 出题引擎配置」。
- `.env` 已切到 `LLM_PROVIDER=deepseek` + 加了 `DEEPSEEK_API_KEY/BASE_URL/MODEL` 行；
  **DEEPSEEK_API_KEY 仍为空**（用户说已提供 key 但本会话未见明文），待用户粘贴 key 后写入并重启后端（改 .env 不热加载）。

## 冲突解决：git stash pop 撞上 agent_core 架构重构

- 本地 HEAD 已是重构后的 `agent_core` 架构（出题走 `pipeline.stream_question` → `provider.stream(schema=QuestionSchema)`，
  经 `translate_stream` 转 AG-UI 帧；`GenkitProvider.stream` 是通用消息级流，委托 `agent_core.adapters.genkit`）。
  我之前的暂存改动基于旧 `app.ai.generation` 架构，`git stash pop` 产生三方冲突（3 文件 both modified）。
- 解决：保留上游架构（`git checkout --ours` 三个冲突文件），丢弃基于旧代码的暂存改动；在新架构上重新施加同一修复意图：
  1. `app/domain/provider.py`：`EducationLLMProvider` 加 `configured` 属性（默认 True）。
  2. `app/domain/genkit_provider.py`：`GenkitProvider.configured` 返回 `self._resolve() is not None`。
  3. `app/ai/subagents/question/pipeline.py`：`stream_question` 在 `provider` 未 `configured` 时显式 `yield QuestionFailed("未配置 AI 出题引擎…")` 而非静默空迭代。
  4. `app/ai/subagents/question/agent.py`：引入 `EVENT_STEP`，`run()` 捕获 STEP status=error 的 label 作 `fail_reason`，收尾优先回显 `fail_reason`，否则诚实文案（不再甩锅科目/年级）。
  5. `frontend/.../question_gen_fold.dart`：`_emptyHint` 改为中性「检查 AI 出题引擎配置」。
- 验证：后端 `tests/ai/test_question_subagent_stream.py` 5 例通过；`flutter analyze` 两个改动文件 No issues found。
- 旧 note 写的 `GenkitProvider.generate_question_stream` 在新架构已不存在，修复点以本节的 1–4 为准。

## 双 .env 收敛：删除根目录 .env，仅留 backend/.env

- `backend/app/core/config.py` 的 `env_file=("../.env", ".env")`：pydantic-settings 按元组顺序加载、**后者覆盖前者**，
  所以从 `backend/` 启动时效 config = **backend/.env 胜出**（含 `LLM_PROVIDER=deepseek` + key + model）。
- 根目录 `.env` 曾写 `LLM_PROVIDER=mock`，是隐患（backend 一旦丢了 deepseek 行就会静默回退 mock 致出题失效）；且 `DEEPSEEK_API_KEY` 在两份里重复。
- 已：`cp /Users/annilq/Documents/develop/openedu/.env /tmp/openedu_root.env.bak` 备份后删除根目录 `.env`；
  并修正 `config.py` 注释（原「本地开发从仓库根目录的 .env 读取」已不准确，改为「优先 backend/.env，根目录 .env 已弃用」）。
- 验证：删除后 `settings.LLM_PROVIDER=='deepseek'`、key 存在、model 正确。docker-compose.yml 不引用 .env；两文件均 gitignored。
- **项目约定**：LLM/数据库等后端配置统一放 `backend/.env`，不要再建/依赖仓库根目录 `.env`。

## 缺陷：多学科出题静默少题（"数学出了、语文没出"）

- 现象：specs = 数学×1 + 语文×1，落库任务只有 1 道数学题，前端无任何报错。
- 排查（重要方法论）：
  1. 直接查 `app.db`：`task.specs` 有 2 条、`taskquestion` 只有 1 行 → 请求规格是对的，少的是题。
  2. 用 TestClient 跑 `/api/v1/tasks/generate` N 次：后端 11/11 都吐 2 张卡 → **后端生成链路没问题**。
  3. 注意「跑着的后端 ≠ 本地 `.env` 配置」：8000 端口那个进程用的是**另一套库**（自定义 ModelConfig
     `deepseek-v4-pro`），本地 `app.db` 的 `model_config` 是空的。复现要打真实端口，别只信本地库。
- 根因（三处叠加）：
  1. **单题失败被静默吞掉**：出题是逐题串行，某题失败只发一条 `STEP(status="error")`
     （`translate.py` 把 `QuestionFailed` 转的帧）；前端 `QuestionGenFold.apply` 不判 `status`，
     把它当普通进度 STEP 折叠 → 流结束照常落库 → 残缺任务 + 零提示。
  2. `agent.py` 的 `fail_reason` 变量**声明后从未赋值**（重构时丢失），收尾文案还用
     `items[0].subject`，多学科会说出「已生成 2 道数学题」这种错话。
  3. 放大因素：真实模型出 2 题实测 **19–36 秒**，而前端 dio `receiveTimeout` 只有 30 秒。
- 修复：
  - 后端 `agent.py`：接上 `failures` 列表（捕获 STEP status=error）+ `_finish_message()` 三态文案；
    `TOOL_RESULT` 带上 `requested/count/failed/fail_reason`。
  - 前端 `question_gen_fold.dart`：STEP `status=='error'` → 记入 `failures`，不推进 `liveIndex`。
  - 前端 `home_notifier.dart`：`expected`（specs count 之和）→ `TaskGenSuccess.isShort/shortMessage`。
  - 前端 `parent_task_form_view.dart`：少题走 `AppToast.error` + 预览区 `_FailureBanner`。
  - `dio_network_service.dart`：流式端点默认 `receiveTimeout` 10 分钟（SSE 不该受 30 秒短超时约束）。
- 测试：`tests/api/routes/test_tasks_generate.py` 的 4 个用例**此前全崩**（打桩点 `router.build_ai_provider`
  在 ADR-0033 下沉到 service 后已不存在）——这条回归防线长期失效，正是本 bug 漏出的原因。已改打
  `service.build_ai_provider` 并新增部分失败用例；`FakeLLMProvider` 加 `fail_at` 支持。
- 验证：后端 259 passed（剩余 4 failed + 1 error 均为 review/会话隔离的**既有**问题，与本次无关）。
