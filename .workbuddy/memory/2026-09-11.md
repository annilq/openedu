# 2026-09-11

- Ran `setup-matt-pocock-skills`: scaffolded per-repo config for mattpocock engineering skills.

- 架构评审（research skill 手动执行，因 skill 被 disable）：对照 gist「PI/OpenClaw agent stack」评审 backend agent_core + AI 集成。结论=整体合理、分层正确、9 条 layering 不变量由 `tests/ai/test_layering_invariants.py` 以 AST 守住。落点偏差：(1) 无 context compaction（长会话爆窗风险，grep compact 零命中）；(2) 无后台 extension/hook seam（仅 manifest SOP 注入 system prompt）；(3) ADR 引用悬空——docstring 引 ADR-0033/0021/0003 等但无正文可定位（项目用单 context 懒创建约定，故 docs/adr/ 空本身不算缺陷，但被引决策应有唯一事实源）。报告写入 `docs/agent-core-architecture-review.md`。
- 可行性分析：能否将 pi-ai/pi-agent-core 引入本后端作 agent 内核。结论=不能。硬约束：pi-* 是 TS/npm（Node20+），backend 是 Python/FastAPI/SqlModel（genkit-fastapi），全仓库无 package.json/tsconfig。当前 `agent_core` 已是 pi-* 设计原则的忠实 Python 移植（端口/loop/事件信封齐全且被测试守住）。引入只三条路径：A 用 TS 重写（与 Python 后端无法 import）；B 另起 TS 微服务（双运行时+跨语言事件序列化+业务胶水重写，代价高）；C 不引入（推荐）。报告写入 `docs/agent-core-pi-feasibility.md`。
  - Created `AGENTS.md` (root) with `## Agent skills` block.
  - Created `docs/agents/{issue-tracker.md, triage-labels.md, domain.md}`.
  - Decisions: GitHub issue tracker (`gh` CLI, remote annilq/openedu), default 5 triage labels, single-context domain docs (root `CONTEXT.md` + `docs/adr/` created lazily). `triage` skill is installed.
  - xin 已删除 `frontend/CONTEXT.md`；现在仓库无 `CONTEXT.md`、无 `docs/adr/`，符合单 context 懒创建约定，配置无需改动。

- domain-modeling：用 `/domain-modeling` 对照 frontend(Dart)/backend(Python) 真实代码校验词表，发现首版词表 2 处错误 + 5 处缺词，已据代码事实重写 `CONTEXT.md`。
  - 关键代码事实：错题=`WrongQuestion`(progress.py)，题目=`Question`(question.py)且独立；无 Correction 实体，作答=`AnswerRecord`(source∈practice|review)；任务=`Task`(draft→ready→assigned→done)；任务题=`TaskQuestion`；间隔重复=`review_scheduler.py`(阶段0..4, 间隔1/2/4/7/15天, 毕业移除)；掌握度=`KnowledgeMasteryModel`；User.role∈{parent,child}，学习者即 child 账户（无独立 Student 实体）。
  - 已剔除错误概念：订正(独立实体)→改为场景；学生≠儿童账户→合并；通过→改为毕业(Graduation)。
  - 新增实词：任务/任务题/题库/知识点/掌握度/打卡/复习项。
  - 代码 docstring 引用了 ADR-0004/0017 等。xin 已回「落」。

- 已落 `docs/adr/` 五份 ADR（本仓库 0001+ 重排，正文保留原编号溯源，全部以真实代码 docstring 引用为据）：
  - 0001=Question Bank 模型(原0004) / 0002=Parent-Child双模式(原0014) / 0003=Seam SubAgent契约(原0021) / 0004=边界与设计系统(原0003) / 0005=遗忘曲线调度与出题推理(原0017)。
  - 0005 顺带锚定了你之前排查的「生成中推理闪现」缺陷：ADR-0017 要求推理在生成期持续可见、题卡落定后持久显示（已在 parent_task_form_view.dart:757 落地）。

- improve-codebase-architecture 跑了一轮（产出 8 候选 HTML 报告，已 open + present）。用户选 #1「修生成任务流式推理闪现」并 grill 确认：删 `ReasoningTypewriterWidget`、内联直接 `Text(s.liveReasoning)`、`▌` 光标随 `streaming`、空窗保留占位文案、抽 public `PreviewGenerating` + 补回归测试。
  - 已落地：`lib/shared/widgets/reasoning_typewriter.dart` 删除；新建 `lib/features/home/presentation/widgets/parent/preview_generating.dart`（`PreviewGenerating`，纯映射）；`parent_task_form_view.dart` 改用 public widget 并删私有 `_PreviewGenerating`；新建 `test/preview_generating_test.dart`（3 例，断言推理同步显示抓闪现回归）。
  - 验收：3 文件 `flutter analyze` 全无 issue。但 `flutter test` 在本沙箱跑不起来（flutter_tester 无法启动，WebSocketException）——执行需真机/CI。真机判据：生成任务时推理逐帧连续显示，不再约5秒后闪现全文。

- improve-codebase-architecture 报告的 #2「assistant 持久化收进 service」也 grill 确认并落地：整段编排（预路由/配额/会话upsert/事件流折叠持久化/TutorLog）从 `router.py` 搬入新建 `service.py`，`AssistantChatReq` 契约搬到新建 `schemas.py`，router 退化为 3 行薄壳。`runtime` 以可选参数注入（默认 `_get_runtime()` 单例）便于单测。
  - 新建 `backend/app/features/assistant/service.py`(编排) + `schemas.py`(请求契约)；`router.py` 从 337 行降到 ~22 行薄壳；`conftest.py` 的 `build_provider` 打桩点由 `assistant.router` 改指 `assistant.service`（否则旧 HTTP 测试 `test_assistant.py` 会变红）。
  - 新增存活测试 `tests/features/assistant/test_service.py`（2 例，同步函数+asyncio.run 驱动，注入桩 AgentRuntime、绕过 HTTP 与真 LLM）：(1) happy-path 折叠为 5 步 Message 轨迹(input/routing/tool_call/tool_result/output)+会话 done；(2) 娃娃伴学落 TutorLog(question/blocked)。pytest 跑通 2 passed，ruff 全绿。
  - 验收判据：pytest 2 passed + ruff 全绿。`test_assistant.py`(HTTP 级) 在本沙箱收集阶段被内容保护 broker 拦截(PermissionError: Sensitive content approval timed out)——环境限制，非代码问题；其在 CI/真机需复跑确认 conftest 改点后仍绿。
  - 删除测试验证：把编排移回 router 会让这两条 service 测试无法成立（深度保证书）。

- improve-codebase-architecture 报告连带的 `tasks` 同构泄漏也 grill 确认并落地（诚实修正：原报告说 tasks 的 `event_stream` 与 assistant「逐字同构」，实测 tasks 的是纯透传、无折叠/落库，真正逐字复制的是 `_get_runtime()` 单例）。
  - 新建 `backend/agent_core/runtime_singleton.py` 暴露 `get_runtime()`（AgentRuntime.discover() 仅一次）；`assistant/service.py` 与 `tasks` 都改用它、删各自 `_RUNTIME`/`_get_runtime` 副本（消除复制，删测量=复杂度集中到单例模块，router/service 变薄无 agent_core 细节泄漏）。
  - `tasks generate_task` 收进 `tasks_service.generate_task_stream(req, parent, session, runtime=None)`（返回 AsyncIterator[str]，对齐 assistant 的 `chat`），router 退化为「空 specs 校验 → StreamingResponse(...)」薄壳；subagent context 构造（业务语义）从 HTTP 层收进 service，符合 ADR-0033「router 只剩鉴权→调 service→翻译响应」不变量。
  - 新增存活测试 `tests/features/tasks/test_service.py`（1 例，桩 AgentRuntime 注入、monkeypatch 掉 build_ai_provider/build_retriever、绕过 HTTP 与真 LLM）：断言 SSE 帧逐条透传、run 收到 business="question"、ctx.extra["subject"]==规格学科。
  - 验收：ruff 5 文件全绿；pytest `tests/features/assistant/test_service.py` 2 passed（#2 未破坏）+ `tests/features/tasks/test_service.py` 1 passed。混合跑时 broker 间歇性拦截 `.env` 读取（PermissionError: Sensitive content approval timed out）——环境限制，单独跑两文件均通过。

- improve-codebase-architecture 报告 #3「review 写路径收进 service」+ #4「错题转移收口」合并 grill 并落地（用户选合并 A）：
  - 新建 `app/domain/review_scheduler.apply_review_outcome(*, session, wq, correct)`：遗忘曲线状态机单一事实源（答对推进/末位毕业删除/答错重置）；接收已加载实体，内部 commit+refresh。
  - `review/service.py` 加 `ReviewNotFound`/`ReviewNotDue` 纯领域异常（不碰 AppErrorException，因其继承 HTTPException）+ `submit_answer`（整段作答编排收口：归属校验 404 / due 守卫 409 提为领域异常、批改、落 answer record、调 apply_review_outcome）。
  - `review/router.py` 退化为薄壳：try/except 捕获领域异常→翻译 404/409。
  - `review/repository.py` 删 `mark_review_result`（repository 不再持状态机）；`tasks/repository.py` 的 `upsert_wrong_question` existing 分支改调 `apply_review_outcome`（消除与 mark_review_result 的逐字重复）。
  - 新增 `tests/features/review/test_scheduler.py`（3 例：答对推进/末位毕业/答错重置，纯 domain 最强护栏）+ `tests/features/review/test_service.py`（3 例：due 守卫 ReviewNotDue / 归属 404 ReviewNotFound / happy-path 批改+推进+落 AnswerRecord）。
  - 验收：ruff 7 文件全绿（修掉 review_scheduler 漏 import UTC + import 排序）。pytest 本沙箱**未跑通**——broker 冷却期拦截 `.env`（纯收集阶段，连纯 domain 的 test_scheduler 也被拦），与 #2 同款环境限制；代码与已通过的 assistant/tasks service 测试同构，CI/真机需复跑。
  - 删除测试：删 router 编排→复杂度集中 service（good）；状态机从两份降到一份（无平移）。

- improve-codebase-architecture 报告 #5「共享 SSE 分帧解析」grill 并落地（用户指示后续按推荐直接执行、不再逐点问）：
  - `assistant_api_client.dart` 抽私有 `_streamSse(path, body) async* Stream<AssistantEvent>`，承载原 `streamChat`(45-66) 与 `streamGenerate`(116-134) 逐字复制的分帧循环；两端点退化为 `yield* _streamSse(...)` 单行委托。`_decode`/`_parseFrame` 已是共享私有，不动。
  - 诚实修正：原报告称「逐字复制」属实，但 `streamGenerate` 本就是 `extension on AssistantApiClient`，重复的是分帧循环而非整类；全前端仅此一处手写 SSE 分帧（grep 确认无第三处）。
  - 新增 `test/assistant_api_client_test.dart`（4 例，纯 data 层、FakeSseNetwork 桩 NetworkService）：(1) 同块多帧+跨块切分还原顺序；(2) 末帧无尾随 `\n\n` 仍解析；(3) streamGenerate 走同一逻辑 delegation（非复制）；(4) 非法 data 帧跳过、合法帧不受影响。
  - 验收：`flutter analyze` 两文件全绿。pytest/flutter test 本沙箱**未跑通**——flutter_tester 无法启动(WebSocketException) + 内容保护 broker 间歇拦截，环境限制；CI/真机需复跑。删测量：streamGenerate 内 19 行循环删除，分帧逻辑单一事实源成立（grep 确认全 lib 仅 `_streamSse` 一处 `while(idx!=-1)`）。
  - 至此报告 8 候选落地：#1 #2 #3 #4（含 tasks 同构泄漏收口）#5 全完成；剩 #6-#8（复制泄漏清理批次：泛型 StreamingFoldConsumer / Question↔TaskQuestion 映射 adapter / assign_focus_interest 收口）强度低于前五个，属清理类，待用户决定。

- improve-codebase-architecture 报告 #6/#7/#8 收尾核查（继续按推荐直接执行）：
  - **#6 不做（诚实结论）**：原报告称「泛型 StreamingFoldConsumer 收口 3 个 notifier 流消费粘合」。实测三处（assistant_notifier 70-90 / tutor_notifier 80-97 / home_notifier 62-92）真正逐字重复仅 2 行骨架（`var fold=X(); await for(ev){fold=fold.apply(ev);...}`），但每帧 state 更新语义异构（_render 占位气泡 / 重建 TutorLoaded 列表 / TaskGenPreview 多字段+hasError 早退+落库），error/early-return 控制流各异。抽泛型 helper 只能省 2 行循环却要把控制流拆成 4 回调闭包——**更浅更漏**，违背深 module 精神，删除测试反向（跨 seam 传控制流）。故 #6 不做。
  - **#7 部分做（范围收窄）**：原报告称 `Question↔TaskQuestion` 11 字段拷贝 ×3。实测仅 `tasks/repository.py` 的 `create_task_from_bank`(原325) 与 `add_bank_questions_to_task`(原370) 是**逐字相同的 Question ORM→TaskQuestion** 拷贝（真重复）；`service.py` 两处（310 生成器对象 / 420 dict，需 str()/int() 转换）来源异构，硬合会错配，保持原样。已抽 `question_to_task_question(*, q, task_id)` adapter 收口 repository 两处（单一事实源），ruff 全绿，grep 确认 repository 仅剩 adapter 内一处 `TaskQuestion(`。
  - **#8 做**：`assign_focus_interest` 轮询分配确为重复——`_generate_task_questions_for_specs`(原278/297) 与 `regenerate_all`(原528/535) 都做 `focus_interests[i % len]` 且都 guard 空。已抽 `_round_robin_focus(focus_interests, i)` helper 收口两处；顺带删掉 `batch_generate` 路径里仅用于该行的 `n_focus` 局部变量（避免 F841）。ruff 全绿，grep 确认无残留 `n_focus`/内联轮询。
  - **本轮 architecture 评审全部候选处理完毕**：#1-#5 落地，#6 经核查为过度抽象已弃，#7/#8 复制泄漏已收口（#7 收窄到真重复）。所有收口均 ruff 全绿；测试因沙箱限制（flutter_tester 启动失败 + 内容保护 broker 间歇拦截 .env）未能本会话执行，CI/真机需复跑确认。

- 删除「每日配额」功能（前后端全链路，xin 明确说不需要）：后端删 `domain/quota.py`（`SUBJECTS` 学科枚举迁出到新建 `domain/subjects.py` 保留，非配额）、`TutorQuota`/`TutorUsage` 两表、`tutor` 的 `/quota`+`/usage` 端点/函数/schema、`assistant/service.py` 的预路由配额拦截块与 `_child_quota_decision`、`config.TUTOR_DAILY_LIMIT`、`core/errors` 两配额码(TUTOR_QUOTA_EXCEEDED/TUTOR_SUBJECT_FORBIDDEN)、conftest+test_assistant 配额用例；前端删 `tutor_quota_screen`/`tutor_quota_form`/`tutor_usage_card` 三文件 + `test/tutor_notifier_test.dart`、`home_screen` 的 AI 管控导航条目与 `_buildAiControl`/`_aiControlEmptyState`、`tutor_notifier` 的 `tutorQuotaNotifierProvider`/`tutorUsageNotifierProvider`、`selected_child_provider` 的额度加载行、`models.dart` 的 `TutorQuotaModel`/`TutorQuotaUpdateReq`/`TutorUsageModel`。**保留 TutorLog**（家长可见答疑日志，F-305，非配额）。验收：后端 ruff 绿 + import 冒烟 + assistant 路由测试通过（上一轮）；前端 `flutter analyze` 零 issue（本轮）。改动均**未 commit**。
