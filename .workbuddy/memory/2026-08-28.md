# 2026-08-28

## wayfinder · 娃娃兴趣画像 + 个性化出题
- 用 /wayfinder 规划「添加娃娃兴趣画像（受控多选+自由补充）→ AI 出题按兴趣个性化」扩展。
- 产出：`wayfinder/MAP.md`（地图索引）+ `wayfinder/分析_领域与业务关系.md`（domain/业务关系基线）+ `wayfinder/tickets/01..07-*.md` 七张决策票。
- 用户拍板：①画像中心 ②受控多选+自由补充 ③默认轻融入+兴趣题模式 ④User 表加 JSON 列 interests。
- 已收口（research 票）：WF-2 数据建模（User.interests: list[str]|None，create_all 加列+旧 child 回填 []）、WF-3 出题管线注入（generate_question 加 interests/focus_interest，langchain/mock/tasks 三处改动点+双模式 prompt 草案）。
- 开放前沿（HITL，每会话收口一票）：WF-1 兴趣分类词表、WF-4 兴趣题模式交互、WF-5 前端采集UI(含补编辑入口)、WF-6 安全护栏措辞、WF-7 答疑是否个性化。
- **WF-1 已收口（HITL 拍板）**：两级分类——15 个一级（动物/自然/太空/科学/运动/艺术/音乐/故事阅读/动画影视/历史文化/美食/机械交通/编程科技/角色扮演/玩具游戏）+ 各自可选二级；「其他爱好」自由文本 50 字内经安全层后参与出题。取值约束已写回 WF-2/WF-3/WF-5。WF-4、WF-5 由此解锁。
- 下一步前沿：WF-4（兴趣题模式交互）或 WF-5（前端采集 UI），二者均已解锁。
- **WF-4 已收口（HITL 拍板）**：入口=家长「布置练习任务」表单（复用草稿→审阅→派发）；同表单叠加「按兴趣出题」开关（关=自动轻融入画像，开=WF-1 词表点选）；支持多选主题、题量轮询均分；审阅页每题加「兴趣：XX」标签。**两处修正写回**：WF-3 的 `focus_interest` 由 `str|None` 改为 `list[str]|None`（多主题均分）；WF-2 扩展 `Task.focus_interest: list[str]|None`（任务级）。
- 下一步前沿：WF-5（前端兴趣采集 UI，已解锁）、WF-6（安全措辞）、WF-7（答疑个性化）。
- **WF-5 已收口（HITL 拍板）**：创建即采兴趣 + 允许编辑，且**新增与编辑共用同一页面** `ChildFormScreen`（mode create/edit，edit 回填且锁 username/password）；`InterestPicker` 组件（WF-1 一级 chips + 可展开二级多选，categories 收叶子 key）+ 单 50 字「其他爱好」free_text；`UserModel.interests: InterestsModel`。**两处依赖/修正**：①后端 `children.py` 仅 POST/GET，**需新增 `PUT /children/{id}`** 支撑编辑保存；②WF-2 已修正 `User.interests` 形状为 `{"categories": list[str], "free_text": str|null}`（叶子 key+单自由文本）。
- 收口进度：WF-1/2/3/4/5 全收口；剩余前沿 WF-6（安全护栏措辞）、WF-7（答疑个性化）。
- 下一会话建议：开 WF-6（兴趣注入安全护栏措辞）或 WF-7（答疑是否个性化）；或转实现阶段（WF-2 schema+PUT 端点+WF-3/WF-5 UI 落地）。
- 关键接缝：langchain_provider.generate_question prompt、UserCreate/User schema、AddChildScreen→POST /children、init_db 无 Alembic。兴趣是调味剂非 curriculum 锚点(knowledge_point)，绝不绕过 _SYSTEM 年龄锁/学科白名单。

## 实现阶段 · WF-1~WF-5 落地并验证（同日）
- 用户指令「实现」，按已收口的 WF-1~WF-5 全量落地（后端 6 文件 + 前端 10 文件）；WF-6（安全护栏措辞）/ WF-7（答疑个性化）仍为开放前沿，未纳入本次实现。
- 后端：models（UserBase.interests / UserUpdate / TaskBase.focus_interest / TaskBatchCreate.focus_interest）、crud（batch_generate_task 收 focus_interest + 新增 update_user）、children.py 新增 `PUT /children/{id}`（补 WF-5 依赖缺口）、tasks.py（`_extract_interests_pool` + `_generate_task_questions_for_specs` 按题轮询 `focus_interests[i % n]`，聚焦态不叠加轻融入）、domain 三处（provider 签名 + mock 打标 + langchain 双模式 prompt）、db.py 幂等加两列。
- 前端：models.dart（InterestsModel / UserModel.interests / TaskModel.focusInterest；TaskSpecModel 不加兴趣字段——兴趣是任务级非规格级）、数据层 createChild(interests) + updateChild 三层贯通、新增 InterestPicker + ChildFormScreen（create/edit 共用，edit 锁账号密码）、home_screen 与侧栏铅笔接编辑入口、parent_task_form_view「按兴趣出题」开关 + 主题芯片、删除 add_child_screen.dart。
- 验证：后端 `uv run pytest` 104 passed / 1 skipped；前端 `flutter analyze` **No issues found**、`flutter test` **31/31 passed**。
- 功能冒烟：兴趣池 = categories + free_text（无兴趣→None）；聚焦 2 主题×4 题轮询「恐龙/太空/恐龙/太空」，3 主题「恐龙/太空/足球/恐龙」；关开关回落「（兴趣池：…）」。
- 实现详情与踩坑已写回 `wayfinder/MAP.md` 新增的 Implementation 节，票清单 WF-1~WF-5 标记为「已收口 + 已实现」。
