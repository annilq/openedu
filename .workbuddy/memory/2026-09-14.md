# 2026-09-14

## 架构/规范文档化（AGENTS.md 渐进披露）

- 新建 `docs/agents/architecture.md`：基于实际代码核对前后端架构（agent_core 内核 + app 集成层同发 wheel、SSE 单一入口 /api/v1/assistant/chat、分层不变量 test_layering_invariants.py、前端 design token 单一事实源）。
- 新建 `docs/agents/development.md`：lint（ruff/flutter analyze）/ test（pytest/flutter test）/ ADR 规范 / 跨层硬规则（guard、service 不反向依赖）/ 命令速查（uv、flutter、docker、gh）。
- 重写 `AGENTS.md` 为渐进披露索引：顶层结论 + 速览 + 命令表 + 链接到上述两文档及原有 issue-tracker/triage-labels/domain 子文档。
- 发现：README 架构图已落后于代码（旧 /ai/tutor/ask、/ai/tasks/generate 已废弃，收敛到 assistant SSE 入口）；代码 docstring 引用 ADR 0003/0021/0031/0032/0033 等多篇但 docs/adr/ 仅 0001–0005 落地（引用悬空，P0 风险）。

## README 架构段对齐到当前代码

- 架构段：改为 agent_core（框架无关内核，零 app/三方依赖）+ app（FastAPI 集成层）同发 wheel；标注 AI 单一 SSE 入口 /api/v1/assistant/chat 与分层不变量 test_layering_invariants.py。
- 目录树：backend 补 agent_core/、features/*、ai/subagents/、scripts/；frontend 补 main/(AdaptiveShell)、services、shared/theme、dev/theme_preview、analysis_options。
- API 表：纠正 `POST /api/v1/tasks`→`/tasks/generate`；删除不存在的 `/tutor/ask`、`/tutor/quota`，改标 assistant/chat 为 AI 统一入口（tutor router 仅 GET /logs）。
- 新增「开发流程（命令速查）」段：后端 uv/ruff/pytest、前端 flutter analyze/test、docker、CI 说明。
- 修断链：路线图原 `docs/decisions/娃娃学习App_ADR.md`（不存在）改为 `docs/adr/` + `compliance.yml`；技术栈与闭环步骤同步修正。
