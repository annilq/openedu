# 娃娃学习 App · 架构 / 开发规范 / 业务功能分析

> 分析日期：2026-08-28
> 范围：基于仓库内 `backend/`、`frontend/` 代码与 13 条 ADR、技术架构文档（前端/后端）、PRD 全三期、产品方案、术语表、T08–T13 任务文档、前端 `CODE_CONVENTIONS` / `CONTEXT` / `.impeccable` / `docs/adr/*` 等。
> 结论：文档与代码高度一致，已落地至「一期 MVP + 二三期主体能力」，是一套有清晰架构纪律的家庭私用学习闭环工具。

---

## 0. 一句话定位

平板优先的 Flutter 原生 App + Python（FastAPI/LangChain）后端 + PostgreSQL，面向「家长当班主任、AI 辅助出题与讲解」的家庭学习闭环。架构基线为 `fastapi/full-stack-fastapi-template`（二次裁剪），前端为 Clean Architecture + Riverpod。

---

## 1. 技术架构

### 1.1 整体分层

```
Flutter 平板 App（Clean Arch + Riverpod）
        │ HTTPS / Bearer JWT
        ▼
FastAPI 应用（uvicorn，同步 SQLModel）
  ├─ API 层      api/routes/ + api/deps（DI/鉴权）
  ├─ 领域层      domain/（业务语义接口 + Provider 抽象 + 纯函数）
  └─ 数据层      models.py(SQLModel) + core/db + crud
        │
        ▼
PostgreSQL（家庭/生产）· 本地可回退 SQLite
```

关键隔离：**业务（api/domain）从不直接 import langchain**；厂商适配只在 `LangChainProvider` 内；换模型 = 改配置，不碰业务代码（ADR-003 / ADR-004）。

### 1.2 后端架构（已实现，与文档一致）

- **工程基线**：`fastapi/full-stack-fastapi-template` 二次开发（ADR-013）。裁剪掉 React 前端、邮件体系、Traefik、Superuser、Alembic（一期用 `create_all`）。
- **技术栈**：FastAPI `>=0.141`、SQLModel、pydantic-settings、pyjwt、pwdlib(Argon2+bcrypt)、psycopg(binary)、langchain-openai（仅 OpenAI 兼容端点覆盖国产模型）、Python `>=3.14`、uvicorn、pytest + ruff。
- **领域层（核心）**：
  - `LLMProvider` 抽象（`provider.py`）+ `build_provider()` 工厂（mock/langchain 延迟导入，mock 模式零 langchain 依赖）。
  - 业务服务以「抽象 + 工厂」接入：`QuestionGenerator`、`Grader`、`TutorService`(F-302)、`review_scheduler`(遗忘曲线纯函数)、`mastery`(掌握度纯函数)、`safety`(双层内容安全)、`quota`(AI 管控纯函数)、`retriever`(知识库检索，T11)。
  - 所有 provider 方法为 `async`，业务层用 `asyncio.run` 同步封装，由同步路由在独立线程调用，无事件循环冲突。
- **API 契约**：统一前缀 `/api/v1`；路由 `auth/children/tasks/review/mastery/tutor/health`。鉴权 `require_parent` / `require_child` 角色守卫；归属校验越权返回 403/400。

### 1.3 前端架构（已落地分层，与架构文档一致）

- **范式**：Clean Architecture + Riverpod（编译期安全 DI）；`StateNotifierProvider` + sealed 状态机 `Idle→Loading→Success/Error`（tutor 为 `Idle→Loading→Loaded`，错误保留气泡）。
- **目录**：`configs/`（kApiBase 按环境切局域网 IP）、`main/`（ProviderScope）、`shared/`（跨 feature 复用的 data/domain/widgets/theme/exceptions）、`services/auth_session/`（全局登录态）、`features/<name>/{data,domain,presentation}`。
- **网络层**：Dio + 双拦截器（Token 注入 Authorization、错误统一转 `HttpException`）；`NetworkService` 抽象。
- **依赖方向**：UI → domain Provider → Repository → DataSource → 后端 REST；外层依赖内层，内层不反向依赖。

---

## 2. 开发规范

### 2.1 后端规范

| 项 | 约定 |
|---|---|
| 代码质量 | `ruff`（select E/W/F/I，ignore E501），`pyproject.toml` 统一管理；`target-version=py314` |
| 配置 | 全部走 `pydantic-settings` + 环境变量 / `.env`，密钥不入库 |
| 数据库 | SQLModel「模型即 schema」——`UserBase→UserCreate/User/UserPublic` 派生，不另建 schemas 文件 |
| 主键 | UUID（`uuid.uuid4`），天然分布式友好 |
| 测试 | pytest，conftest 用独立临时 DB 覆盖；`smoke` marker 跑真实大模型（需 key + `RUN_LLM_SMOKE=1`） |
| 部署 | Dockerfile（python:3.14-slim + `fastapi run`）+ docker-compose（postgres:16-alpine + backend）；密钥全走 env |
| 变更纪律 | 架构变更先改文档与 ADR，再改代码（正常开发流程） |

### 2.2 前端规范（强约束，已执行多轮整改）

- **单文件 ≤ 300 行**（含 import/空行）：屏幕骨架尽量 < 200 行；子组件同样适用。超即拆。自查命令：
  `find lib \( -name "*_screen.dart" -o -name "*_view.dart" -o -path "*/widgets/*.dart" \) -exec wc -l {} + | sort -rn`
- **拆分原则**：按「视觉区块」拆而非机械切行；跨特性复用 → `shared/widgets/`（`App*` 前缀语义组件）；特性内私有 → `features/<f>/presentation/widgets/`；状态与视图分离（dumb/reusable 子组件只收参 + 回调）。
- **Lint**：继承 `package:flutter_lints/flutter.yaml`（`analysis_options.yaml`），`flutter analyze` 零回归是每阶段门禁。
- **ADR 机制**：`frontend/docs/adr/` 记录代码组织/设计决策（0001 抽共享做题组件、0002 中性灰白主题+桌面壳、0003 Linear 化重设计、0004 Task 多学科+题库快照），Status 有 Accepted/Draft。

### 2.3 设计系统（`.impeccable.md`，家长专业体验优先）

- **定位反转**（ADR-0003）：主要用户是家长（专业工作台），不再为低龄维护特殊约束；删除暖绿主题，仅一套「中性 + 靛蓝」令牌。
- **字号**：正文 15sp 基线密排阶梯（Inter 西文/数字 + HarmonyOS Sans SC CJK 回退）；tabularFigures。
- **色板**：surface 微暖白 `#FBFAFA` / 卡片 `#FFFFFF` / 1px 描边 `#ECECEA`；**无阴影**，靠描边分层；单一 accent 靛蓝 `#5E6AD2`（暗 `#7B82EA`）；CTA 近黑底白字；四档降饱和语义色（positive/warning/error/info 极淡底）。
- **表面五级**：surface / surfaceRaised / surfaceHover / surfaceSunken / surfaceActive；禁止 `withValues(alpha:)` 透明度变体。
- **转场分级**：120ms 交互 / 200ms 状态 / 300ms 页面；crossfade；hover/press/tooltip(500ms)/Cmd+K/侧栏首字母快捷键；无半透明 focus ring（用 bg 变化表达）。
- **令牌纪律**：所有间距/圆角/字号走 `AppSpacing/AppRadius/AppText`，禁止 `Colors.*` 硬编码。

### 2.4 质量保障现状

- 后端：pytest 已落地（`test_loop` 闭环、domain 纯函数单测、safety/tutor/retriever/mastery/quota/review_scheduler 均有覆盖）；ruff 全过。报告中称全量 101 passed + 1 skipped（含 T11）。
- 前端：`flutter analyze` 0 问题；`flutter test` 覆盖 notifier 状态机（mocktail），如 tutor 13 passed、children 31 passed。
- 代码审查修复机制：多个任务（T08/T11/T13）由 general-purpose agent 做安全/时区/防重入修复，并补测试。

---

## 3. 业务功能

### 3.1 角色模型

- **家长（parent）**：应用管理员/「班主任」，注册即管理员；负责出题、查进度、管控 AI 额度与内容范围、查 AI 日志。
- **娃娃（child）**：独立账号，绑 `parent_id`；进度/题目/作答按娃隔离（账号隔离）。当前二年级、四年级两娃。

### 3.2 一期 MVP（F-101~F-107，刷题 + 打卡）

| 功能 | 角色 | 说明 |
|---|---|---|
| F-101 注册/登录 | parent | JWT 鉴权 |
| F-102 娃娃账号管理 | parent | 增/列娃娃、绑 parent_id、设年级（T13 补齐 UI 入口） |
| F-103 AI 出题并建任务 | parent | 学科/年级/知识点/题型/难度/题量（已演进为多学科批量 + 草稿审阅，ADR-0004） |
| F-104 娃娃查看今日任务 | child | 拉任务+题目（**不含答案**，防作弊） |
| F-105 作答与即时批改 | child | 客观题规则判分，开放题交 provider；答错自动归集错题 |
| F-106 任务打卡 | parent/child | 完成标记 done + 写 checkin（streak +1） |
| F-107 进度看板 | parent | 正确率/连续打卡/累计天数 |

### 3.3 二期（F-201~F-204，错题本 + 遗忘曲线 + 掌握度）

- 错题自动归集（`WrongQuestion`，child+question 唯一，重复错累加不建多条）。
- 遗忘曲线复习调度（`review_scheduler` 纯函数：间隔 1/2/4/7/15 天；答对推进阶段、答错重置计时、末位毕业删除）。
- 复习作答（`/review/answer`）复用批改逻辑，更新调度状态。
- 掌握度看板（`/tasks/children/{id}/mastery`，`domain/mastery.py` 纯函数）：基础分（历史 40% + 近 10 次 60% 加权）+ 活跃错题封顶 + 6 级等级（未开始/待加强/薄弱/巩固中/较扎实/已掌握），含 `mastered_count`。

### 3.4 三期（F-301~F-306，AI 伴学 + 知识库 + 安全 + 管控）

- **F-302 AI 自由答疑**：`POST /tutor/ask` → `TutorService.explain()`（输入校验 → 知识库检索注入 → provider 适龄讲解 → 输出校验）。
- **F-304 内容安全（双层 + 日志）**：①系统层年龄锁 prompt ②`check_input`（越狱/非学习类，对 question+knowledge_point+context 统一校验）③`check_output`（敏感词）；命中返回 `SAFE_REFUSAL`，记 `tutor_log.blocked`，**不向娃娃暴露拒绝原因**。
- **F-305 AI 交互日志**：`GET /tutor/logs`（家长可查，越权 403）。
- **F-306 AI 使用管控**（`domain/quota.py` + `/tutor/quota`、`/tutor/usage`）：按娃配 每日提问上限 / 每日时长上限(分钟) / 学科白名单；`/tutor/ask` 前置强制，**学科越界→403、次数/时长达限→429**；null=不限、0=禁用；配置校验非法→422。
- **F-301/AC-305 知识库检索（T11）**：`KnowledgeRetriever` 接口 + `build_retriever()` 工厂，当前 `MockKnowledgeRetriever` 走内置**自编**知识点库（无版权风险），`RETRIEVER_PROVIDER` 可按 `vector` 演进。🔴 **教材版权（ADR-012）未解决**——上线前硬门槛。

### 3.5 数据模型（ER 要点）

`User`(家长1—*娃娃自关联) → `Task`(draft/ready/assigned/done) → `TaskQuestion`(派发快照) + `Question`(题库层，可跨 Task 复用) → `AnswerRecord` / `WrongQuestion` / `TutorLog` / `TutorQuota`(每娃1) / `TutorUsage`(按日)。

**ADR-0004 关键模型演进**：
- `Task` 删单值学科字段，学科下沉到题；`status` 扩为 `draft→ready→assigned→done`（先审阅编辑、再锁定成卷、再派发）。
- `Question` 本身即题库层（删 `task_id` 独立实体，可跨 Task 复用）。
- 新增 `TaskQuestion` 派发快照层（派发时深拷贝冻结，家长改题库不污染已派发任务）。
- 批量端点 `POST /tasks/batch-generate`（多 `specs` 数组），原单科端点内部转调，保留兼容。

**安全细节**：`Question.answer` 仅家长端 `generate` 返回，娃娃端 `/tasks/today` 恒为 `None`（防作弊）。

---

## 4. 关键设计决策与质量亮点

1. **LLM 可插拔**：业务只依赖 `LLMProvider` 抽象；mock 模式零 key 即可跑通全闭环（注册→加娃→出题→作答→批改→打卡→进度），已本地验证。
2. **内容安全（儿童硬约束）**：双层确定性校验 + 年龄锁系统 prompt + 全程日志，前端无需重复校验。
3. **答案隔离**：娃娃端永远拿不到标准答案，从源头防作弊。
4. **掌握度算法**：基础分 + 活跃错题封顶 + 等级映射，复习答对自然抬升分数，毕业解除封顶，口径清晰可单测。
5. **快照隔离**：`TaskQuestion` 派发冻结，家长编辑草稿/题库不污染在做的任务。
6. **架构纪律**：13 条 ADR + 前端 4 条 ADR + 300 行门禁 + ruff/flutter_lints 门禁，文档与代码同步演进。

---

## 5. 技术债与风险（需关注）

- 🔴 **教材版权（ADR-012）**：当前知识库为自编内容；凡对外分发/上线前必须取得教材授权或换公版/自编。硬门槛。
- **缺 Alembic 迁移**：一期 `init_db()` 直接 `create_all`；ADR-0004 新增 `TaskQuestion`、改 `Question` 字段需数据迁移（旧 `Question` 转独立题库题 + 为每个旧 Task 生成快照行）。生产前需补回迁移。
- **缺限流/监控**：对外服务前需加 rate limit 与基础观测。
- **缺后台任务框架**：二期复习调度真正「到点 push」需 APScheduler/Celery（当前为娃娃端拉 `/review/due`）。
- **作答写多表非单事务**：极端并发下掌握度看板可能读中间态，生产前可合并事务。
- **LangChain + Python 3.14 兼容性**：接真实模型时需验证生态支持度。
- **前端待整改**：`parent_dashboard`(1074 行) 已拆分，`child_home`(312 行) 仍轻微超标，下一轮整改。

---

## 6. 总结

本项目是一套**架构纪律清晰、文档与代码高度一致**的家庭学习闭环产品：

- **架构**：FastAPI + 领域层（抽象+工厂可插拔）+ SQLModel + PostgreSQL 后端；Flutter Clean Architecture + Riverpod 前端；关键隔离是「业务不绑定 LangChain」。
- **规范**：后端 ruff/pytest/模板基线 + env 配置；前端 300 行单文件门禁、flutter_lints、ADR 决策机制、`.impeccable` 令牌化设计系统（家长专业体验优先、中性+靛蓝、无阴影、密排 15sp）。
- **业务**：一期 MVP（出题/批改/打卡/进度）已闭环，二期（错题/遗忘曲线/掌握度）已落地，三期（AI 伴学答疑/内容安全/使用管控/知识库检索）主体能力已落地；核心约束是儿童内容安全双层防护与教材版权硬门槛。

建议下一步优先级：**①补 Alembic 迁移以承接 ADR-0004 模型变更 → ②落实教材版权来源或切换公版/自编内容 → ③加限流与监控 → ④复习调度后台化**。
