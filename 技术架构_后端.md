# 后端技术架构文档

> 适用项目：娃娃学习 App（小学→初中，平板优先）
> 配套文档：`娃娃学习App_ADR.md`（架构决策记录）、`技术架构_Flutter.md`、`功能规格_PRD_全三期.md`
> 本文档描述**当前已实现的一期 MVP** 的真实架构，并显式标注**二三期预留的扩展点**。
> 基线：基于 [`fastapi/full-stack-fastapi-template`](https://github.com/fastapi/full-stack-fastapi-template) 二次开发（ADR-013）。

---

## 1. 文档目的与范围

本文档是后端工程的「蓝图 + 现状」：

- **蓝图**：分层、模块边界、关键抽象、数据模型、API 契约、部署形态。
- **现状**：当前已落地代码（FastAPI + SQLModel + 领域层 + PostgreSQL/SQLite）与文档一致。
- **扩展点**：二三期（错题本 / 知识库 RAG / AI 答疑 / 复习调度）在架构中预留的接口位与目录位。

读者：后端开发、前端（Flutter）开发、技术负责人、你（产品/家长兼 owner）。

---

## 2. 总体架构视图

```
┌────────────────────────────────────────────────────────────┐
│  Flutter 平板 App（Clean Arch + Riverpod）                  │
│  调用 REST JSON API，Bearer JWT 鉴权                        │
└───────────────────────────┬────────────────────────────────┘
                            │  HTTPS
                            ▼
┌────────────────────────────────────────────────────────────┐
│  FastAPI 应用（uvicorn 运行，同步 SQLModel）                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ API 层       │  │ 领域层        │  │ 数据层           │  │
│  │ api/routes/  │→ │ domain/       │→ │ models.py(SQLModel)│  │
│  │ api/deps.py  │  │  Provider 抽象│  │ core/db.py(engine) │  │
│  │ core/config  │  │  QuestionGen  │  │ crud.py            │  │
│  │ core/security│  │  Grader       │  │                    │  │
│  └──────────────┘  └──────┬───────┘  └────────┬─────────┘  │
│                          │                   │            │
│              build_provider() 选择实现        │            │
│              ┌───────────┴──────────┐        │            │
│              │ MockProvider         │        │            │
│              │ LangChainProvider ───┼─ 调大模型│           │
│              └──────────────────────┘        │            │
└──────────────────────────────────────────────┼────────────┘
                                               ▼
                                  ┌──────────────────────────┐
                                  │ PostgreSQL（家庭/生产）    │
                                  │ 本地开发可回退 SQLite      │
                                  └──────────────────────────┘

        ★ 关键隔离：业务(api/domain) 从不直接 import langchain
          厂商适配只在 LangChainProvider 内；换模型=换配置，不碰业务代码
```

请求流（以「出题」为例）：
1. 家长端 `POST /api/v1/tasks/generate` → `api/routes/tasks.py`
2. 依赖注入拿到 `session`（`Session`）与 `require_parent`
3. 调 `domain.get_generator()` → `build_provider()` 返回 `MockProvider` 或 `LangChainProvider`
4. `QuestionGenerator.generate()` 委托 provider 产出 `GeneratedQuestion`
5. 写入 `Question`/`Task` 表，返回 `TaskResp`（家长接口含 answer）

---

## 3. 技术栈选型与理由

| 关注点 | 选型 | 理由 |
|---|---|---|
| Web 框架 | **FastAPI** | 异步原生、类型提示驱动、自动 OpenAPI 文档、生态成熟 |
| 工程基线 | **fastapi/full-stack-fastapi-template** | 官方模板，SQLModel+pwdlib+pyjwt+Docker 一体；裁剪掉 React 前端/邮件/Traefik，保留后端骨架（ADR-013） |
| ORM | **SQLModel** | SQLAlchemy + Pydantic 合一，模型即 schema，少一层转换；模板原生约定 |
| 运行模式 | **同步**（`create_engine` + `Session`） | 跟随模板约定；家庭规模并发极低，同步够用且调试简单 |
| 本地回退 DB | **SQLite** | 无 Docker 也能跑通验证，降低本地启动门槛 |
| 生产 DB | **PostgreSQL**（`psycopg[binary]`） | 关系清晰、JSON 字段可用、未来 RAG 向量（pgvector）可同库 |
| 鉴权 | **pyjwt (JWT) + pwdlib（Argon2+bcrypt）** | 模板原生方案；pwdlib 已替代 passlib，规避 passlib/新 bcrypt 冲突坑 |
| 主键 | **UUID**（`uuid.uuid4`） | 模板约定；无自增泄漏、天然分布式友好 |
| Agent 编排 | **LangChain（Python）** | 一期即上 AI 出题；自带 100+ 厂商 provider 抽象，组件可复用 |
| 配置 | **pydantic-settings** | 环境变量 + `.env` 统一，敏感信息不入代码 |
| 容器化 | **Docker + docker-compose** | 本地一键起 PG + backend；架构按可上云设计 |
| 运行 | **uvicorn (ASGI)** | 异步性能足够家庭/小规模使用 |
| Python | **>=3.14** | 跟随模板；2026 年中生态应已跟上 |
| 测试 | **pytest**（模板 conftest 模式） | 模板自带 `TestClient` + 临时 DB 覆盖约定 |

**已验证可跑通**：无模型 key、无 langchain 依赖时，纯 `MockProvider` 模式即可完整跑通闭环（已本地验证：注册→加娃娃→出题→作答→批改→打卡→进度，pytest 2 passed，零报错）。

---

## 4. 目录结构与模块职责

```
backend/
├── app/
│   ├── main.py                # FastAPI 入口：lifespan(init_db)、CORS、挂载 api router、/health
│   ├── models.py              # SQLModel 模型 + 请求/响应 schema（模型即 schema）
│   ├── crud.py                # CRUD 函数（模板约定：路由薄，逻辑在 crud）
│   ├── core/
│   │   ├── config.py          # Settings(pydantic-settings)：DB/安全/LLM 全部可配
│   │   ├── db.py              # engine(create_engine)、init_db()、get_session 依赖
│   │   └── security.py        # pwdlib 哈希(Argon2+bcrypt)、pyjwt 签发/校验
│   ├── api/
│   │   ├── main.py            # API v1 路由聚合
│   │   ├── deps.py            # DI：get_db / get_current_user / require_parent / require_child
│   │   └── routes/
│   │       ├── auth.py        # /auth/register, /auth/login
│   │       ├── children.py    # /children (家长的娃娃管理)
│   │       ├── tasks.py       # /tasks/* 出题/批改/打卡/进度/错题本（核心闭环）
│   │       ├── review.py      # /review/* 遗忘曲线复习调度（T05）
│   │       ├── mastery.py     # /tasks/children/*/mastery 掌握度看板（T06）
│   │       ├── tutor.py       # /tutor/ask 答疑 + /tutor/logs 家长日志 + /tutor/quota,/usage 使用管控（T08/T10）
│   │       └── health.py      # /health
│   └── domain/                # ★ 领域层（架构核心，见 §5）
│       ├── __init__.py        # build_provider() 工厂 + get_generator/get_grader/TutorService
│       ├── provider.py        # LLMProvider 抽象 + GeneratedQuestion 数据类 + tutor()
│       ├── mock_provider.py
│       ├── langchain_provider.py
│       ├── question_generator.py
│       ├── grader.py
│       ├── review_scheduler.py # 遗忘曲线纯函数：1/2/4/7/15 天间隔与阶段推进（T05）
│       ├── mastery.py          # 掌握度纯函数：分数公式 + 活跃错题封顶 + 等级映射（T06）
│       ├── safety.py           # 内容安全层：年龄锁系统提示 + 输入/输出确定性校验（T08，F-304）
│       ├── quota.py            # AI 使用管控纯函数：check_quota 三类拦截 + 配置校验（T10）
│       ├── retriever.py        # 知识库检索：KnowledgeRetriever 抽象 + 内置自编库 + 工厂（T11）
│       └── tutor.py            # AI 伴学答疑编排：输入校验→检索注入→provider→输出校验（T08/T11，F-302）
├── tests/
│   ├── conftest.py            # pytest TestClient + 独立临时库覆盖（避免旧 schema 干扰）
│   ├── utils/user.py          # 测试辅助：注册/登录/获取 token
│   └── api/routes/test_loop.py  # 闭环 + 错误路径契约
├── pyproject.toml             # 依赖与构建（hatchling），替代 requirements.txt
├── Dockerfile                 # Python 3.14-slim + uvicorn
└── .env.example
```

### 4.1 与模板的差异（裁剪清单）

模板默认包含但本项目**不需要**的部分：

| 模板模块 | 处理 | 原因 |
|---|---|---|
| React 前端（`app.frontend()`） | 删除 | 我们用 Flutter |
| 邮件找回密码（Mailpit + email-templates） | 删除 | 家庭自用，不需要 |
| Alembic 迁移 | 删除（一期用 `create_all`） | 快速迭代；生产前需补回 |
| Superuser / first user 机制 | 删除 | 家长注册即管理员 |
| Items 示例路由 | 删除 | 模板示例 |
| Traefik / HTTPS | 删除 | 家用内网，不需公网 HTTPS |
| utils.py（邮件发送） | 删除 | 随邮件删除 |

---

## 5. 领域层详解（核心架构决策落地）

领域层是本文档最关键的隔离设计，对应 `ADR-003`（不在 LangChain 之上重复封装 provider）。

### 5.1 LLMProvider 抽象（`domain/provider.py`）

```python
@dataclass
class GeneratedQuestion:
    subject, grade, knowledge_point, qtype   # choice|fill|calc|open
    stem, options, answer, explanation, difficulty

class LLMProvider(ABC):
    def generate_question(self, *, subject, grade, knowledge_point, qtype, difficulty) -> GeneratedQuestion
    def grade_open(self, *, question, student_answer) -> dict
```

- 业务只依赖这个抽象，**不感知** Mock 还是 LangChain。
- `qtype` 约定：客观题 `choice|fill|calc` 规则判分；`open` 开放题交给 provider。
- 注意：方法是**同步**的（跟随模板同步 SQLModel 约定），LangChainProvider 内部如有 async 调用则用 `asyncio.run()` 包裹。

### 5.2 Provider 工厂与可插拔（`domain/__init__.py`）

```python
def build_provider() -> LLMProvider:
    if settings.LLM_PROVIDER == "langchain":
        from .langchain_provider import LangChainProvider   # 延迟导入
        return LangChainProvider()
    return MockProvider()
```

- **延迟导入**保证：mock 模式无需安装 langchain 也能跑。
- 换厂商 = 改 `LLM_PROVIDER` + `LLM_BASE_URL` + `LLM_MODEL`，业务零改动。
- 未来新增 `OpenAIProvider` / `HunyuanProvider`：实现 `LLMProvider`，在工厂加分支即可。

### 5.3 出题 / 批改流程

- `QuestionGenerator`：薄封装，委托 provider 出 `GeneratedQuestion`，写入 `Question` 表。
- `Grader`：
  - 客观题（choice/fill/calc）：归一化后字符串比对，`correct` + `score`(1.0/0.0)。
  - 开放题（open）：委托 `provider.grade_open()`，返回 `{"correct", "score", "explanation"}`。

### 5.4 ★ 二三期预留扩展点（接口位已规划）

| 未来模块 | 在架构中的位置 | 预留接口（建议签名） |
|---|---|---|
| 知识库检索 | `domain/retriever.py`（T11 已落地） | `KnowledgeRetriever.retrieve(subject, grade, knowledge_point, query) -> list[KnowledgeChunk]`（内置自编库 mock 实现；**不解决教材授权**，见 ADR-012）；`build_retriever()` 工厂按 `RETRIEVER_PROVIDER` 可切向量库 |
| 意图路由 | `domain/intent.py` | `IntentRouter.route(query) -> Intent`（决定走出题/讲解/答疑） |
| 复习调度 | `domain/review_scheduler.py`（T05 已落地） | `next_interval_days / advance_stage` 纯函数 + crud `list_due_wrong_questions`（遗忘曲线 1/2/4/7/15 天） |
| 掌握度看板 | `domain/mastery.py` + `api/routes/mastery.py`（T06 已落地） | 按知识点聚合作答记录 + 活跃错题，`compute_mastery_score / mastery_level` 纯函数；家长 `GET /tasks/children/{id}/mastery` |
| AI 答疑 | `domain/tutor.py` + `api/routes/tutor.py`（T08/T11 已落地） | `TutorService.explain()`：输入校验→**知识库检索注入（T11）**→`provider.tutor()`→输出校验；娃娃 `POST /tutor/ask`（F-302），家长 `GET /tutor/logs`（F-305） |
| 内容安全 | `domain/safety.py`（T08 已落地） | `tutor_system_prompt()` 年龄锁 + `check_input()/check_output()` 确定性校验；命中返回 `SAFE_REFUSAL`（F-304） |
| 使用管控 | `domain/quota.py` + `api/routes/tutor.py`（T10 已落地） | `check_quota()` 纯函数三类拦截（学科越界403 / 次数429 / 时长429，故事 23/26）+ `validate_quota_config()` 值域校验；`/tutor/ask` 前置强制，`/tutor/quota` `/tutor/usage` 家长配置与查询 |

这些模块将以**同样的「抽象 + 工厂」模式**接入，不破坏现有分层。

---

## 6. 数据模型与 ER

```
User ──<parent_id>── User          (家长 1—* 娃娃，自关联，UUID)
User(parent) 1 ──< parent_id >── * Task
User(child)   1 ──< child_id  >── * Task
Task          1 ──< task_id   >── * Question
Question      1 ──< question_id>─ * AnswerRecord
User(child)   1 ──< child_id  >── * AnswerRecord
User(child)   1 ──< child_id  >── * Checkin
Task          1 ──< task_id   >── * Checkin
User(child)   1 ──< child_id  >── * WrongQuestion
Question      1 ──< question_id>─ * WrongQuestion
User(child)   1 ──< child_id  >── * TutorLog
User(child)   1 ──< child_id  >── 1 TutorQuota      (使用管控配置，每娃一条)
User(child)   1 ──< child_id  >── * TutorUsage      (按日一条：当日累计秒数)
```

| 表 | 主键 | 关键字段 | 说明 |
|---|---|---|---|
| user | UUID | username(唯一), hashed_password, role(parent\|child), display_name, grade, parent_id | 家长与娃娃同表，role 区分；娃娃 parent_id 指向家长 |
| task | UUID | parent_id, child_id, title, subject, grade, knowledge_point, qtype, difficulty, count, status(pending\|done) | 一次布置=一个任务，含 N 道题 |
| question | UUID | task_id, subject, grade, knowledge_point, qtype, stem, options(JSON), answer, explanation, difficulty | 客观题 answer 存标准答案；**娃娃端接口不返回 answer** |
| answer_record | UUID | question_id, child_id, student_answer, correct, score, source(practice\|review), created_at | 每次作答一条；source 区分练习/复习，created_at 供掌握度近因窗口 |
| checkin | UUID | child_id, task_id, checkin_date | 打卡记录，用于连续天数 streak |
| wrong_question | UUID | child_id+question_id 唯一, first_wrong_at, wrong_count, review_stage, last_wrong_at, due_at | 错题集：重复错累加不建多条；遗忘曲线调度（答对推进、答错重置、末位毕业删除） |
| tutor_log | UUID | child_id, grade, subject, knowledge_point, question, answer, input_safe, output_safe, blocked, created_at | AI 答疑交互日志（F-305）；家长可查；blocked 标记安全兜底命中线 |
| tutor_quota | UUID | child_id(唯一), daily_ask_limit, daily_minutes_limit, allowed_subjects(JSON) | AI 使用管控配置（T10，故事 23/26）：每娃一条；null=该项不启用（次数回退全局 TUTOR_DAILY_LIMIT），0=今日禁用 |
| tutor_usage | UUID | child_id+usage_date 唯一, used_seconds | 当日 AI 使用累计：只记服务端实测答疑耗时秒数（次数口径沿用 tutor_log 计数） |

**SQLModel 约定**：模型即 schema——`UserBase` 派生 `UserCreate`（请求）和 `User`（表），`UserPublic`（响应），不再单独维护 pydantic schemas 文件。

**答案安全**：`Question.answer` 仅在家长端 `generate` 接口返回（`include_answer=True`），娃娃端 `/tasks/today` 一律 `None`，防作弊。

### 6.1 掌握度口径（T06，F-204 / AC-203）

家长看板「各知识点掌握度」的数据口径（实现见 `domain/mastery.py`）：

- **基础分（0~100）**：该知识点历史作答正确率 40% + 最近 10 次作答正确率 60% 加权。复习与练习记录统一计入（`answer_record.source`），最近窗口取 `created_at` 倒序。
- **活跃错题封顶**：存在未毕业错题（`wrong_question` 中存在记录）时不算掌握，分数封顶 `min(85, 60 + max_review_stage×6)`——复习答对推进阶段 → 封顶提高 → 分数上升（满足 AC-203）；毕业（末位 15 天档答对，错题移除）→ 解除封顶 → 可达到「已掌握」（≥85 且无活跃错题）。
- **等级**：未开始 / 待加强(<50) / 薄弱(50~69) / 巩固中(≥70，有活跃错题) / 较扎实(70~84，无活跃错题) / 已掌握(≥85，无活跃错题)。
- **已掌握计数**：看板汇总 `mastered_count`（等级=已掌握的知识点个数），供家长一眼看到掌握情况。

### 6.2 内容安全设计（T08，F-304 / AC-302）

三层防护（详见 `domain/safety.py` 与 `domain/tutor.py`），确定性、可单测：

1. **系统层年龄锁（输出前必套）**：`tutor_system_prompt(grade, subject)` 在每次模型调用前注入「仅适合 X 年级、纯学习相关」的硬约束；`langchain_provider.tutor()` 实际消费该提示，业务方无法绕过。
2. **输入校验 `check_input()`**：对娃娃可输入字段（`question` + `knowledge_point` + `context` 统一拼接）扫描越狱/指令注入意图与非学习类主题，命中即**不调用模型**，直接返回 `SAFE_REFUSAL` 并标记日志 `blocked=True`。
3. **输出校验 `check_output()`**：对模型返回做敏感词扫描，命中同样返回 `SAFE_REFUSAL` 并标记 `blocked=True`（日志 `output_safe=False`）。

日志 `tutor_log` 记录每次问答的 `input_safe / output_safe / blocked`，家长可在 `/tutor/logs` 监督（F-305）。**不向娃娃暴露任何拒绝原因细节**（AC-302）。

> 当前为确定性规则层；后续可在 `safety.py` 之上叠加更强的模型审查（如语义级越狱检测）而不改调用方。

### 6.3 AI 使用管控设计（T10，故事 23/26 / PRD State-driven EARS）

家长按娃配置（`tutor_quota` 表 + `PUT /tutor/quota`），`/tutor/ask` 前置强制（`domain/quota.check_quota` 纯函数），顺序：**学科范围 → 次数上限 → 时长上限**：

- **每日提问次数**：`daily_ask_limit`（null 回退全局 `TUTOR_DAILY_LIMIT`，0=今日禁用）；次数口径沿用 `tutor_log` 当日计数。
- **每日使用时长**：`daily_minutes_limit` 分钟（null 不限时，0=今日禁用）；服务端按每次答疑实测耗时（`time.perf_counter`，向上取整秒）累计入 `tutor_usage`（child+日期唯一）。
- **内容范围**：`allowed_subjects` 学科白名单（null 不限）；越界提问 403 并提示允许的学科。

拦截语义：越界（内容范围）→ **403**；超额（次数/时长）→ **429**，均携带面向娃娃的友好文案。配置值域由 `validate_quota_config` 校验（上限非负、学科 ∈ {数学,语文,英语}、白名单非空），非法 → 422。娃娃角色不可读写管控配置（403）。

### 6.4 教材知识库检索设计（T11，故事 24/25 / AC-305 检索能力）

`TutorService.explain` 在**输入安全校验之后、调用模型之前**执行检索（`domain/retriever.py`）：

- **接口**：`KnowledgeRetriever.retrieve(subject, grade, knowledge_point, query) -> list[KnowledgeChunk]`（结构化文档：subject/grade/knowledge_point/content/source）。
- **当前实现**：`MockKnowledgeRetriever` 检索内置**自编**知识点库（学科精确 + 知识点/查询关键词子串匹配），**不含任何教材版权内容**。
- **注入方式**：命中 chunk 拼为「【知识库】」段落追加到 `context` 传入 provider（langchain 讲解会以知识库内容优先对齐教材口径）；未命中则上下文原样。检索内容不参与输入安全校验（校验对象仅为娃娃可输入字段）。
- **可演进**：`build_retriever()` 工厂按 `RETRIEVER_PROVIDER`（mock/builtin；vector 预留）切换，业务层不感知。
- **边界**：🔴 教材版权（ADR-012）为对外分发硬门槛，本层**仅实现检索能力，不解决授权**；上线前须换用已授权/公版内容源。

---

## 7. API 契约总表

基址：`/api/v1`（本地默认 `http://127.0.0.1:8000/api/v1`）。鉴权：`Authorization: Bearer <JWT>`。

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/auth/register` | 公开 | 家长注册（role=parent），返回 token+user |
| POST | `/auth/login` | 公开 | 登录，返回 token+user |
| POST | `/children` | 家长 | 新增娃娃账号（username/password/display_name/grade），自动绑 parent_id |
| GET | `/children` | 家长 | 列出当前家长的娃娃 |
| POST | `/tasks` | 家长 | 按 child_id/subject/grade/knowledge_point/qtype/difficulty/count 出题并建任务，返回含答案 |
| GET | `/tasks/today` | 娃娃 | 当前娃娃的全部任务+题目（不含答案） |
| POST | `/tasks/{id}/answer` | 娃娃 | 提交单题作答，返回 正确/分数/解析；答错自动归集错题集 |
| POST | `/tasks/{id}/checkin` | 家长或娃娃 | 任务打卡，标记 done 并写 checkin |
| GET | `/tasks/wrong-questions` | 娃娃 | 娃娃自查错题本（不含答案，防作弊） |
| GET | `/tasks/children/{id}/wrong-questions` | 家长 | 家长查某娃娃错题本（含答案/解析，供核查） |
| GET | `/tasks/children/{id}/progress` | 家长 | 娃娃进度：总量/正确数/正确率/连续打卡天数/打卡天数 |
| GET | `/tasks/children/{id}/mastery` | 家长 | 知识点掌握度看板（T06）：按知识点聚合分数/等级/活跃错题，含已掌握计数 |
| GET | `/review/due` | 娃娃 | 待复习队列（遗忘曲线到点的错题，不含答案防作弊） |
| POST | `/review/answer` | 娃娃 | 复习作答：复用批改逻辑，更新调度状态（答对推进/毕业，答错重置计时） |
| POST | `/tutor/ask` | 娃娃 | AI 伴学答疑（T08/T11/F-302）：{subject,grade,knowledge_point?,context?,question} → 适龄讲解；先过使用管控（T10：学科越界→403、次数/时长达限→429），再检索知识库注入上下文（T11），过内容安全层（F-304），最后落 `tutor_log` 并累计当日耗时 |
| GET | `/tutor/logs?child_id=` | 家长 | AI 答疑日志（T08/F-305）：某娃娃全部问答记录（越权→403），供家长监督 |
| GET | `/tutor/quota?child_id=` | 家长 | 查看某娃娃 AI 使用管控配置（T10，故事 23/26；未配置字段全 null=走全局默认；越权→403） |
| PUT | `/tutor/quota?child_id=` | 家长 | 设置 AI 使用管控（T10）：{daily_ask_limit?, daily_minutes_limit?, allowed_subjects?} 整体覆盖，null=清除该项；非法值→422 |
| GET | `/tutor/usage?child_id=` | 家长 | 某娃娃当日用量（提问次数+累计秒数），回带生效限额便于展示剩余 |
| GET | `/health` | 公开 | 健康检查 |

> 完整请求/响应字段见 `models.py`（SQLModel 即 schema）。后续三期会新增：`/knowledge/*`（知识库管理，AC-305 deferred，受版权硬门槛 ADR-012 约束）。

---

## 8. 配置与部署

### 8.1 配置项（`core/config.py` + `.env`）

| 变量 | 默认 | 说明 |
|---|---|---|
| `DATABASE_URL` | `sqlite:///./app.db` | 生产改 `postgresql+psycopg://...`（自动归一化 `postgres://` → `postgresql+psycopg://`） |
| `SECRET_KEY` | `changeme` | 生产必须改 |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | `11520`（8 天） | JWT 有效期 |
| `CORS_ORIGINS` | `["*"]` | 生产收紧为平板 App 域名/IP |
| `LLM_PROVIDER` | `mock` | `langchain` 通用 OpenAI 兼容端点；`deepseek` 走 DeepSeek 快捷预设 |
| `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` | 空 | 国产模型填 OpenAI 兼容端点（如混元/通义），`LLM_PROVIDER=langchain` 时使用 |
| `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` / `DEEPSEEK_MODEL` | 空 / `https://api.deepseek.com` / `deepseek-chat` | `LLM_PROVIDER=deepseek` 时使用，无需填 LLM_* |
| `LLM_TEMPERATURE` | `0.3` | 出题稳定性 |
| `TUTOR_DAILY_LIMIT` | `50` | AI 答疑每日提问次数的全局默认上限（F-304）；T10 起家长可按娃在 `/tutor/quota` 覆盖（`daily_ask_limit`），时长与学科范围管控不在此表、为按娃配置 |
| `RETRIEVER_PROVIDER` | `mock` | 知识库检索实现（T11/AC-305 检索能力）：`mock`/`builtin` = 内置自编知识点库（无版权风险）；`vector` = 预留向量库（未实现，未知值回退 mock 并告警） |

### 8.2 部署形态

- **本地开发**：`cd backend && python3.14 -m venv .venv && source .venv/bin/activate && pip install -e . && python -m app.main`，SQLite，MockProvider，零外部依赖即可演示。
- **家庭/生产**：`docker-compose up` 起 `postgres` + `backend`；backend 连 PostgreSQL（`postgresql+psycopg://wawa:wawa@db:5432/wawa`）。前端 Flutter 改 `kApiBase` 为局域网/公网地址即可。
- **可上云**：backend 无状态、DB 外置，天然可搬轻量云；CORS/密钥走环境变量，不写死。

---

## 9. 安全设计（非功能约束，对应 ADR-008/009）

1. **密码**：pwdlib 哈希（Argon2 优先 + bcrypt 兼容），不用明文。
2. **鉴权**：pyjwt（HS256），`api/deps.get_current_user` 解析；`require_parent` 限制家长专属接口，`require_child` 限制娃娃专属。
3. **授权**：任务/进度操作校验 `parent_id` / `child_id` 归属，越权返回 403/400。
4. **答案隔离**：娃娃端 never 拿到标准答案（见 §6）。
5. **内容安全（三期启用，框架先就位）**：
   - 系统层 prompt 锁死「只输出适合 X 岁、纯学习相关内容」（见 `langchain_provider._SYSTEM`）。
   - 输出后校验：`domain/safety.py` 做敏感词/越狱检测，异常则拦截并转家长。
   - 家长可查所有 AI 交互日志（`answer_records` / 未来 `tutor_logs`）。
   - 家长可设每日使用时长上限与内容范围（存 `users` 或独立 `parent_settings`）。

---

## 10. 已知技术债 / 待办

- [ ] 缺 Alembic 迁移：当前 `init_db()` 直接 `create_all`，生产需补回迁移工具（模板自带 alembic，可恢复）。T06 为 `answer_record` 新增 `created_at` 字段，无迁移则需删库重建。
- [ ] 缺后台任务框架：二期复习调度（遗忘曲线）需 APScheduler/Celery。
- [ ] 作答写多表非单事务：练习/复习流程分两次 `commit`（写 answer_record、更新错题/调度），极端并发下掌握度看板可能读到瞬时中间态；家庭场景可接受，生产前可合并为单事务。
- [ ] 缺限流/监控：对外服务前需加 rate limit 与基础观测。
- [ ] 缺模型输出校验重试：`LangChainProvider._parse` 失败兜底较粗糙，需结构化校验 + 重试。
- [ ] 缺单元测试：领域层（Grader 归一化、Provider 契约）应补测试。
- [ ] LangChain + Python 3.14 兼容性：需在接真实模型时验证 `langchain` 是否已支持 3.14。

---

> 本文档与 `功能规格_PRD_全三期.md` 的「技术约束」章节互为参照。任何架构变更需先更新本文档与 ADR，再改代码（正常开发流程）。
