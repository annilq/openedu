"""任务引导 SubAgent 清单。

**优先级 20**：高于 ``query``(12) / ``question``(0) / ``tutor``(-10)。这条优先级是本
subagent 存在的理由——「创建 / 派发」是**写意图**，而 ``query`` 的 ``triggers`` 含泛词
「任务 / 作业」，不压过它的话写意图必被只读查询接走（实测：``'帮我创建一个任务，包含四年级数学题'``
在只有 query/question/tutor 时路由结果为 ``query``）。

**triggers 必须具体**：路由是「词是否被包含」的匹配，泛词（``创建`` / ``建`` / ``任务``）
会把「查一下我创建过的任务」也拽过来。词表只收「动作 + 对象」的完整短语，
``hints`` 刻意留空——本 subagent 不接受启发式兜底，宁可漏给 ``query`` 也不误抢查询意图。

**roles 仅家长**：娃娃端没有布置任务的入口（``visible_businesses`` 按角色过滤，
这也是 ADR-0026 安全边界的一部分），娃娃端说「帮我创建任务」仍会落到 ``query`` 的只读答复。
"""
from __future__ import annotations

MANIFEST = {
    "business": "guide",
    "name": "任务引导",
    "description": "把「创建 / 派发任务」这类写意图引导到对应页面（助手只读，不代做写操作）。",
    "roles": ["parent"],
    "triggers": [
        "创建任务", "创建一个任务", "创建个任务", "创建一份任务", "创建新的任务",
        "新建任务", "新建一个任务", "新建个任务",
        "建个任务", "建一个任务",
        "创建作业", "新建作业", "建个作业", "建一个作业",
        "布置任务", "布置作业", "布置一下",
        "派发任务", "派发作业", "帮我派发",
        "添加任务", "加个任务", "加一个任务",
    ],
    # 留空是有意的：写意图需要**高精度**，启发式兜底会把查询意图一并抢来。
    "hints": [],
    "priority": 20,
    "skills": [],
}
