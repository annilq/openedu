# T13 · 家长端「添加娃娃」入口（补票，F-102 / AC-102）

> 背景：排查发现 UI 层**无创建娃娃账号入口**——后端 `POST /children`（T02）与前端数据层
> （`children_remote_data_source.dart` / `children_notifier.dart.createChild`）早已就绪，
> 但全项目无任何 UI 调用点，家长首页空态文案「还没有娃娃账号」后并无创建按钮。

## 目标
家长可在 App 内直接创建娃娃账号，创建后列表立即刷新并自动选中，便于接着布置任务。

## 实现
- **新页面** `features/children/presentation/screens/add_child_screen.dart`：
  表单（昵称 / 账号 / 密码 / 年级 1~6 下拉），前端校验（必填 + 密码 ≥4）、防重入 `_submitting`、
  错误兜底展示（账号重复 400 → 「已被使用」）；提交成功 `pop(true)`（notifier 内部已自动刷新）。
- **家长首页接入** `parent_dashboard.dart`：娃娃列表区块两处入口——
  空态 Card 内「添加娃娃」按钮（FilledButton.tonalIcon）、非空列表尾部 `_AddChildTile` 加号块
  （样式对齐 `_ChildSelector`）；`_openAddChild` 返回成功后，若此前未选中娃娃则自动选中第一个。
- **测试** `test/children_notifier_test.dart`：
  - notifier（mocktail）：createChild 成功传参并刷新列表 / 失败进入 Error / loadChildren 四态
  - widget：AddChildScreen 空字段提示必填、密码不足 4 位提示长度

## 代码审查修复（general-purpose agent）
1. `_friendlyError` 的 `contains('400')/contains('422')` 分支失效（dio 只透出 detail 字符串）→ 删除，改为语义匹配（registered/已注册）+ 通用兜底。
2. 建号成功但刷新列表失败误报"创建失败"→ `createChild` 改为返回 `UserModel?`（成功返回新用户并刷新），页面按返回值判定成功，兜底文案提示"可能已创建成功，请返回刷新查看"。
3. 自动选中改为**定向选中**：页面 `pop(created.id)`，首页按返回的新用户 id 精确选中（不再依赖列表排序）。
4. 硬编码间距改为 `AppSpacing` 令牌；密码不再 trim（保留真实密码，仅校验空白）；`_AddChildTile` 定高 `minHeight: 64` 与 `_ChildSelector` 对齐。

## 测试结果
`flutter analyze` 0 问题；`flutter test` **31 passed**（新增 5 条）。

## 提交
见 `git log`（feat(frontend): 家长端添加娃娃账号入口 ...）。
